@extends('layouts.admin')
@section('title', 'Parent Journeys')
@section('content')
<div class="container-fluid py-3"><h3 class="fw-bold mb-1">Parent / Guardian Journeys</h3><div class="text-muted small mb-3">Decision-maker communication, reminders, and next actions.</div><div class="row g-2 mb-3">@foreach($dashboard['stats'] as $label => $value)<div class="col-6"><div class="card border-0 shadow-sm"><div class="card-body py-2"><div class="small text-muted">{{ str_replace('_',' ',$label) }}</div><div class="fs-4 fw-bold">{{ $value }}</div></div></div></div>@endforeach</div><div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table table-sm mb-0" aria-label="Parent guardian journeys"><thead><tr><th>Guardian</th><th>Subject</th><th>Decision</th><th>Next Action</th><th></th></tr></thead><tbody>@foreach($dashboard['journeys'] as $journey)<tr><td>{{ $journey->guardian_name }}<div class="small text-muted">{{ $journey->guardian_phone }}</div></td><td>{{ class_basename($journey->subject_type) }} #{{ $journey->subject_id }}</td><td>{{ $journey->decision_status }}</td><td>{{ $journey->next_action }}</td><td><form method="POST" action="{{ route('admission.parent-journeys.reminder', $journey) }}">@csrf<button class="btn btn-sm btn-outline-primary">Reminder</button></form></td></tr>@endforeach</tbody></table></div><div class="card-footer bg-white">{{ $dashboard['journeys']->links() }}</div></div></div>
@endsection
