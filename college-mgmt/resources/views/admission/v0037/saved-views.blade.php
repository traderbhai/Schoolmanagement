@extends('layouts.admin')
@section('title', 'Admission Saved Views')
@section('content')
<div class="container-fluid py-3"><h3 class="fw-bold mb-1">Admission Saved Views</h3><div class="text-muted small mb-3">Reusable filters for control room, counsellor desk, conflicts, performance, and automation logs.</div><form method="POST" action="{{ route('admission.saved-views.store') }}" class="card border-0 shadow-sm mb-3">@csrf<div class="card-body row g-2"><div class="col-md-3"><input class="form-control" name="surface" value="assessment_control_room" aria-label="Surface"></div><div class="col-md-3"><input class="form-control" name="name" placeholder="View name" required aria-label="View name"></div><div class="col-md-4"><input class="form-control" name="filters_json" value='{"status":"pending"}' aria-label="Filters JSON"></div><div class="col-md-2"><button class="btn btn-primary w-100">Save</button></div></div></form><div class="card border-0 shadow-sm"><table class="table table-sm mb-0" aria-label="Saved views"><thead><tr><th>Name</th><th>Surface</th><th>Filters</th></tr></thead><tbody>@foreach($views as $view)<tr><td>{{ $view->name }}</td><td>{{ $view->surface }}</td><td><code>{{ json_encode($view->filters) }}</code></td></tr>@endforeach</tbody></table></div></div>
@endsection
