<div class="card shadow-sm mb-3"><div class="card-header py-2 fw-semibold">Timetable Versions</div><div class="table-responsive"><table class="table table-sm mb-0"><thead><tr><th>Version</th><th>Program</th><th>Status</th><th>Effective</th><th>Lifecycle Actions</th></tr></thead><tbody>@foreach($versions as $version)<tr><td>Version {{ $version->version_number }}</td><td>{{ $version->program?->code }}</td><td>{{ $version->status }}</td><td>{{ optional($version->effective_from)->format('d M Y') }}</td><td><div class="d-flex flex-wrap gap-1"><form method="POST" action="{{ route('academics.pmc.timetable-versions-v041.freeze', $version) }}">@csrf<input type="hidden" name="decision_reason" value="Frozen from PMC version board"><button class="btn btn-xs btn-outline-primary py-0 px-1">Freeze</button></form><form method="POST" action="{{ route('academics.pmc.timetable-versions-v041.unfreeze', $version) }}">@csrf<input type="hidden" name="decision_reason" value="Revision required after operational review"><button class="btn btn-xs btn-outline-warning py-0 px-1">Unfreeze</button></form><form method="POST" action="{{ route('academics.pmc.timetable-versions-v041.rollback', $version) }}">@csrf<input type="hidden" name="decision_reason" value="Rollback requested after impact review"><button class="btn btn-xs btn-outline-danger py-0 px-1">Rollback</button></form></div></td></tr>@endforeach</tbody></table></div><div class="card-footer py-2">{{ $versions->links() }}</div></div>
@isset($workflows)
<div class="card shadow-sm mb-3"><div class="card-header py-2 fw-semibold">Version Lifecycle Workflow</div><div class="table-responsive"><table class="table table-sm mb-0"><thead><tr><th>Version</th><th>Lifecycle</th><th>Approval</th><th>Reason</th><th>Published/Frozen</th></tr></thead><tbody>@forelse($workflows as $workflow)<tr><td>{{ $workflow->timetableVersion ? 'Version '.$workflow->timetableVersion->version_number : 'Version pending' }}</td><td>{{ $workflow->lifecycle_status }}</td><td>{{ $workflow->approval_status }}</td><td>{{ $workflow->decision_reason ?: $workflow->override_reason }}</td><td>{{ optional($workflow->published_at ?: $workflow->frozen_at)->format('d M Y H:i') }}</td></tr>@empty<tr><td colspan="5" class="text-muted">No lifecycle workflow records.</td></tr>@endforelse</tbody></table></div><div class="card-footer py-2">{{ $workflows->links() }}</div></div>
@endisset
<div class="card shadow-sm mb-3"><div class="card-header py-2 fw-semibold">Change Requests</div><div class="table-responsive"><table class="table table-sm mb-0"><thead><tr><th>Change</th><th>Status</th><th>Reason</th><th>Decision</th></tr></thead><tbody>@foreach($changes as $change)<tr><td>{{ $change->change_type }}</td><td>{{ $change->status }}</td><td>{{ $change->reason }}</td><td><form method="POST" action="{{ route('academics.pmc.timetable-change-requests.decide', $change) }}" class="d-flex gap-1">@csrf @method('PATCH')<input type="hidden" name="status" value="approved"><input class="form-control form-control-sm" name="decision_note" placeholder="Decision note"><button class="btn btn-sm btn-outline-primary">Approve</button></form></td></tr>@endforeach</tbody></table></div><div class="card-footer py-2">{{ $changes->links() }}</div></div>
<div class="card shadow-sm"><div class="card-header py-2 fw-semibold">Impact Records</div><div class="table-responsive"><table class="table table-sm mb-0"><thead><tr><th>Impact</th><th>Affected</th></tr></thead><tbody>@foreach($impacts as $impact)<tr><td>{{ $impact->title }}</td><td>{{ $impact->affected_count }}</td></tr>@endforeach</tbody></table></div><div class="card-footer py-2">{{ $impacts->links() }}</div></div>
