<div class="card shadow-sm mb-3"><div class="card-header py-2 fw-semibold">Substitution Recommendations</div><div class="table-responsive"><table class="table table-sm mb-0"><thead><tr><th scope="col">Group</th><th scope="col">Original</th><th scope="col">Substitute</th><th scope="col">Date</th><th scope="col">Score</th><th scope="col">Why Recommended</th><th scope="col">Checks</th></tr></thead><tbody>@foreach($recommendations as $rec)<tr><td>{{ $rec->courseGroup?->name }}</td><td>{{ $rec->originalTeacher?->user?->name }}</td><td>{{ $rec->substituteTeacher?->user?->name ?? 'Uncovered' }}</td><td>{{ optional($rec->substitution_date)->format('d M') }}</td><td>{{ $rec->score }}</td><td class="small">{{ collect($rec->reasons ?? [])->map(fn($reason) => str_replace('_', ' ', $reason))->join(', ') }}</td><td class="small">@foreach(($rec->conflict_checks ?? []) as $key => $value)@if($key !== 'ranked_candidates')<div>{{ str_replace('_', ' ', $key) }}: {{ is_array($value) ? json_encode($value) : $value }}</div>@endif @endforeach</td></tr>@endforeach</tbody></table></div><div class="card-footer py-2">{{ $recommendations->links() }}</div></div>
<div class="card shadow-sm"><div class="card-header py-2 fw-semibold">Timetable Notifications</div><div class="table-responsive"><table class="table table-sm mb-0"><thead><tr><th scope="col">Notification</th><th scope="col">Recipient</th><th scope="col">Status</th></tr></thead><tbody>@foreach($notifications as $notification)<tr><td>{{ $notification->title }}</td><td>{{ $notification->recipient_type }}</td><td>{{ $notification->status }}</td></tr>@endforeach</tbody></table></div><div class="card-footer py-2">{{ $notifications->links() }}</div></div>
