# SchoolManagement Codex Project Context

## Runtime
- Laravel/PHP app in `C:\Users\mohd.naved\Documents\SchoolManagement\college-mgmt`.
- Local demo server usually runs on `http://localhost:8001`.
- Test runtime: `C:\tmp\php-8.5.7\php.exe` with `PHPRC=C:\tmp\php-8.5.7-codex-ini`.
- Run tests with: `$env:PHPRC='C:\tmp\php-8.5.7-codex-ini'; C:\tmp\php-8.5.7\php.exe artisan test`.

## Demo Users
- Admin: `admin@college.com` / `password`.
- Dean Academics: `dean@college.com` / `password`.
- PMC Head / Program Chair: `chair@college.com` / `password`.
- PMC Manager: `pmc.manager@college.com` / `password`.
- CoE: `exam@college.com` / `password`.
- IQAC Head: `iqac.head@college.com` / `password`.
- Program Leader / HOD: `hod@college.com` / `password`.
- Faculty mentor: `faculty.mentor@college.com` / `password`.

## Completed Department Baselines
- Admission OS completed through v0.039, including hierarchy, workflows, communication safety, assessments, offer/seat control, and Admission-to-Academics handoff.
- Academics OS v0.01 created flexible Academics hierarchy for Dean Office, PMC, CoE/Examination, IQAC, and Program Leadership.
- Academics OS v0.011 added Academics command center and attention workspaces.
- PMC OS v0.02 added initial PMC dashboard: curriculum readiness, faculty allocation, timetable readiness, student monitoring, and basic reports.
- PMC OS v0.03 added PMC command desk, workbench, curriculum governance, faculty workload, timetable control, student success, reviews/actions, saved views, reports, exports, seeder data, and tests.
- PMC OS v0.04 added complete PMC real-world operating workflows for planning, curriculum governance, faculty allocation, timetable governance, delivery, student success, reviews/actions, approvals, automation, analytics, exports, policy audit, seeded demo data, and tests.
- PMC OS v0.041 added realistic pre-timetable and timetable completion: term-wise student course allocation, student baskets, core sections, elective/lab/tutorial groups, exact group faculty assignments, faculty preferences/workload rules, locked/manual slots, constraint-based generator, conflict/quality scoring, planner views, version/freeze/impact workflows, substitution intelligence, notifications, reports, seeder data, and focused tests.
- PMC OS v0.042 upgraded timetable production realism: elective choice-window allocation with capacity/waitlist decisions, auto section/group build runs, enhanced timetable validation for student clashes, faculty availability, adjunct-day constraints, room capacity/type mismatch, daily faculty load, explainable quality scoring, solver attempt diagnostics, publish blocker checks, seeded demo data, browser-verified pages, and focused tests.
- PMC OS v0.043 added timetable publish/freeze lifecycle governance: publish from generation runs, block PMC publish when hard checks exist, Dean/Admin override with reason, official timetable version creation, workflow companion records for published/frozen/unfrozen/rollback states, lifecycle notifications, version board actions, seeded demo data, browser verification, and focused tests.
- PMC OS v0.044 added official scoped timetable views: PMC official audience timetable, student self-view based on exact course-group membership, faculty self-view based on group-faculty assignment, role-safe routes, compact filterable/paginated UI, browser-verified PMC page, and focused tests for student/faculty scope.
- PMC OS v0.045 added conflict resolution governance: detected timetable constraints can be converted into owner-assigned resolution actions, actions carry priority/status/due/evidence/closure notes, closing actions updates publish-check state, planner/reports show resolution actions, demo data includes open and resolved examples, and focused tests verify action lifecycle.
- PMC OS v0.046 added faculty availability governance: faculty/adjunct availability request submission, PMC approval/return/rejection workflow, approved requests apply to timetable faculty preference records used by the generator, PMC and faculty availability pages, seeded request/preference examples, browser verification, and focused tests.
- PMC OS v0.047 added faculty load governance: workload reviews are calculated from exact group faculty assignments, generated timetable items, and approved faculty preferences; reviews classify normal/underload/overload/critical load, support PMC approval/overload decisions with notes, display on load-planning pages, and are seeded/tested.
- PMC OS v0.048 added room/lab/resource readiness governance: reviews are calculated from generated timetable items and classrooms, capacity/lab/high-utilization risks are classified, PMC/Dean decisions require notes for exceptions/revisions, publish checks are updated from open room readiness blockers, reports display refresh/decision controls, and demo data/tests cover the workflow.
- PMC OS v0.049 hardened PMC timetable write scope: high-risk setup actions now enforce program/batch/term/subject write scope for PMC managers/officers/program leaders, while Dean Academics, Academic Owner, Director, Admin, and PMC Head retain override/oversight authority; tests prove in-scope writes pass and out-of-scope writes are blocked.
- PMC OS v0.050 added course basket exception governance before timetable locking: PMC can request add/drop/repeat/backlog/improvement/audit/open-elective exceptions, decisions update student course allocations, Dean-only approval is enforced for improvement/audit/credit-overload cases, exception flags are visible on course allocation pages, and seeded demo/tests cover the workflow.
- PMC OS v0.051 added section/group adjustment governance: PMC can request split/merge/rebalance/student-move/lock/unlock changes, impact and strength changes are recorded, Dean approval is enforced for merge/unlock/capacity-risk cases, approved changes update group strength/status/lock state and membership safely even when duplicate target membership exists, and group builder pages show adjustment controls and review queues.
- PMC OS v0.052 added faculty assignment acknowledgement governance: PMC can request acknowledgement for exact section/group faculty assignments, assigned faculty can accept, accept with constraints, raise concerns, or decline, PMC/Dean can review concerns with audit notes, unresolved acknowledgements update publish checks, and faculty-allocation pages show acknowledgement constraints.
- PMC OS v0.053 removed raw-ID entry from key PMC timetable operating forms by adding database-backed selectors for programs, batches, terms, subjects, students, course groups, faculty, rooms, slots, and timetable versions across allocation, grouping, faculty assignment, locked slots, generator, substitution, change request, availability, and scoped timetable filters.
- PMC OS v0.054 upgraded substitution intelligence from first-active-teacher selection to deterministic ranking by same-slot conflicts, faculty availability, adjunct-day fit, subject expertise, backup assignment, daily/weekly workload, and explainable reasons/conflict checks displayed on the substitution page.
- PMC OS v0.055 added curriculum validation control: refreshable database-backed validations for syllabus version, credit rules, course outcomes, CO-PO mapping, and rollout readiness; curriculum readiness scores update from validations and the curriculum page shows blockers and recommended actions.
- PMC OS v0.056 added concrete PMC planning/readiness workflow: planning cycles for annual/semester/program-term readiness, generated readiness checklist sections, blocker counts, checklist progress updates, planning approval/status changes, and conversion of readiness blockers into accountable PMC work items.
- PMC OS v0.057 added concrete student success intervention workflow: source-backed risk refresh from attendance/exam/grievance/mentor signals, retention-risk plans, intervention lifecycle records, parent/guardian escalation records, quick mentor/parent commands, seeded demo data, rendered student-success UI, and focused tests.
- PMC OS v0.058 added concrete course delivery checkpoint workflow: source-backed delivery refresh from timetable/attendance/marks/feedback/faculty assignment signals, planned-vs-conducted/missed sessions, delivery risk bands/scores, remedial action lifecycle, seeded demo data, rendered delivery UI, and focused tests.
- PMC OS v0.059 added section/group-level course delivery tracking: delivery trackers per exact course group, session delivery logs linked to timetable generation items, risk reasons/actions for missed group sessions and pending faculty logs, seeded demo data, compact delivery UI sections, and focused tests.
- PMC OS v0.060 added advanced PMC review/action governance: minutes approval with auto-created follow-up actions, action dependencies, reminders/escalations, action evidence register, evidence-based closure verification, seeded demo data, rendered review governance UI, and focused tests.
- PMC OS v0.061 added demand-driven timetable generation: weekly session demand records per course group, multi-session scheduling from faculty weekly hours/group rules, session type/index/duration tracking on generated items, operational timetable-entry sync on publish, seeded demand data, rendered timetable demand UI, and focused tests.
- PMC OS v0.062 hardened timetable solver quality: break/lunch slots are excluded from teaching placement, multi-slot labs/tutorials require physically contiguous non-break slot blocks, occupancy blocks every slot in a multi-slot class for faculty/room/group/student checks, break-slot and incomplete-block hard constraints are detected, student compactness uses slot order, seeded demo data shows break-slot diagnostics, and focused tests cover the rules.
- PMC OS v0.063 improved operational timetable quality checks: faculty consecutive-teaching pressure is flagged from actual slot order, student/course-group daily load pressure is detected from configured group limits, avoidable same-day student gaps are flagged, compactness scoring expands multi-slot sessions by ordered slots, seeded reports show the new warnings, and focused tests cover the quality rules.
- PMC OS v0.064 added strategy-aware timetable placement: feasible day/slot/room candidates are now ranked instead of accepting the first feasible slot, strategies influence placement for student compactness, faculty balancing, adjunct priority, and room optimization, generated items store placement score/reasons, solver diagnostics record strategy-aware scoring, seeded demo data reflects the strategy ranking, and focused tests prove compact-student placement behavior.
- PMC OS v0.065 added timetable solver explainability for manual PMC review: generated items store the top alternate day/slot/room candidates with score and reasons, the generator table shows solver score/reasons/alternatives, the generation item model exposes its generation-run relationship, seeded demo rows include alternatives, and focused tests verify alternatives persist and render.
- PMC OS v0.066 made solver alternatives actionable: PMC can apply a stored alternate placement from the generator table, the route validates the stored alternative and draft run state, updates day/slot/room/confidence/metadata, preserves previous placement, refreshes timetable quality/publish checks, writes an audit event, and focused tests cover the workflow.
- PMC OS v0.067 added conflict-safe solver alternative application: applying an alternative now refreshes pre/post quality, rolls back automatically if the move introduces new hard conflicts, records blocked-alternative metadata, allows only Dean/Admin/Academic Owner/Director override with explicit reason, audits override use, and focused tests cover rollback and override paths.
- PMC OS v0.068 added safe manual generated-item moves: PMC can manually move a generated class to a selected day/slot/room from the generator table, the move validates draft run state and scoped access, refreshes quality/publish checks, rolls back if new hard conflicts appear, allows Dean/Admin/Academic Owner/Director override with explicit reason, writes audit events, and focused tests cover safe move, rollback, and override.
- PMC OS v0.069 added source-backed pre-publish timetable impact preview: PMC can refresh impact rows from a generation run before publish/revision, including affected students, faculty, rooms, course groups, manual/alternative changes, conflicts, and notification audience; results are stored in timetable impact records, linked in run metadata, shown on the generator page, audited, seeded-compatible, browser-verified, and tested.
- PMC OS v0.070 made timetable publish impact-safe by automatically refreshing source-backed generation impact preview during every successful publish, embedding the reviewed impact summary into the lifecycle workflow publish summary, preserving the existing hard-conflict gate, and testing the official publish path.
- PMC OS v0.071 added impact-aware publish notification metadata: queued student/faculty timetable publish notifications now carry generation run id, reviewed impact preview, audience count, quality/conflict summary, and operational sync counts so notification rows are auditable and tied to the publish impact review.
- PMC OS v0.072 upgraded timetable notification reports: reports now filter notifications by type/recipient/status and show compact operational context from notification metadata, including audience count, affected student/faculty/room/group counts, operational sync count, generation run, quality score, and hard/soft conflicts.
- PMC OS v0.073 added individual publish notification audit: successful timetable publish now creates per-faculty and per-student queued notification rows from generated timetable items and active course-group memberships, preserving the existing audience-level rows while enabling recipient-level delivery/status tracking.
- PMC OS v0.074 added notification delivery-state operations: PMC can mark timetable notifications queued/sent/read/failed/cancelled from reports, failure/cancellation requires a note, status history is retained in notification metadata, and every status change is audited.
- PMC OS v0.075 added timetable notification retry operations: failed/cancelled notifications can be requeued from reports, retry count/notes/next retry time/status history are retained in metadata, retry actions are audited, and seeded demo data exposes the retry control.
- PMC OS v0.076 added a guided semester launch control on the PMC Timetable OS dashboard: ordered launch stages cover course baskets, sections/groups, faculty allocation, readiness inputs, generation/validation, and publish/notify with blocker counts, next action guidance, and filtered source links.
- PMC OS v0.077 added course-basket readiness diagnostics: PMC now sees total/ready allocations, ungrouped baskets, waitlists, pending exceptions, credit overload baskets, validation-flagged allocations, recommended action text, and launch-stage blocker integration on timetable and student-basket pages.
- PMC OS v0.078 added section/group readiness diagnostics: PMC now sees total/ready groups, unlocked groups, under-minimum groups, over-capacity groups, groups without faculty, ungrouped allocations, pending adjustments, strength mismatches, recommended action text, and launch-stage blocker integration on timetable and group-builder pages.
- PMC OS v0.079 added faculty allocation readiness diagnostics: PMC now sees exact assignment readiness, assigned groups, groups missing primary faculty, groups without backup coverage, pending/missing acknowledgements, missing faculty preferences, unapproved assignments, load-review blockers, overload reviews, recommended action text, and launch-stage blocker integration on timetable and faculty-allocation pages.
- PMC OS v0.080 added readiness-input diagnostics: PMC now sees faculty preference completeness/restriction signals, active/hard/soft locked slots, missing locked-slot context, hard-lock collisions, room-readiness blockers, lab-not-ready counts, capacity exceptions, recommended action text, and launch-stage blocker integration on timetable and locked-slot/readiness pages.
- PMC OS v0.081 added generation/validation diagnostics: PMC now sees latest generation status, scheduled/unscheduled classes, hard conflicts, soft warnings, quality score/band, solver attempts, failed attempts, open resolution actions, blocking publish checks, impact-preview readiness, stale input-source count, recommended action text, and launch-stage blocker integration on timetable and generator pages.
- PMC OS v0.082 added publish/freeze readiness diagnostics: PMC now sees official version status, lifecycle/approval status, missing official version/workflow flags, blocking publish checks, pending change requests, failed/queued notifications, operational sync count, impact records, affected students/faculty, recommended action text, version-board links, failed-notification report links, and launch-stage blocker integration on timetable and version/freeze pages.
- PMC OS v0.083 added substitution emergency diagnostics: PMC now sees today/tomorrow substitution recommendations, uncovered classes, pending and low-score recommendations, failed/queued substitution notices, same-day change requests, repeated faculty and repeated course-group substitution risk, blocker totals, and recommended action text on timetable and substitution-intelligence pages.
- PMC OS v0.084 added course-delivery execution diagnostics: PMC now sees faculty session-log gaps, planned/missed/cancelled/rescheduled sessions, attendance/lesson-plan/material update gaps, missing planned/covered topics, behind groups, overdue reviews, open remedials, blocker totals, and recommended action text on course-delivery and delivery-risk pages.
- PMC OS v0.085 added student-success intervention effectiveness diagnostics: PMC now sees overdue interventions, stale risk-plan reviews, parent-call follow-through, missing parent outcomes, intervention evidence gaps, repeat-risk students, escalated interventions, closure effectiveness rate, blocker totals, and recommended action text on student-success/intervention pages; the legacy student-success page links to the current v0.085 surface.
- PMC OS v0.086 added high-risk PMC route enforcement diagnostics and hardening: generation validation/publish, timetable freeze/unfreeze/rollback, conflict resolution actions, version-linked change requests, and group-linked substitution recommendations now use scope-aware checks where source context exists; the policy audit page shows tracked high-risk routes, scope-aware routes, broad-write routes, missing audit rows, untested rows, and recommended action text.
- PMC OS v0.087 added allocation pressure diagnostics: PMC now sees elective choice-window pressure, submitted/allocated/waitlisted choices, pending choice students, waitlist subjects, add/drop exceptions, repeat/backlog cases, Dean-approval pending cases, duplicate student-subject baskets, active students with no basket, single-course baskets, manual overrides, recent allocation rounds, pressure totals, and recommended action text on the timetable dashboard plus allocation/student-basket pages.
- PMC OS v0.088 added faculty suitability diagnostics: PMC now sees subject expertise gaps, adjunct-day availability risk, restriction notes/unavailable slots, acknowledgement concerns/declines, overload risks, unapproved suitability, backup-only primary gaps, suitable assignment count, blocker totals, and recommended action text on timetable and section/group faculty-allocation pages.
- PMC OS v0.089 made faculty suitability operational instead of informational: suitability is now a readiness checklist item, the launch-control faculty stage includes suitability/expertise blockers, generation surfaces sync a run-scoped `faculty_suitability` publish check, normal PMC publish is blocked when the generated run's exact course groups have suitability blockers, Dean/Admin override behavior remains available, and term/run scoping prevents unrelated faculty preferences or load reviews from blocking the wrong timetable.
- PMC OS v0.090 added student course basket self-service: students can review allocated/waitlisted courses, group/section membership, timetable preview, submit allocation acknowledgements, raise objections/add-drop/waitlist follow-ups, PMC can review those requests, objection/add-drop submissions create allocation-exception records, demo data targets the primary demo student, and focused tests/browser checks verify the flow.
- PMC OS v0.091 added student elective choice self-service: students can view active elective choice windows, rank elective preferences, submit choices with duplicate/max-window validation, choices are stored in `academic_pmc_elective_choices`, PMC allocation consumes submitted choices through the existing allocation processor, demo data includes an open window, and focused tests verify the student-to-PMC allocation path.
- PMC OS v0.092 added PMC data reconciliation and health checks: PMC can refresh and review database-backed checks for generated-to-operational timetable sync, allocated course baskets versus student subject enrollments, group memberships versus approved allocations, scheduled groups versus delivery trackers, and published timetable notification coverage; seeded demo rows expose OK/warning states, lifecycle-linked timetable runs are counted across publish/freeze/revision states, and focused tests verify mismatch detection.
- PMC OS v0.093 made reconciliation actionable: mismatched checks can now run scoped repair actions for generated-to-operational timetable sync, allocation-to-enrollment links, group membership allocation links, scheduled-group delivery trackers, and missing timetable publish notification audit rows; repairs are write-authorized, audited, refresh reconciliation after execution, respect legacy enrollment enum values, and focused tests verify mismatch repair.
- PMC OS v0.094 added source-backed reconciliation drilldown: every refresh stores bounded sample mismatch records for timetable sync, allocation/enrollment links, group membership links, delivery tracker coverage, and publish notification coverage; the reconciliation table renders compact sample mismatches so operators can see representative affected records before running repair.
- PMC OS v0.095 added reconciliation export/audit: PMC users can export the current filtered data reconciliation view as CSV with counts, status, recommendations, and sample mismatches; exports honor status/group filters and write `academic_pmc_export_logs` records for auditability.
- PMC OS v0.096 added scheduled/CLI data reconciliation: `academics:pmc-reconcile-data` refreshes checks through the existing policy-authorized service, supports `--repair` for safe repair actions, uses admin/Dean/PMC actor fallback without bypassing authorization, is scheduled hourly, and is covered by command tests.
- PMC OS v0.097 added reconciliation run observability: scheduled/manual CLI reconciliation executions now persist run history with source, status, repair flag, actor, check/mismatch/critical/repair counts, failure reason, and metadata; the reconciliation page shows recent runs so PMC can audit scheduler health without server log access.
- PMC OS v0.098 completed reconciliation demo readiness: `AcademicsOperatingDemoSeeder` now seeds realistic completed, repair, and failed reconciliation run-history rows so the reconciliation page is database-populated immediately after a fresh demo seed, and focused tests assert the seeded run history renders.
- PMC OS v0.099 completed manual reconciliation audit coverage: UI-triggered Refresh Checks and per-check Repair actions now write `academic_pmc_data_reconciliation_runs` records with actor, source, repair flag, counts, repaired count, route metadata, and check key, so scheduled and manual reconciliation operations share one auditable run history.
- PMC OS v0.100 hardened manual reconciliation failure observability: UI refresh and repair actions now create a running run-history row before work starts and update it to completed or failed with failure reason and exception metadata, so failed manual reconciliation attempts are auditable instead of disappearing into request logs.
- PMC OS v0.101 made reconciliation run history actionable: the data reconciliation page now shows run summary counts for total/completed/failed/running/manual-repair runs, supports `run_status` filtering with visible filter summary, and links failed/running/completed run counts to filtered run-history views.
- PMC OS v0.102 added reconciliation run-history exports: PMC can export the current filtered run-history view as CSV, exports honor `run_status`, include source/status/repair/count/failure details, and write `academic_pmc_export_logs` rows with report key `data_reconciliation_runs`.
- PMC OS v0.103 added reconciliation scheduler health signals: the reconciliation page now shows scheduler health status, last successful run, last failed run, stale running count, and recommended action; stale running runs are seeded for demo warning coverage and running-run notes are visible in the run table.
- PMC OS v0.104 added stale reconciliation run closure: PMC can mark stale running reconciliation runs as failed with a required reason, fresh running runs are protected from premature closure, closure metadata records actor/time/reason, and the run-history table exposes the action only for eligible stale runs.
- PMC OS v0.105 added department activity audit for stale run closure: marking a stale reconciliation run failed now writes a `DepartmentActivityLog` entry with action `academic_pmc_v105_reconciliation_stale_run_closed`, subject run id/type, actor, reason, source, and started-at metadata.
- PMC OS v0.106 made reconciliation audit operationally visible: the data reconciliation page now shows a compact recent audit trail for refresh, repair, stale-run closure, and reconciliation subject activity, including actor, action, reason/details, timestamp, and subject reference.
- PMC OS v0.107 added reconciliation audit export governance: PMC can export the recent reconciliation audit trail as CSV with action filtering, actor/action/details/subject columns, and `academic_pmc_export_logs` records under report key `data_reconciliation_audit`.
- PMC OS v0.108 added operator-facing reconciliation audit filtering: the data reconciliation page now exposes an Audit Action filter, visible audit filter summary, and all-audit reset controls while preserving status/group/run filters.
- PMC OS v0.109 completed demo readiness for reconciliation audit governance: `AcademicsOperatingDemoSeeder` now seeds database-backed refresh, repair, and stale-run closure audit rows tied to demo reconciliation checks/runs, so the audit trail and filters are populated immediately after a fresh seed.
- PMC OS v0.110 added reconciliation audit date-window governance: the audit trail page controls and CSV export now support `audit_from`/`audit_to` filters, visible date filter summaries, export-log capture of action/from/to filters, and empty-state behavior for date windows with no audit events.
- CoE OS v0.03, IQAC OS v0.04, Program Leadership OS v0.05, Course Delivery OS v0.06 are present as operating slices.
- Dean Academics OS v0.07 added Dean command dashboard, attention queues, branch health, program risk, reviews/actions, handoff, calendar, reports, and exports.
- Dean Academics OS v0.08 added planning cycles, advanced reviews/actions, risk governance, approval cockpit, faculty workload, student success, curriculum, exam readiness, quality, induction, analytics, calendar, saved views, and policy audit.

## Current Sprint
- Academics PMC OS v0.110 is the current verified baseline.
- Scope delivered: v0.041 timetable workflow plus v0.042 elective choice allocation/auto grouping/solver validation, v0.043 publish/freeze/rollback lifecycle governance, v0.044 official scoped timetable views, v0.045 conflict resolution actions, v0.046 faculty availability request/approval workflow, v0.047 faculty load review/approval workflow, v0.048 room/lab readiness review/publish-check workflow, v0.049 record-level write-scope enforcement for high-risk timetable setup actions, v0.050 course basket exception governance for add/drop/repeat/backlog/improvement/audit/open-elective workflows, v0.051 section/group adjustment governance for split/merge/rebalance/student-move/lock/unlock workflows, v0.052 faculty assignment acknowledgement governance, v0.053 database-backed selector controls for key PMC timetable forms, v0.054 ranked substitution intelligence with explainable checks, v0.055 curriculum validation control, v0.056 concrete PMC planning/readiness workflow, v0.057 concrete student success intervention workflow, v0.058 concrete course delivery checkpoint/remedial workflow, v0.059 exact section/group/session-level delivery tracking, v0.060 advanced review/action governance with evidence-based closure verification, v0.061 weekly session-demand timetable generation plus operational timetable sync, v0.062 break-safe contiguous multi-slot timetable placement with hard validation constraints, v0.063 operational timetable quality checks for faculty consecutive load, student group daily load, and avoidable student gaps, v0.064 strategy-aware candidate ranking for timetable placement, v0.065 solver alternatives/explainability for manual PMC review, v0.066 apply-alternative workflow with audit/quality refresh, v0.067 conflict-safe alternative apply rollback/override governance, v0.068 safe manual generated-item moves from selected day/slot/room with rollback/override governance, v0.069 source-backed pre-publish timetable impact preview, v0.070 automatic publish-time impact refresh in lifecycle summaries, v0.071 impact-aware publish notification metadata, v0.072 metadata-aware notification reporting with filters, v0.073 individual publish notification audit rows, v0.074 notification delivery-state operations, v0.075 retry operations for failed/cancelled timetable notifications, v0.076 guided semester launch control with ordered stages, blockers, next actions, and source links, v0.077 course-basket readiness diagnostics integrated into launch blockers and student basket pages, v0.078 section/group readiness diagnostics integrated into launch blockers and group-builder pages, v0.079 faculty allocation readiness diagnostics integrated into launch blockers and faculty allocation pages, v0.080 readiness-input diagnostics integrated into launch blockers and locked-slot/readiness pages, v0.081 generation/validation diagnostics integrated into launch blockers and timetable-generator pages, v0.082 publish/freeze readiness diagnostics integrated into launch blockers and version/freeze pages, v0.083 substitution emergency diagnostics integrated into timetable and substitution-intelligence pages, v0.084 course-delivery execution diagnostics integrated into course-delivery and delivery-risk pages, v0.085 student-success intervention effectiveness diagnostics integrated into student-success/intervention pages with a legacy-page link, v0.086 high-risk route enforcement diagnostics plus scope-aware hardening for key timetable lifecycle actions, v0.087 allocation pressure diagnostics integrated into timetable/allocation/student-basket pages, v0.088 faculty suitability diagnostics integrated into timetable/faculty-allocation pages, v0.089 run-scoped faculty suitability publish enforcement integrated into readiness, launch control, generator publish checks, and publish blocking, v0.090 student course basket self-service with acknowledgements, objections, add/drop/waitlist follow-up, PMC review, and demo-student data, v0.091 student elective choice self-service feeding the existing PMC elective allocation processor, v0.092 data reconciliation checks for timetable sync, allocation/enrollment links, group memberships, delivery trackers, and notification coverage, v0.093 write-authorized reconciliation repair actions for fixable data drift, v0.094 source-backed sample mismatch drilldown on reconciliation checks, v0.095 filtered CSV export plus export audit logging for reconciliation reports, v0.096 scheduled CLI reconciliation command with optional repair, v0.097 durable run-history observability for scheduled/manual reconciliation executions, v0.098 seeded demo run-history rows for completed/repair/failed reconciliation runs, v0.099 UI refresh/repair actions logged into the same reconciliation run history, v0.100 failed manual refresh/repair attempts logged with failure reason/exception metadata, v0.101 summary cards plus `run_status` filtering for reconciliation run history, v0.102 filtered CSV export plus export audit logging for reconciliation run history, v0.103 scheduler health signals with stale-running detection and recommended action, v0.104 operator closure for stale running reconciliation jobs with required reason and metadata, v0.105 DepartmentActivityLog audit entries for stale run closures, and v0.106 visible recent reconciliation audit trail on the reconciliation control page.
- Latest v0.110 addition: audit date-window filtering for reconciliation audit trail UI and CSV export.
- Latest cross-app hardening pass: student course-content routes now require active subject enrollment; teacher assignment/material/announcement/exam/attendance writes now enforce taught-subject/student-roster scope; student fee status uses active `fee_demands` plus active penalties as the outstanding source of truth; admin fee dashboard/collection now prefers demand-ledger balances when demands exist, filters selected-student fee structures by course, and blocks direct wrong-course payment submissions.
- Latest CoE/Exam hardening pass: Exam Cell grade sheets, marks entry, hall-ticket lists, and hall-ticket downloads now use subject/term enrollment eligibility instead of program-only rosters, preventing marks or hall tickets for in-program students who are not enrolled in the exam subject.
- Latest student admit-card hardening pass: student admit-card list/download now uses active subject/term eligibility from `student_subject_enrollments` or compatible legacy enrollments, preventing same-program but unenrolled exams from appearing or being downloaded.
- Latest admin communication hardening pass: Admin Bulk Mail now builds query-backed audiences instead of materializing full recipient collections, counts only deliverable unique emails, filters "All Active Students" through active student profiles, filters program/batch sends through active matching students, and chunks queued sends for large recipient sets.
- Latest student subject-registration hardening pass: student self-registration now shows only active subjects for the student's current program/term, writes canonical `student_subject_enrollments`, synchronizes compatible legacy `enrollments` when a matching semester exists, uses `credits` instead of obsolete `credit_hours`, and drops subjects by marking both canonical and legacy rows dropped.
- Latest student timetable hardening pass: student timetable now reads canonical `student_subject_enrollments` plus compatible legacy enrollments, filters active timetable rows by enrolled subject plus student program/batch/current term scope, and groups numeric `day_of_week` values by model day name so valid Monday-Saturday rows render correctly.
- Latest student results/transcript hardening pass: GradeService, student results, and transcript generation now discover semesters and subjects from canonical `student_subject_enrollments` plus compatible legacy enrollments, so PMC student course-basket allocations without legacy rows still produce result, SGPA/CGPA, and transcript reports.
- Latest student/teacher attendance hardening pass: teacher attendance rosters and attendance submission authorization now include canonical active `student_subject_enrollments` matched through timetable `term_id` or compatible semester-to-term mapping, while student attendance summaries and session drilldowns now resolve PMC term-backed timetable rows and block subject drilldowns for unenrolled students.
- Latest student exam-registration hardening pass: student exam registration lists, direct registration actions, and dashboard upcoming-exam cards now filter subject exams through active canonical `student_subject_enrollments` plus compatible legacy enrollments, with term-to-semester mapping for legacy exam records, so same-program but unenrolled subject exams are hidden and direct registration is forbidden.
- Latest academic transcript hardening pass: Academic transcript screen/PDF now use one shared report builder, include only exams for subjects the student is actually enrolled in through canonical `student_subject_enrollments` or compatible legacy enrollments, map PMC terms to legacy semester exam records, and use the current `subjects.credits` field instead of obsolete `credit_hours`.
- Latest admin academic roster hardening pass: admin exam result entry and admin attendance marking now build rosters from canonical active `student_subject_enrollments` plus compatible legacy enrollments, map timetable/exam semester records to PMC terms, reject submitted result/attendance rows for students outside the actual roster, and include term-backed timetable rows in admin attendance reports/exports/PDFs.
- Latest admin enrollment synchronization pass: admin manual and bulk subject-enrollment workflows now keep legacy `enrollments` and canonical `student_subject_enrollments` in sync, map selected semesters to student program/batch terms, use valid canonical enrollment types (`compulsory`/`elective`), reactivate existing rows, validate every bulk subject id, and mark matching canonical rows dropped when a legacy enrollment is removed.
- Latest parent portal academic visibility pass: parent dashboard SGPA/attendance, child attendance, and child results now use canonical enrolled subjects plus compatible legacy enrollments, resolve PMC term-backed timetable/result rows through semester-to-term mapping, and hide rogue same-term attendance/results for unenrolled subjects.
- Latest course feedback hardening pass: student course feedback now lists and accepts feedback only for active canonical `student_subject_enrollments` plus compatible legacy enrollments, fixes the submitted-state rendering bug, blocks direct feedback routes for unenrolled subjects, stores feedback against the resolved feedback term, and teacher feedback aggregation now counts only feedback from actually enrolled students while rendering response counts/comments correctly.
- Latest student course hub hardening pass: student course hub index/detail now uses canonical active `student_subject_enrollments` plus compatible legacy enrollments, removes the invalid `Subject::teachers` dependency that could crash the course list, shows faculty names from active timetable assignments, hides unenrolled subjects, and permits legacy active enrollments on detail pages.
- Latest student discussion-board hardening pass: discussion index/show/store/reply/resolve now share one active canonical-or-compatible-legacy enrollment check, dropped or unenrolled subjects are blocked, legacy active enrollments can use discussions, new questions are term-stamped from the resolved enrollment, and thread owners must still have current subject access before marking discussions resolved.
- Latest student attendance-condonation hardening pass: condonation create/store now calculate eligible low-attendance subjects only from active canonical `student_subject_enrollments` plus compatible legacy enrollments, reject direct requests for unenrolled or >=75% attendance subjects, persist the resolved term, and prevent duplicate open requests for the same subject/term.
- Latest student marks-appeal hardening pass: marks appeal create/store now expose and accept only non-absent results for subjects the student is actively enrolled in through canonical `student_subject_enrollments` or compatible legacy enrollments, blocking direct appeals for rogue unenrolled-subject results or absent results.
- Latest student leave hardening pass: student leave creation now works against the shared teacher/student leave table by making teacher linkage nullable and adding attachment support, stamps student-originated records with `leave_type=student`, computes leave days, and blocks overlapping pending/approved leave ranges while allowing rejected history.
- Latest student placement eligibility hardening pass: student placement listing/apply now enforces drive minimum CGPA using result-derived student CGPA, shows ineligible-drive guidance in the UI, and blocks direct low-CGPA applications while preserving status/deadline/duplicate checks.
- Latest assignment workflow hardening pass: student assignment listing/show/submit now use canonical active `student_subject_enrollments` plus compatible legacy enrollments, teacher submission rosters use the same real enrolled-student source, the submissions view posts grading to the correct route/payload, and grading blocks rogue non-roster submissions plus marks above assignment maximum.
- Latest student quiz hardening pass: quiz listing/show/start/submit/result now use canonical active `student_subject_enrollments` plus compatible legacy enrollments, legacy-enrolled students can complete valid quizzes, and submitted option IDs are accepted only when the option belongs to the current question.
- Latest student materials/announcements hardening pass: direct subject material and announcement pages now accept canonical active `student_subject_enrollments` plus compatible legacy enrollments, keep unpublished materials hidden, and continue blocking students outside the actual subject enrollment.
- Latest student scholarship eligibility pass: scholarship schemes now support structured minimum CGPA, maximum family income, and proof-document requirements; student scholarship lists show eligibility context and block direct ineligible applications; required proof uploads are stored and available to admins through a protected proof-download route.
- Latest admission applicant scholarship hardening pass: applicant scholarship awards now enforce active schemes, program compatibility, structured CGPA/family-income eligibility, verified-document requirements for proof-based schemes, duplicate prevention, and max amount/seat limits from direct staff routes.
- Latest test/data stability pass: `ClassroomFactory` now generates unique `room_number` values to match the database unique constraint and prevent random full-suite failures.
- Latest fee-demand scholarship handoff pass: academic fee-demand generation now carries awarded/disbursed applicant scholarships through completed enrollment confirmations into student fee demands, caps deductions at total fees, and manual demand create/update now reject deductions above total amount to prevent negative final balances.
- Latest library circulation hardening pass: library reservations are now operational for students and staff, student catalog pages show reservation actions/status from the database, staff get a reservation queue with fulfil/cancel actions, direct issue respects pending reservation priority, damaged/lost copies are blocked, and issue due dates cannot exceed membership rules.
- Latest student NOC/library-clearance pass: library NOC eligibility now groups student/teacher borrower checks correctly, unpaid returned fines and unreturned books are scoped to the requested user, and admin NOC document approval/fulfillment is blocked until library clearance is complete.
- Latest admission gateway webhook hardening pass: gateway webhook payment resolution is now provider-scoped, conflicting order/payment identifiers no longer mutate either admission payment, and payment-id-only webhooks cannot verify a payment owned by another configured provider.
- Latest student fee-payment proof hardening pass: student payment proof submissions now use active fee demands as the source of truth, block amounts above the selected demand or total open balance, and prevent duplicate pending proof submissions for the same demand.
- Latest fee-payment proof verification workflow pass: Admin/Accounts now have a payment-proof review queue with filters, proof download, verify, and reject actions; verification creates a fee receipt/payment, updates linked demand balances to partially/fully paid, maps NEFT/RTGS/UPI proof methods safely into stored fee payments, and rejection records staff notes.
- Latest NOC fee-clearance hardening pass: student NOC document approval and fulfillment now require both library clearance and academic fee-demand clearance, blocking active pending/partial/overdue fee balances while allowing fully paid historical demands.
- Latest NOC hostel-clearance hardening pass: student NOC document approval and fulfillment now also require hostel fee clearance, blocking pending hostel fee balances while allowing paid or waived hostel demands.
- Latest student career-event hardening pass: student career-event registration now closes for unpublished, past-date, deadline-past, or full-capacity events while allowing registration on the deadline date; direct POST behavior and listing visibility are regression-covered.
- Latest CMC career-event operations pass: students can cancel open unattended event registrations, attended/closed registrations are protected, and CMC staff can mark attendance from the event registration list with wrong-event URL protection.
- Latest CMC career-event type hardening pass: CMC event create/edit/filter pages now use the database-safe canonical event types (`seminar`, `mock_interview`, `workshop`, `company_visit`, `career_fair`, `other`), invalid legacy hyphenated values are rejected before persistence, and staff-side create/filter behavior is regression-covered.
- Latest CMC career-event deletion hardening pass: CMC can delete empty events, but deletion is blocked once students have registered so registration and attendance history is preserved; the event list disables delete for registered events.
- Latest CMC career-event schedule/capacity hardening pass: new events cannot be created in the past, registration deadlines must be on or before the event date, and event capacity cannot be reduced below the current registration count.
- Latest transport assignment hardening pass: admin transport assignment now rejects inactive stops, assignment dropdowns only offer active routes/stops/vehicles, and assignment end dates cannot be earlier than the assignment start date.
- Latest transport fleet governance pass: admin transport now has a vehicle fleet update workflow; capacity cannot be reduced below active assignments, assigned vehicles cannot be moved to maintenance/inactive, and fleet rows show active assignment counts.
- Latest asset/inventory hardening pass: asset returns now reject return dates before the assignment date; inactive consumable inventory items cannot receive or issue stock through direct routes; the asset register UI shows inactive stock clearly and hides movement actions for inactive items.
- Latest hostel room lifecycle hardening pass: hostel room updates now reject capacity reductions below active occupants and block moving occupied rooms to maintenance/reserved; new allocations and transfers now require an active block plus an available or partially occupied room, closing reserved/inactive-block bypasses from direct routes.
- Latest hostel outpass lifecycle hardening pass: pending outpasses with past out times can no longer be approved; approved future outpasses cannot be marked returned before the approved out time; the hostel outpass list now shows expired/not-out-yet states instead of unsafe action buttons.
- Latest hostel complaint closure hardening pass: resolving or closing hostel complaints now requires resolution notes, reopened/in-progress complaints clear `resolved_at`, and the admin complaint modal labels resolution notes as required for resolved/closed states.
- Latest grievance lifecycle hardening pass: admin grievance resolve/close now requires resolution notes, reopened grievances clear resolution metadata, and students can close only staff-resolved grievances instead of silently closing open/under-review/escalated issues.
- Latest departmental grievance hardening pass: HOD/department grievance resolve/escalate direct routes now enforce lifecycle guards, allowing resolution only for open/under-review/escalated records and escalation only for open/under-review records, preserving resolved/closed history.
- Latest Program Chair grievance hardening pass: Program Chair grievance updates now enforce assigned program scope, require resolution notes before resolving, stamp `resolved_by`, and block resolved/closed grievance history from being changed through direct Program Chair routes; the Program Chair grievance table marks locked history rows as non-editable.
- Latest leave workflow hardening pass: teacher leave creation now blocks overlapping pending/approved ranges; Admin, HOD, and Program Chair leave review routes now allow decisions only on pending applications; Admin cannot delete reviewed leave history; Program Chair leave actions enforce assigned-program scope; and an additive migration guarantees `leave_applications.reviewed_at` exists for HOD/PMC review timestamps.
- Latest attendance condonation hardening pass: student condonation requests now persist the requested session deficit, Program Chair condonation approve/reject routes enforce assigned-program scope and pending-only decisions, approvals cannot condone more sessions than requested, reviewed decisions stamp `reviewed_at`, and the PMC condonation list shows actionable requested-session values from the database.
- Latest elective override hardening pass: Program Chair elective override direct routes now enforce assigned-program scope, active elective-enrollment status, same program/term active elective replacements, duplicate active-enrollment prevention, and audit metadata (`previous_subject_id`, `override_reason`, `overridden_by`, `overridden_at`) on canonical student subject enrollments.
- Latest term promotion hardening pass: term promotion approvals/rejections now operate only on pending or on-hold records, approval requires the student still being in the promotion source term, reviewed promotion history cannot be edited/reapproved/rejected, edit screens no longer bypass transition logic by directly setting approved/rejected, and bulk approval processes only reviewable eligible current-term records.
- Latest student subject-registration hardening pass: student self-service registration no longer reactivates completed subject history, and self-service drops are limited to active elective enrollments in the student's current term; compulsory, completed, or old-term enrollments are protected from direct drop routes while valid drops still synchronize canonical and legacy enrollment rows.
- Latest admin enrollment hardening pass: admin manual and bulk subject enrollment now require semester-to-term mapping, active subjects matching each student's program/term, completed canonical history is not reactivated, bulk enrollment skips unsafe rows and reports when nothing eligible is created, and completed legacy enrollment history cannot be deleted from enrollment management.
- Latest teacher exam-result hardening pass: teacher result entry now validates nested result payloads, rejects marks above the exam total, requires marks unless the student is marked absent, keeps roster-scope enforcement, and clears stored marks when an absent result is submitted.
- Latest admin exam-result hardening pass: admin result entry now accepts the existing UI `marks` payload plus legacy `marks_obtained`, validates marks against exam total, requires marks for non-absent students, clears marks/grade when absent, preserves canonical roster enforcement, and stores grade/remarks consistently.
- Latest marks-appeal review hardening pass: Exam Cell appeal review now locks resolved/rejected history, requires remarks for final decisions, requires revised marks for approvals, blocks revised marks above exam total, stores revised marks only for approved appeals, and updates the linked exam result only through valid approvals.
- Latest exam anomaly hardening pass: Exam Cell anomaly resolution now requires resolution notes, persists those notes, stamps resolver metadata, blocks repeat resolution of already closed anomaly history, and the anomaly detail action form exposes required resolution-note input plus error feedback.
- Latest Exam Cell grade-sheet hardening pass: CoE/Exam Cell marks save now validates marks against exam total, requires marks unless the student is marked absent, keeps enrolled-roster enforcement, and stores absent results with null marks instead of zero so reports distinguish true zero from absence.
- Latest Exam Cell result-publication hardening pass: CoE/Exam Cell result publication now records `published_at`/`published_by`, blocks publication until every eligible roster student has marks or an absence record, blocks unresolved anomaly cases and pending marks appeals, prevents repeat publication, and preserves existing result remarks instead of overwriting moderation notes.
- Latest post-publication result-lock pass: Teacher, Admin, and Exam Cell result-entry save routes now block direct marks/absence/grade/remark edits after `exams.published_at` is set, and their result-entry pages show a locked published-results state so official results can only change through controlled appeal/correction workflows.
- Latest published-exam schedule-lock pass: Admin and Exam Cell exam schedule update/delete routes now block changes after result publication, and exam list/detail/edit pages replace unsafe edit/delete controls with a locked published-exam state so official exam/result history cannot be invalidated by direct schedule mutations.
- Latest official-result appeal gate pass: student marks appeals now expose and accept only published official exam results; draft/unpublished marks remain hidden from appeal creation and direct appeal POSTs are rejected until the Exam Cell publishes the result.
- Latest official-result visibility pass: student results, student dashboard SGPA/CGPA, student academic summary, parent dashboard/results, GradeService reports, academic transcripts, and scholarship CGPA fixtures now treat only exams with `published_at` as student/parent/officially visible results; draft marks remain operational staff data until publication.
- Latest student result API visibility pass: `/api/v1/student/results` now returns only published official exam results, closing the mobile/API path that could expose draft marks even after the web result pages and PDF/report builders were hardened.
- Latest result-publication notification pass: successful Exam Cell result publication now queues idempotent per-student `result_published` notifications with links back to the official results page, so students are actively informed when previously hidden draft marks become official.
- Latest Stage 2 workflow-integrity pass: fee demands are now protected from unsafe financial state changes. Fully paid demands are locked from ordinary edits, non-zero demands cannot be manually marked fully paid, paid/linked demand history cannot be deleted, and only untouched pending demands remain deletable. Admin student/teacher delete actions now archive records by marking profiles inactive, removing active roles, and writing activity logs so academic, fee, attendance, timetable, and result history is preserved.
- Latest Stage 2 academic master-data integrity pass: admin program, batch, term, subject, and department write routes now protect dependent academic history. Programs with students/batches/subjects/exams/timetable/PMC records cannot be deleted or structurally changed; batches cannot be cancelled or reduced below current strength and cannot be deleted with operational dependencies; terms with enrollment/exam/timetable/fee/PMC history cannot be deleted; subjects with enrollment/exam/timetable/course-content/PMC history cannot be deleted or structurally changed; departments with programs/courses/subjects/staff/students/hierarchy records cannot be deleted. Empty setup records remain deletable.
- Latest Stage 2 CMC placement lifecycle integrity pass: Admin and CMC placement drive routes now share lifecycle guards. Drives with applications cannot be deleted, drives cannot be completed while applied/shortlisted/interview applications remain open, selected-offer drives cannot be cancelled, vacancies cannot be reduced below selected offers, direct admin student-add respects drive status/deadline/duplicate/active-student rules, final selected/rejected/withdrawn placement decisions are locked from ordinary status reversal, and selected counts cannot exceed vacancies.
- Latest Stage 2 legacy PMC timetable route hardening pass: legacy Program Chair timetable write routes no longer grant all-program write access to unassigned chairs, scoped chairs are blocked from out-of-program timetable saves, imports, copies, auto-schedule acceptance, and publishes, nested auto-schedule suggestions are checked for batch/subject program ownership, and published timetable versions are locked from legacy save/publish/import/copy/auto-schedule mutation routes so changes must move through the newer PMC revision/version workflows.
- Latest Stage 2 approval final-state hardening pass: legacy HOD and Program Chair approval action routes now verify the approval queue role and pending status before approving or rejecting. Finalized approval records cannot be reapproved/rejected through these direct role-specific routes, and HOD/Program Chair users cannot action another role queue even when the applicant is otherwise in scope.
- Latest Stage 2 academic governance integrity pass: Academic calendar, curriculum-change, and OBE write routes now use explicit academic policy checks instead of relying only on the broad `academic/*` route role list. Accounts users remain able to access their academic fee surfaces but cannot directly mutate academic planning, curriculum, or OBE records. Program Chair/HOD users can still submit curriculum and manage OBE planning data, Dean/Admin-level users review final curriculum decisions, finalized curriculum changes cannot be reapproved/rejected, and OBE outcomes with mapping/attainment/survey history cannot be deleted through direct routes.
- Latest Stage 2 legacy PMC timetable save compatibility pass: scoped legacy Program Chair `chair.timetable.save-slot` now creates operational timetable entries with required legacy `semester_id` and `course_id` bridge values derived from the selected term/program, so guarded legacy save paths no longer fail against the older non-null timetable schema while still preserving program scope and published-version locks.
- Latest Stage 2 academic scholarship lifecycle pass: legacy academic scholarship write routes now require explicit scholarship-management authority instead of the broad academic role set, blocking Program Chair/HOD/Exam-style direct financial-aid mutations. Scholarship create/update now requires a positive percentage or fixed discount, fixed-amount-only scholarships persist safely with `percentage=0`, overlapping active duplicate awards for the same student/name/type are rejected, and delete now archives by setting `status=inactive` instead of destroying scholarship history.
- Latest Stage 2 term-promotion authority pass: academic term-promotion write routes now require explicit promotion-management authority instead of the broad academic role set, blocking Accounts/Exam-style direct promotion mutations. Promotion approval and bulk approval now also require the destination term to be a later term in the same program and batch, preventing stale or malformed promotion records from moving students into unrelated terms.
- Latest Stage 2 manual fee-payment ledger pass: admin manual fee collection now applies paid receipts to active fee demands oldest-first, blocks manual overpayment above the student's active open demand balance, and keeps manual receipt behavior aligned with verified payment-proof demand reduction.
- Latest Stage 2 hostel fee-waiver audit pass: hostel fee waivers now require a staff-entered reason, record waived timestamp and actor, and paid hostel demands record the staff actor while clearing any stale waiver metadata. Waived/paid hostel fee history remains final-state protected and continues feeding hostel/NOC fee-clearance checks.
- Latest Stage 2 student scholarship approval integrity pass: admin scholarship approval now revalidates the application at decision time, blocking inactive schemes, wrong-program schemes, missing required proof files, failed CGPA thresholds, and missing/excess family-income eligibility before any award amount is approved. This closes stale/direct application records that previously could bypass student-side eligibility checks.
- Latest Stage 2 admission refund lifecycle pass: admission refund creation now enforces one active refund per applicant, requires linked payments to belong to the applicant and be verified, and blocks requested/approved amounts above remaining verified refundable balance. Refund approve/reject/process routes now enforce pending/approved lifecycle transitions, require positive approved amounts, and prevent processed refund history from being silently reversed or reprocessed.
- Latest Stage 2 admission payment verification finality pass: admission payment verify/reject routes now operate only on pending payments, preventing verified payments from being rejected/reverified and rejected payments from being revived through direct staff routes. Rejection notifications now use the actual staff verification note, keeping applicant-facing guidance aligned with the stored audit note.
- Latest Stage 2 applicant scholarship disbursement integrity pass: applicant scholarship awards now require positive amounts, disbursement references must be unique, disbursement rechecks applicant/scheme compatibility and current scheme maximum before funds leave the queue, and disbursement notes are appended without destroying original award notes.
- Latest Stage 2 admission document verification finality pass: admission document verify/reject routes now operate only on pending applicant documents, preventing verified documents from being rejected/reverified and rejected documents from being revived through direct queue routes. The applicant-detail document action method also has the same pending-only guard if it is routed later.
- Latest Stage 2 admission offer finality pass: applicant-facing and staff-facing offer accept/decline routes now block stale issued offers when the applicant is already in a final admission state (`rejected`, `withdrawn`, or `enrolled`), preventing old offer links or direct staff routes from changing finalized applicant outcomes.
- Latest Stage 2 admission merit decision lock pass: merit list regeneration is blocked once matching applicants have active issued/accepted offer letters, direct merit decision changes are blocked for entries linked to active offers, and final non-pending merit outcomes cannot be changed to another outcome through ordinary decision routes. Pending entries remain decidable through the existing workflow.
- Latest Stage 2 admission enrollment finality pass: enrollment now locks the applicant row during confirmation, generates deterministic applicant-based enrollment numbers instead of `max(id)+1`, rejects duplicate roll numbers within the same batch before creating student/handoff side effects, and regression-covers repeat enrollment so completed confirmations cannot create duplicate students or handoff records.
- Latest Stage 2 student scholarship disbursement integrity pass: student scholarship disbursement now stores a structured unique disbursement reference, revalidates scheme activity, program eligibility, proof/CGPA/income requirements, approved amount, scheme maximum, and remaining seat capacity at disbursement time, and blocks stale or duplicate-reference disbursements before finalizing financial aid.
- Latest Stage 2 shared approval-chain integrity pass: generic approval chain history now has schema-backed workflow metadata (`workflow_type`, `step_order`, `sla_hours`, `parent_approval_id`), assigned-role/admin/recorded-approver access control on the chain view, and regression coverage proving direct unrelated users cannot inspect approval history while valid shared-chain approvals advance exactly once to the next approver.
- Latest Stage 2 student document-request integrity pass: inactive student profiles can still view their document request history, but new official document request creation and direct POST submission are now blocked unless the student status is active. This prevents archived/inactive profiles from generating new bonafide/NOC/ID/letter workflows through student self-service.
- Latest Stage 2 academic-year master-data integrity pass: academic years with linked semesters, batches, or fee structures can no longer be deleted or have start/end years or dates rebounded through admin routes, preventing cascade loss of semester/fee setup and invalid academic boundaries. Safe label/current-year updates remain allowed.
- Latest Stage 2 semester master-data integrity pass: semesters with enrollments, exams, or timetable entries can no longer be deleted or have academic year, number, or dates rebounded through admin routes, preventing cascade loss of roster/exam/timetable history. Safe name/current-semester updates remain allowed.
- Latest Stage 2 legacy course master-data integrity pass: legacy course records with students, fee structures, admissions, exams, or timetable entries can no longer be deleted or structurally changed through admin routes, preventing cascade loss of student/fee/timetable history. Courses with active students cannot be deactivated, while safe label/description updates remain allowed.
- Latest Stage 2 classroom master-data integrity pass: classrooms with timetable or exam history can no longer be deleted, structurally changed, deactivated while active schedules/upcoming exams depend on them, or reduced below active timetable batch capacity. Safe label/capacity increases remain available while preserving room allocation history.
- Latest Stage 2 timetable-slot master-data integrity pass: timetable slots used by timetable entries, teacher availability, PMC locked slots, generated timetable items, or session delivery logs can no longer be deleted, have their start/end time or break classification changed, or be deactivated through admin routes. Safe name/sort-order updates remain allowed so labels can be cleaned up without invalidating schedules.
- Latest Stage 2 fee-structure receipt-history integrity pass: fee structures linked to fee receipt/payment history can no longer be deleted or financially restructured through admin routes, preventing the legacy `fee_payments.fee_structure_id` cascade from destroying receipt history or changing historical receipt meaning. Unused fee setup records remain editable and deletable.
- Latest Stage 2 admission configuration lifecycle pass: required document requirements linked to applicant uploads, selection steps linked to sessions/scores, and admission fee installments linked to admission payments are now protected from destructive deletes and contract/financial restructuring through both admin configuration and admission staff routes. Safe description/instruction-style updates remain available where they do not reinterpret applicant history.
- Latest Stage 2 admission seat-matrix integrity pass: admission seat matrices with selection, waitlist, offer, or completed enrollment history can no longer be deleted, and category/total capacity cannot be reduced below already selected/offered/enrolled applicant commitments. Capacity increases remain allowed so institutes can expand intake without invalidating historical decisions.
- Latest Stage 2 admission waitlist-promotion integrity pass: manual waitlist promotion now requires a configured seat matrix and checks both total capacity and applicant-category capacity before changing a waitlisted merit entry to selected. This prevents direct promotion from bypassing published seat controls when no matrix exists or when a category is already full.
- Latest Stage 2 admission offer auto-promotion integrity pass: applicant-facing and staff-facing offer decline auto-promotion now uses shared seat-capacity checks. Promotion requires a configured seat matrix, excludes the declined applicant's released seat from current commitments, checks total and applicant-category capacity across selected merit entries, active offers, and completed enrollments, and skips final-state or already-offered waitlisted applicants.
- Latest Stage 2 student exam-registration finality pass: student exam registration now hides and blocks result-published exams, and reviewed exam-registration records (`approved` or `rejected`) cannot be reset to `pending` through repeated student self-service POSTs. Pending/idempotent registration remains available for eligible future exams.
- Latest Stage 2 assignment submission finality pass: student assignment self-service can no longer overwrite a graded submission or reset it back to `submitted`, preserving marks, feedback, grader, and graded timestamp. Non-graded resubmissions remain allowed before deadline rules, and resubmissions without a new file preserve the previous uploaded file path.
- Latest Stage 2 quiz attempt finality/visibility pass: quiz submission now rechecks that the quiz is still active and published before scoring, preventing already-started attempts from being submitted after closure or unpublish. Student result viewing now respects `show_result_immediately` while the quiz is active and releases results once the quiz window has closed.
- Latest Stage 2 teacher content archival pass: teacher material and subject announcement delete routes now soft-delete records instead of destroying them. Published teaching content history and material file references remain available via archived records, while normal teacher/student lists continue to hide archived content.
- Latest Stage 2 admin timetable entry history pass: admin timetable entries with attendance, substitution, published status, or version history can no longer be structurally changed through direct update routes. Delete requests for history-bearing entries now archive/deactivate the entry and preserve attendance/history rows; empty draft entries remain physically deletable.
- Latest Stage 2 parent profile archival pass: admin parent delete now soft-deletes the parent profile, preserves the user account and `parent_student` linkage history, removes the active parent role, and writes an archive activity log instead of cascading through parent/student portal history.
- Latest Stage 2 library fine finality pass: library fine payment now requires a returned/lost issue with a positive unpaid fine, blocks direct payment of active issued/overdue fines before final return calculation, prevents duplicate fine payments, and return processing resets positive recalculated fines to unpaid so NOC/clearance cannot be bypassed by stale pre-return fine state.
- Latest Stage 2 CMC company lifecycle pass: admin company deletion now archives recruiter records with placement-drive or internship history by deactivating the company and writing an activity log, preserving placement drives, selected offers, and internship rows that would otherwise be cascade-deleted. Empty setup company records remain deletable.
- Latest Stage 2 profile self-delete hardening pass: generic `/profile` account deletion now blocks institutional student, teacher, parent, and applicant accounts from self-deleting users that own academic/admission/portal history. Plain non-operational users can still delete their account, while institutional accounts must go through the relevant admin/archive workflow.
- Latest Stage 2 user-role integrity pass: admin user-role assignment now grants the actual Spatie authorization role in addition to writing the `user_roles` audit row, reactivates expired assignments safely, revokes actual roles when the final active assignment is removed or expired, expires indefinite roles correctly, and blocks current/final admin role removal from the user-role management routes.
- Latest Stage 2 notification archival pass: user notification delete now soft-deletes records instead of destroying operational notification history, while normal notification lists/counts continue to hide archived records. Ownership policy coverage also confirms direct routes cannot mark-read or archive another user's notifications.
- Latest Stage 2 notice visibility pass: student, parent, and API notice reads now share a database-backed `Notice::visibleTo()` scope that enforces published status, publish/expiry dates, and audience-role visibility. Direct student/API notice detail routes now return 404 for future, expired, or non-visible audience notices instead of exposing internal/future communications.
- Latest Stage 2 notice archival pass: admin notice delete now soft-deletes institute notices instead of destroying official communication history. Archived notices remain hidden from admin/student/parent/API normal reads through default model scoping while preserving the original notice row for audit/history queries.
- Latest Stage 2 applicant document replacement hardening pass: applicant self-service uploads now block replacement of already verified admission documents before validation/storage, preserving the verified file path, version, verifier, and timestamp. Rejected or pending document replacement remains available through the existing applicant correction flow.
- Latest Stage 2 applicant admission-payment proof hardening pass: applicant self-service admission fee proof submission now validates that the installment is active and belongs to the applicant's program/batch, blocks duplicate pending or verified proofs for the same applicant/installment, and still allows rejected proofs to be corrected through a new pending submission.
- Latest Stage 2 applicant admission gateway-payment hardening pass: applicant gateway initiation now requires the payment record to still be pending, and gateway webhooks no longer mutate verified or rejected admission payment records. Final staff decisions remain authoritative even if delayed gateway events arrive later. Parent result visibility regression coverage was also stabilized to assert against the rendered draft mark value instead of random CSRF-token text.
- Latest Stage 2 student document/NOC lifecycle hardening pass: official document requests now require explicit approval before staff can upload and mark a document ready. Pending requests can no longer be fulfilled directly through the admin route, and the admin queue hides the ready-upload control until the request is in approved/processing state, preserving review/audit order for NOC and other official documents.
- Latest Stage 2 transport assignment eligibility hardening pass: admin transport assignment creation now revalidates the selected student status at POST time, blocking inactive/archived student profiles from receiving new active transport assignments through direct routes even though the UI already lists only active students.
- Latest Stage 2 transport assignment end-date hardening pass: active transport assignments can no longer be ended with a future date through direct routes. Immediate ending now requires an end date on or before today, preventing future service from being marked inactive early and freeing student/vehicle eligibility before service actually ends.
- Latest Stage 2 asset assignment return-date hardening pass: active asset assignments can no longer be returned with a future date through direct routes. Returning an issued asset now requires a returned date on or before today, preventing assets from being marked available/maintenance before they are actually returned.
- Latest Stage 2 inventory movement date hardening pass: consumable inventory receive and issue routes now reject future movement dates before changing stock or writing movement history. Current stock can no longer be inflated or reduced today by a future-dated receipt/issue record submitted through direct routes.
- Latest Stage 2 library lost-issue return hardening pass: admin library return routes now allow returns only for active `issued` or `overdue` records. Lost or otherwise finalized issue records can no longer be returned through a direct route that would make a lost copy available again or inflate book availability counts.
- Latest Stage 2 library borrower eligibility hardening pass: admin library issue routes now revalidate borrower status at POST time, blocking inactive/archived student and teacher profiles from receiving new book issues through direct routes even though the issue UI already lists only active borrowers.
- Latest Stage 2 library reservation eligibility hardening pass: student library reservation creation now revalidates active student status at POST time, blocking inactive/archived student profiles from creating new reservation queues through direct routes while preserving read-only access to their existing library page/history.
- Latest Stage 2 library membership limit integrity pass: admin membership create/update now checks the borrower's active issued/overdue books before saving. A membership limit can no longer be lowered below the current active issue count through direct routes, preventing inconsistent circulation limits while books are already out.
- Latest Stage 2 student fee-payment proof eligibility hardening pass: inactive/archived students can still view fee payment submission history, but the create/store routes now block new payment proof submissions and the student payment page hides the submit action with database-backed guidance.
- Latest Stage 2 student fee-payment verification hardening pass: admin/accounts proof verification now blocks pending proofs linked to already closed fee demands and blocks unlinked proofs whose amount exceeds the student's current open fee balance, preventing stale student proofs from creating paid receipts that no active demand can absorb.
- Latest Stage 2 fee-demand ledger finality pass: academic fee demands can no longer be created as `fully_paid` with a non-zero open balance, and demands with linked payment activity can no longer have amount, scholarship deduction, final balance, or due-date ledger fields rewritten through direct update routes. Audited adjustment/refund/waiver workflows remain the expected path for financial corrections.
- Latest Stage 2 hostel fee generation eligibility pass: monthly hostel fee generation now skips stale active allocations whose student profile is inactive/archived, preventing new hostel receivables from being created for non-active students while still counting those records as skipped for operator feedback.
- Latest Stage 2 hostel allocation eligibility pass: hostel allocation UI now lists only active students, direct allocation POST blocks inactive/archived student profiles, and stale active allocations for inactive students cannot be transferred to keep hostel occupancy alive; staff must vacate or reactivate the student through the proper workflow.
- Latest Stage 2 student scholarship eligibility finality pass: student scholarship self-service now blocks inactive/archived students from submitting new applications, and admin approval/disbursement rechecks active student status so stale approved applications cannot create scholarship benefit history after the student leaves active enrollment.
- Latest Stage 2 scholarship scheme finality pass: admission scholarship scheme updates can no longer reduce available seats below existing applicant-awarded/student-approved usage or reduce maximum amount below existing awarded/approved/disbursed amounts, preserving scholarship commitments already made through admission and student scholarship workflows.
- Latest Stage 2 accounts export audit pass: accounts fee-collection, admission-payment, and outstanding-fee CSV exports now write `ActivityLog` export entries with actor, row count, surface, and visible filter summary before streaming the file, preserving traceability for sensitive finance data exports without changing CSV content.
- Latest Stage 2 admission enrollment finality pass: applicants with completed enrollment confirmations can no longer be moved to contradictory CRM statuses such as withdrawn through direct status routes, and bulk applicant status updates skip enrolled applicants. Completed enrollment changes must go through academic student lifecycle or a future audited cancellation workflow.
- Latest Stage 2 student placement result-visibility pass: the legacy `Student::calculateCGPA()` helper now counts only non-absent marks from published exams, keeping placement eligibility and other model-helper consumers aligned with official result publication boundaries. Draft/unpublished marks can no longer make a student eligible for a placement drive before CoE/Exam publication.
- Latest Stage 2 student placement eligibility pass: inactive/archived student profiles can still review placement application history, but the student placement page now disables new applications with database-backed guidance and direct placement application POSTs reject non-active students before creating `placements` rows.
- Latest Stage 2 student career-event eligibility pass: inactive/archived student profiles can still view published career events and existing registration history, but student career-event registration and cancellation routes now reject non-active students before mutating `career_event_registrations`; the event list hides register/cancel controls and shows active-student-only guidance.
- Latest Stage 2 CMC career-event attendance finality pass: once a career-event registration is marked attended, the normal CMC attendance queue now locks that attended state and direct update attempts cannot silently revert it to absent. The registration page shows an attended-lock badge instead of a reversible "Mark absent" action.
- Latest Stage 2 CMC internship eligibility pass: CMC internship creation now revalidates the selected student at POST time and rejects inactive/archived student profiles before creating `internships` rows. The create form already lists active students only; the direct route now matches that operational boundary.
- Latest Stage 2 CMC registered-event contract pass: career events with registration history can no longer have core contract fields (`event_type`, `event_date`, `registration_deadline`) rewritten through the update route. Safe descriptive edits and capacity increases remain available, while capacity still cannot drop below registered count.
- Latest Stage 2 CMC company lifecycle pass: CMC and Admin company update routes now preserve recruiter identity after placement-drive or internship history exists by blocking company-name rewrites, and companies with upcoming/ongoing placement drives cannot be deactivated through update routes. Safe contact/detail edits remain available.
- Latest Stage 2 student grievance eligibility pass: inactive/archived student profiles can still view existing grievance history, but the student grievance create and direct store routes now block new grievance submission before opening fresh operational cases.
- Latest Stage 2 student leave eligibility pass: inactive/archived student profiles can still view leave application history, but the student leave create and direct store routes now block new leave submissions; the leave index hides the apply action and shows active-student-only guidance.
- Latest Stage 2 teacher leave eligibility pass: inactive/archived teacher profiles can still view leave application history, but teacher leave create, direct store, and pending-leave cancel routes now block new mutations unless the teacher profile is active. The teacher leave index hides apply/cancel controls for inactive teachers, shows active-teacher-only guidance, and displays submitted leave reasons in history rows.
- Latest Stage 2 teacher course-content mutation pass: inactive/archived teacher profiles can still view their assigned-course material, assignment, announcement, and submission history, but direct material upload/archive, assignment create, announcement post/archive, and assignment grading routes now require an active teacher profile. Teacher UI hides those mutation controls and shows active-teacher-only/locked guidance while preserving history visibility.
- Latest Stage 2 teacher attendance/result mutation pass: inactive/archived teacher profiles can still open assigned attendance and exam result-entry pages for history/review, but direct attendance-save and exam-result-save routes now require an active teacher profile. Attendance and result-entry pages show locked guidance and disable mutation controls for inactive teachers, while existing published-result locks remain in place.
- Latest Stage 2 mentor identity/active-profile pass: mentoring now consistently stores and resolves mentor ownership through the mentor user's ID, matching the `students.mentor_id` and `mentor_meetings.mentor_id` schema. PMC mentor assignment still accepts teacher profile IDs from the UI but persists the teacher user ID, teacher mentor dashboards use that identity, inactive teacher profiles can view mentee history but cannot send mentor messages or schedule meetings, and mentor detail attendance summaries now derive subjects through timetable entries instead of a non-existent attendance subject column.
- Latest Stage 2 student mentor workflow pass: inactive/archived student profiles can still view mentor details, message history, and meeting history, but student mentor message and meeting-request POST routes now require an active student profile. The student mentor page resolves the mentor as a user record, hides message/meeting forms for inactive students, and shows locked guidance instead of exposing actions that direct routes reject.
- Latest Stage 2 parent dashboard notice visibility pass: parent dashboard recent-notice previews now use the same `Notice::visibleTo()` scope as the full parent notice list, enforcing publish/expiry dates, soft-deleted notice hiding, and audience rules. Parent dashboards now show only `all`/student-family notices and no longer leak teacher/admin/future/expired notices through the preview card.
- Latest Stage 2 student course-feedback finality pass: inactive/archived students can still view enrolled-subject feedback history, but feedback create/store routes now require an active student profile and the student feedback page hides submit controls with active-student-only guidance. Direct duplicate feedback POSTs no longer overwrite existing submitted feedback rows; one submitted response per student/subject/term is preserved.
- Latest Stage 2 student subject-registration eligibility pass: inactive/archived students can still view current subject registrations and available subjects, but subject add/drop routes now require an active student profile and the subject-registration page hides add/drop controls with locked guidance. This prevents inactive profiles from changing academic rosters, timetable memberships, and legacy enrollment rows through self-service direct routes.
- Latest Stage 2 attendance-condonation eligibility pass: inactive/archived students can still view attendance condonation history, but condonation create/store routes now require an active student profile and the condonation page hides the new-request action with locked guidance. This prevents archived profiles from creating new attendance-eligibility exceptions through direct self-service routes.
- Latest Stage 2 student hostel self-service eligibility pass: inactive/archived students can still view hostel outpass and complaint history, but outpass and hostel-complaint create routes now require an active student profile even if a stale active hostel allocation exists. Student hostel pages hide operational forms and show active-student-only guidance while preserving historical hostel records.
- Latest Stage 2 student resume-builder eligibility pass: inactive/archived students can still view saved resume history, but resume save routes now require an active student profile and the resume builder renders as a read-only locked form with active-student-only guidance. This prevents archived profiles from changing placement/career readiness records through direct POSTs.
- Latest Stage 2 student discussion participation eligibility pass: inactive/archived students can still read enrolled-subject discussion history, but discussion post, reply, and mark-resolved routes now require an active student profile. Discussion list/detail pages hide participation controls and show locked guidance while preserving read access.
- Latest Stage 2 applicant admission-operations finality pass: applicant assessment reschedule requests now close once the application reaches a final admission state (`selected`, `rejected`, `withdrawn`, or `enrolled`). Applicant operations pages hide the reschedule form and show closed-state guidance while consent preferences remain available.
- Latest Stage 2 applicant document self-service finality pass: applicant document upload/delete routes and the documents page now treat `enrolled` as a final locked state alongside selected/rejected/withdrawn. Enrolled applicants can still view document history, but direct upload/delete attempts are rejected and existing pending/verified document rows and files remain preserved.
- Latest Stage 2 applicant gateway-payment finality pass: stale pending admission payments can no longer create gateway orders once the applicant has moved out of the active fee-payment window. Enrolled/final-state applicants can still view pending payment history, but the online payment action is hidden and direct gateway initiation routes reject without mutating gateway metadata.
- Latest Stage 2 admission offer-generation finality pass: staff offer generation now skips applicants already in final admission states (`rejected`, `withdrawn`, or `enrolled`) across program merit bulk generation and direct merit-list generation. Stale pending offer detail pages remain readable, but applicant and staff action controls are hidden with locked-state guidance; applicant offer pages also use the applicant layout instead of the generic slot layout so they render without Blade slot errors.
- Latest Stage 2 admission refund reason-integrity pass: refund creation and approval now validate business reason against current applicant/payment state. Withdrawal refunds require withdrawn applicants, rejection refunds require rejected applicants, and excess-payment refunds are capped to actual verified overpayment above configured active admission fees; pending refunds are revalidated at approval so stale status changes cannot release money later.
- Latest Stage 2 admission waitlist/seat-control finality pass: legacy waitlist promotion now blocks final-state applicants and applicants with active offers, and selected-seat counts exclude rejected/withdrawn/enrolled applicants from ordinary pending commitments. The v0.038 waitlist service now refuses final-state waitlist additions and skips final-state rows during automatic seat-release promotion, so stale waitlist rows cannot create fresh seat holds.
- Latest Stage 2 admission selection-committee finality pass: final selection committee decisions now reject applicants already in final admission states (`rejected`, `withdrawn`, or `enrolled`) before writing decision rows, applicant status changes, or sensitive audit entries. This prevents direct committee POSTs from overwriting completed admissions or final outcomes.
- Latest Stage 2 admission merit-list finality pass: single merit decision routes now reject pending entries whose applicant is already rejected, withdrawn, or enrolled, and bulk merit decisions skip final-state applicants and applicants with active issued/accepted offers while reporting actual selected/waitlisted/skipped counts. Stale merit rows can no longer reselect finalized applicants through direct routes.
- Latest Stage 2 Dean result-visibility integrity pass: legacy Dean academic dashboards now aggregate and display only published exam results for program pass rates, recent results, top performers, and academic at-risk lists. Draft/unpublished marks remain available to Exam Cell result-entry workflows but no longer leak into Dean management performance views before official publication.
- Latest Stage 2 institutional/PMC reporting result-visibility pass: Admin analytics, Institutional KPI, AICTE reports, and PMC subject/term reports now calculate official pass rates, grade distribution, averages, and top-student lists only from published exam results. PMC subject performance also now uses the current `subjects.term_number` schema and renders the controller's computed average/grade distribution fields correctly.
- Latest Stage 2 academic risk/mentoring result-visibility pass: Program Chair dashboards, HOD dashboards/performance, PMC at-risk student lists, and mentor detail pages now use only published exam results for academic averages, arrear/academic risk flags, recent exam tables, and mentee result history. This pass also fixed current-term subject counting to use `subjects.term_number` and fixed PMC at-risk attendance grouping through `timetable_entries` instead of the nonexistent `attendances.subject_id`.
- Latest Stage 2 marks-appeal correction integrity pass: Exam Cell appeal review now revalidates the linked official result before writing revised marks. Stale appeal rows cannot mutate draft/unpublished results, absent results, missing results, or results belonging to a different student, while already reviewed appeals remain locked and valid published-result corrections still work.
- Latest Stage 2 academic transcript issuance integrity pass: official academic transcript PDF generation now stores a normalized `semester_data` snapshot on first issue and reuses that snapshot on later downloads instead of silently recalculating from changed result rows. The transcript PDF template now renders both live model reports and stored array snapshots, uses the original issue date, and avoids mojibake dash placeholders.
- Latest Stage 2 consolidated academic report route integrity pass: admin consolidated student report PDFs now call `GradeService` with the required student/semester IDs instead of passing model objects, preserving the published-result-only report contract and preventing direct route TypeErrors for official consolidated reports.
- Latest Stage 2 fee receipt finality pass: shared admin/student fee receipt PDF generation now rejects non-`paid` fee payment records through direct routes, so pending/partial/refunded rows cannot produce official receipts. The receipt PDF template now renders the real `amount_paid` value and uses ASCII-safe currency text to avoid blank or mojibake receipt amounts.
- Latest Stage 2 student scholarship commitment finality pass: approved or disbursed student scholarship applications can no longer be rejected through the generic reject route, preserving committed/disbursed amounts and references. Only pending or shortlisted applications can be rejected; approved/disbursed corrections require a future audited reversal workflow.
- Latest Stage 2 legacy academic scholarship discount-history pass: legacy `/academic/scholarships` records now preserve applied fee-discount history. If a student has fee demands with scholarship deductions, the related legacy scholarship's financial terms cannot be rewritten and the record cannot be archived through direct routes; unused legacy scholarship records remain editable/archivable.
- Latest Stage 2 student document request download finality pass: admin document-request downloads now require `ready` status in addition to an existing output file, matching the student download boundary. Stale output paths on pending, approved, or rejected document requests can no longer expose non-final documents through direct admin download routes.
- Latest Stage 2 admission refund processing revalidation pass: approved refunds now revalidate applicant reason/status, verified payment ownership, and remaining refundable balance again at processing time before writing UTR/processed state. Stale approved refunds can no longer release money after applicant/payment state changes invalidate the refund.
- Latest Stage 2 exam admit-card eligibility pass: student admit-card lists/downloads and Exam Cell hall-ticket lists/downloads now require an approved `exam_registrations` row with `attendance_eligible` and `fee_cleared` both true, in addition to subject/term enrollment eligibility. Pending, rejected, fee-blocked, attendance-blocked, or unregistered students can no longer receive official exam entry documents through list pages or direct download URLs.
- Latest Stage 2 student transcript self-service finality pass: student official transcript download now requires an already issued `academic_transcripts` snapshot instead of generating a fresh official-looking transcript from live grades. Student results and student sidebar navigation hide the transcript link until an issued snapshot exists, while direct download requests redirect with guidance.
- Latest Stage 2 grade-card official-document finality pass: shared student/admin grade-card PDF generation now requires a complete semester report with published results for every enrolled subject. Student Results hides the Grade Card PDF action and shows publication-pending guidance while any subject is still pending, and direct student/admin grade-card URLs return 404 until the semester is officially complete.
- Latest Stage 2 admission offer-letter PDF finality pass: applicant and staff offer-letter PDF downloads now block stale issued offers once the applicant is in a final admission state (`rejected`, `withdrawn`, or `enrolled`). The applicant PDF route is ordered before the parameterized show route so `/offer-letters/{id}/pdf` reaches the PDF controller instead of being swallowed by the detail page.
- Latest Stage 2 admission enrollment-letter finality pass: enrollment confirmation letter downloads now require a `completed` enrollment confirmation with a linked student and enrollment number. Processing, failed, or otherwise incomplete confirmation rows can no longer produce official enrollment proof letters through direct URLs.
- Latest Stage 2 admission assessment call-letter integrity pass: assessment call-letter PDF generation now requires the applicant to be explicitly assigned through `session_applicants` to an active scheduled/ongoing selection session. Unassigned applicants, final-state applicants, absent candidates, and completed/cancelled sessions can no longer receive call letters through direct URLs by merely sharing the same program.
- Latest Stage 2 admission application-PDF finality pass: staff application-form PDF generation now blocks draft applicants and only renders submitted-or-later application records, preventing direct URLs from producing official-looking application PDFs before the applicant has actually submitted the form.
- Latest Stage 2 admission fee-receipt scope pass: admission payment receipt PDF generation now applies Admission hierarchy visibility before streaming verified receipts. Out-of-scope admission members can no longer download another counsellor/team's payment receipt through a direct payment receipt URL.
- Latest Stage 2 legacy PMC timetable PDF export scope pass: legacy Program Chair batch timetable PDF exports now validate program/term/batch scope, and teacher timetable PDF exports are filtered to the actor's visible program assignments unless the actor has Dean/Admin-level academic oversight. Out-of-scope chairs can no longer export another program's batch timetable or a teacher timetable with no visible assigned-program entries.
- Latest Stage 2 admin fee-receipt view finality pass: the printable admin fee receipt page now matches the paid-only PDF receipt contract. Pending or otherwise non-paid `fee_payments` can no longer render official-looking receipt views through direct `admin.fees.receipt` URLs.
- Latest Stage 2 consolidated academic report finality pass: admin consolidated student report PDFs now include only complete semester reports whose enrolled subjects all have published results, and draft-only students return 404 instead of receiving an official-looking consolidated PDF from unpublished marks.
- Latest Stage 2 teacher assignment-submission roster visibility pass: teacher assignment submission lists now show only submissions from students currently in the assignment roster, matching the existing grading guard. Rogue or stale submission rows for non-roster students no longer appear on the teacher submissions page even before a grading attempt.
- Latest Stage 2 asset custody integrity pass: admin asset assignment now checks for existing active asset-assignment rows in addition to the asset status flag, preventing split-state records from creating multiple active custody assignments for the same physical asset through direct reassignment routes.
- Latest Stage 2 hostel allocation history pass: hostel bed reuse and room transfers now preserve prior occupancy rows instead of overwriting vacated/transferred allocation history. The old full room/bed uniqueness has been replaced with room/bed/status indexing plus existing active-bed application guards, so direct routes still block duplicate active occupants while hostel, fee, complaint, and NOC history remain auditable.
- Latest Stage 2 CMC internship integrity pass: internship creation now lists and accepts only active company partners, and direct CMC internship registration blocks overlapping ongoing internship/industrial-training/live-project records for the same student. This prevents archived recruiters and duplicate active external engagements from distorting CMC reports and student-facing internship history.
- Latest Stage 2 official document processing integrity pass: admin document-request approval and ready-upload routes now revalidate that the linked student profile is still active before issuing official student documents. Rejection remains available for stale inactive-student requests so staff can close history with notes, but archived profiles cannot receive fresh approved/ready documents through direct admin routes.
- Latest Stage 2 notice write-side integrity pass: admin notice creation now persists unchecked publish boxes as drafts instead of relying on the database default, and bulk student notice email dispatch only runs for currently visible `students`/`all` notices. Already-visible published notices now block title/content/audience/publish-date rewrites, preserving official communication history while still allowing expiry/unpublish-style administrative updates.
- Latest Stage 2 legacy scholarship active-student integrity pass: legacy academic scholarship creation now lists only active students and active fee-discount scholarship commitments can be created or reactivated only for active student profiles. Inactive/archived students can retain historical inactive scholarship rows, but direct routes can no longer create live fee discounts for archived profiles.
- Latest Stage 2 library reservation fulfillment integrity pass: admin reservation fulfillment now revalidates that the reservation borrower is still active and enforces first-in queue order for pending reservations on the same title. Stale reservations for inactive borrowers must be cancelled, and later queue entries cannot bypass earlier pending reservations through direct fulfilment routes.
- Latest Stage 2 exam schedule history integrity pass: admin and Exam Cell exam update/delete routes now treat draft result rows and exam registrations as operational history. Published exams remain fully locked, and pre-publication exams with result/registration history can no longer have core subject/program/term/type/marks-scale fields rewritten or be deleted through direct routes; safe scheduling/detail edits remain available.
- Latest Stage 2 shared approval-chain idempotency pass: shared approval-chain advancement now returns the existing next approval step when retried for the same parent step instead of creating duplicate pending approvals, protecting Dean/HOD/Program Chair approval queues from duplicate next-step history after retries or concurrent service calls.
- Latest Stage 2 placement-drive contract integrity pass: Admin and CMC placement-drive update routes now share a lock on core drive contract fields once any student application exists. Company, title, job role, package, CGPA criteria, drive date, deadline, eligibility, and vacancies can no longer be silently rewritten through ordinary edit routes after applications are present, while safe operational notes such as venue/location and description remain editable.
- Latest Stage 2 placement company activation pass: Admin and CMC placement-drive create/update routes now reject inactive or archived company partners through the shared placement lifecycle service, closing direct POST paths that could create new drives for archived recruiters or move an existing drive to an inactive company.
- Latest Stage 2 transport setup integrity pass: transport stop creation now rejects inactive routes through direct admin POSTs, keeping route/stop setup consistent with the existing assignment guards that already reject inactive routes, stops, vehicles, and inactive students.
- Latest Stage 2 transport stop-order integrity pass: transport stop creation now rejects duplicate sequence numbers within the same route, preventing ambiguous pickup/drop ordering through direct admin POSTs while preserving existing active-route and assignment guards.
- Latest Stage 2 library membership-limit integrity pass: library membership updates now calculate active issued/overdue books from the actual user-linked student/teacher profiles instead of trusting the submitted member type, preventing direct POSTs from lowering borrowing limits below active issue count by switching a student/teacher membership to another type.
- Latest Stage 2 asset custody-date integrity pass: asset assignment now rejects future assigned-on dates through direct admin POSTs, preventing assets from being removed from availability before physical custody begins while preserving existing stale-active-assignment and return-date guards.
- Latest Stage 2 notice email job-time safety pass: queued bulk notice email dispatch now revalidates the current notice record at job execution time, skipping archived, unpublished, future, expired, or non-student/internal notices so stale queued jobs cannot email students after notice state changes.
- Latest Stage 2 hostel occupancy-date integrity pass: hostel allocation and transfer POSTs now reject future allocated-from dates, preventing rooms/beds from being marked actively occupied before real occupancy starts while preserving allocation history, transfer history, inactive-student guards, and room-capacity protections.
- Latest Stage 2 CMC internship completion-date integrity pass: ongoing internships, industrial training, and live projects can no longer be marked completed with a future end date, preventing final career records from closing before the activity has actually ended while preserving completed-state locking and start-date validation.
- Latest Stage 2 term-promotion active-student integrity pass: term promotion generation, single approval, and bulk approval now exclude inactive/archived students, preventing stale promotion records from moving archived students to a later academic term while preserving existing criteria, source-term, target-term, and reviewed-history guards.
- Latest Stage 2 fee-demand creation integrity pass: manual fee-demand creation now requires an active student and rejects duplicate student/term demands, closing direct POST paths that could create live dues for archived profiles or duplicate ledger obligations for the same academic term.
- Latest Stage 2 fee-payment proof verification integrity pass: admin verification of student-submitted fee payment proofs now revalidates that the linked student profile is still active before creating a paid receipt or reducing demand balance. Inactive/archived-student proofs remain pending for accounts correction instead of mutating the finance ledger through the standard queue.
- Latest Stage 2 student API finance visibility pass: `/api/v1/student/fees` now returns only paid fee-payment rows in the student-facing payment history, keeping pending/internal receipt rows out of mobile/API responses while preserving paid totals and balance calculations.
- Latest Stage 2 parent fee-history visibility pass: parent fee pages now load only paid fee-payment rows for receipt history, preventing pending/internal payment records from appearing as parent-visible receipts while demand, paid-total, and balance guidance remain demand-led.
- Latest Stage 2 student fee-history visibility pass: the student web fee page now loads only paid fee-payment rows for receipt history, keeping pending/internal records in proof-submission queues instead of showing them as receipt history while preserving confirmed paid totals.
- Latest Stage 2 teacher leave cancellation history pass: teacher self-cancellation of pending leave applications now preserves the leave row by marking it rejected with reviewer, timestamp, and cancellation note instead of deleting the record, keeping leave history auditable while still removing the pending blocker.
- Latest Stage 2 leave review schema-alignment pass: HOD and Program Chair student-leave review routes now store review notes in the actual `admin_remarks` column instead of the fallback-only `review_remarks` field, preventing successful direct review routes from failing against the real migrated leave table while preserving reviewer and timestamp audit data.
- Latest Stage 2 admin leave deletion history pass: admin pending-leave delete now preserves the leave row by marking it rejected with admin actor, timestamp, and cancellation note instead of physically deleting the pending application. Reviewed leave history remains locked from delete/review reversal.
- Latest Stage 2 admin mixed-leave requester display pass: admin leave list/detail pages now eager-load and render both teacher and student leave requesters, showing requester type/name instead of treating student leave rows as unknown teacher records.
- Latest Stage 2 fee-demand cancellation history pass: academic fee-demand delete now soft-archives untouched pending demands with cancellation actor, timestamp, and reason instead of physically removing billed obligations. Paid, partial, overdue, closed, or linked fee demands remain blocked from deletion.
- Latest Stage 2 hostel fee payment active-student guard: hostel fee generation already skips inactive students, and the standard hostel fee mark-paid route now revalidates the linked student profile before creating paid hostel fee history. Pending hostel demands for inactive/archived students remain pending for documented waiver or correction instead of being marked paid through the ordinary queue.
- Latest Stage 2 academic calendar history lock: future academic calendar events are now soft-archived instead of physically deleted, while past calendar events are locked from edit/delete so completed official term milestones cannot be silently rewritten. Calendar queries continue to hide archived rows by default.
- Latest Stage 2 admission scoring-rubric history lock: scoring parameters can no longer be deleted once their selection step has sessions or scores, and Admission staff cannot rename, rescore, or reorder used scoring parameters. Description-only updates remain possible for unused-safe documentation changes while historical applicant score interpretation is preserved.
- Latest Stage 2 scholarship scheme contract lock: Admission scholarship schemes with active applicant awards or active student applications can no longer change eligibility contract fields, program scope, proof requirement, type, or active status through ordinary update/toggle routes. Safe descriptive/capacity/amount edits remain available within existing award/seat guards, while eligibility changes require a new scheme version or closed applications.
- Latest Stage 2 academic term master-data integrity pass: admin term updates now persist only validated safe fields, so direct PUT payloads can no longer rebind `program_id`, `batch_id`, `term_number`, or `sort_order`. Terms with academic history also lock their start/end date window while still allowing safe label/current-term updates.
- Latest Stage 2 scoped academic role revocation history pass: program-scoped academic role assignments now deactivate with `revoked_by`/`revoked_at` audit metadata instead of being physically deleted. Reassigning the same user/role/program reactivates the existing row and clears revocation metadata, preserving role-scope history while keeping Spatie role cleanup behavior intact.
- Latest Stage 2 generic user-role revocation history pass: single user-role revoke now expires the `user_roles` row via `active_until` instead of physically deleting it, matching the existing expire-all workflow. Actual Spatie authorization roles are still removed when no other active assignment exists, and final/current admin protection remains intact.
- Latest Stage 2 organization hierarchy reporting-line history pass: org hierarchy reporting lines now have active/revoked audit metadata. Removing a line deactivates it and immediately removes it from hierarchy visibility checks while preserving the row and audit log; re-adding the same parent/child line reactivates the existing record instead of creating duplicate or losing history.
- Latest Stage 2 department member hierarchy reactivation pass: department member deactivation now records `deactivated_by`/`deactivated_at`, and re-adding the same user/department/role reactivates the preserved membership row instead of hitting the unique key or creating duplicate history. Reactivation clears deactivation metadata while existing DepartmentActivityLog entries continue to record membership changes.
- Latest Stage 2 department role/team lifecycle history pass: department roles and teams now record deactivation actor/time. Saving an existing inactive role clears deactivation metadata, and creating a matching inactive team reactivates the preserved team row instead of duplicating structure; active hierarchy views remain driven by `is_active`.
- Latest Stage 2 academic scope assignment lifecycle pass: academic scope deactivation now records actor/time, and reassigning the same user/member/scope/context reactivates the preserved scope row and clears deactivation metadata. Scope access remains based on active/current date windows, while governance history remains auditable.
- Latest Stage 2 NOC download clearance revalidation pass: student document request clearance checks are centralized in `StudentDocumentRequestClearanceService`, and ready NOC downloads now revalidate active student, fee, hostel-fee, and library clearance before streaming through both admin and student direct URLs. A NOC that becomes stale after fulfillment no longer remains downloadable.
- Latest Stage 2 library fine collection audit pass: returned positive library fines now record `fine_paid_at` and `fine_collected_by` when collected through the standard admin route. Existing guards still block paying active issued/overdue fines, zero fines, and already-paid fines, while NOC/library clearance can now be audited back to the collector and collection time.
- Latest Stage 2 transport active-assignment date integrity pass: transport assignment creation now rejects future `start_date` values through direct admin POSTs, preventing future-dated records from immediately consuming active student/vehicle capacity or becoming student-visible active transport before service actually starts. Existing active-student, active-route/stop/vehicle, capacity, and end-date guards remain intact.
- Latest Stage 2 asset return condition integrity pass: returned assets marked `damaged` now move to `maintenance` instead of the available assignment pool, matching the existing `needs_repair` behavior. This prevents damaged physical assets from being immediately reassigned through ordinary custody routes while preserving returned assignment history.
- Latest Stage 2 grievance closed-history integrity pass: standard admin grievance updates now reject already-closed grievances, so student-accepted closed cases cannot be silently reopened, reassigned, or have resolution notes rewritten through direct admin PATCH requests. Resolved-but-not-closed grievances can still be reopened through the existing admin review flow, while Program Chair and departmental closed-history locks remain intact.
- Latest Stage 2 academic curriculum-governance creation integrity pass: curriculum-change submissions now require an active program and, when a subject is selected, an active subject belonging to that same program. Direct POSTs can no longer create Dean/Academics governance work for archived programs, inactive subjects, or mismatched program-subject combinations while existing pending/final approval guards remain intact.
- Latest Stage 2 Exam/CoE anomaly creation integrity pass: new exam anomalies can no longer be logged against already-published exams, and the selected student must belong to the selected exam's program. This preserves official result finality after publication and prevents unrelated student/exam anomaly records from blocking or distorting CoE anomaly queues.
- Latest Stage 2 hostel complaint closed-history integrity pass: standard admin hostel complaint updates now reject already-closed complaints, so accepted/closed hostel service cases cannot be silently reopened, reassigned, or have resolution notes rewritten through direct PUT requests. Resolved-but-not-closed complaints can still be reopened through the existing follow-up flow.
- Latest Stage 2 shared approval escalation idempotency pass: legacy `ApprovalWorkflowService::escalateOverdue()` now reuses an existing pending escalation row for the same approvable, role, status, and parent approval instead of creating duplicate escalated approval tasks. Direct role approval routes remain pending-only and the chain advancement idempotency guard remains intact.
- Latest Stage 2 student document duplicate-open integrity pass: student official document requests now treat `ready` documents as open alongside pending/approved requests, so students cannot submit duplicate bonafide/NOC/ID/letter requests while an issued document is still downloadable. Rejected requests remain correctable through a fresh request.
- Latest Stage 2 student scholarship shortlist eligibility pass: admin scholarship shortlisting now reuses the scholarship approval blocker, so stale pending applications cannot be shortlisted when the student is inactive, the scheme is inactive/wrong-program, required proof is missing, CGPA/income eligibility fails, or other approval prerequisites are no longer true. This keeps the shortlist queue from legitimizing records that approval/disbursement would later reject.
- Latest Stage 2 hostel outpass approval eligibility pass: admin outpass approval now revalidates that the linked student profile and hostel allocation are still active before approving a pending outpass. Stale pending outpasses for archived students or vacated/transferred allocations remain pending for rejection/correction instead of becoming student-visible approved movement permissions.
- Latest Stage 2 library reservation expiry-history pass: student and admin reservation cancellation routes now expire stale pending reservations before attempting cancellation. Direct cancel URLs can no longer rewrite already-expired reservation history as `cancelled`; expired rows remain expired and ordinary pending reservations remain cancellable.
- Latest Stage 2 marks appeal active-student eligibility pass: inactive/archived students can still view marks appeal history, but student marks appeal create/store routes now reject new appeals before opening Exam Cell correction queues. Appeal creation remains limited to published, non-absent, enrolled results for active students.
- Latest Stage 2 legacy admission lifecycle integrity pass: legacy admin admission/enquiry records become locked once admitted or converted to a student. Direct admin update/status/delete/convert routes can no longer rewrite converted admission identity, reverse admitted status, delete linked admission history, or create duplicate students through repeat conversion; ordinary non-final enquiry records remain editable/deletable through the legacy workflow.
- Latest Stage 2 enrollment history integrity pass: admin subject-enrollment delete now blocks rows with academic activity history, including exam results, registrations, attendance, feedback, assignment submissions, or quiz attempts. Untouched non-final enrollment rows can still be removed and synced to dropped canonical status, but activity-bearing enrollment history remains preserved for academics, reports, and student records.
- Latest Stage 2 legacy applicant intake integrity pass: legacy admin applicant creation now requires an active program and, when a batch is selected, an active/upcoming batch belonging to that same program. Direct admin POSTs can no longer create draft applicants for archived programs, cancelled batches, or mismatched program/batch intake combinations while valid legacy applicant creation remains supported.
- Latest Stage 2 attendance-status integrity pass: admin and teacher attendance submission routes now validate every submitted attendance value against the canonical status set (`present`, `absent`, `late`, `excused`). Direct POSTs can no longer persist arbitrary attendance states that would distort student, parent, Dean, PMC, and institutional attendance reports.
- Latest Stage 2 institutional profile update integrity pass: student and teacher self-service profile update routes now reject inactive/archived linked profiles before validation or mutation. Archived students and teachers can still view their portal history, but direct profile update requests no longer rewrite archived contact, guardian, designation, qualification, or specialization records; active profiles remain editable.
- Latest Stage 2 parent-student link integrity pass: admin parent creation now links only active student profiles, and parent update blocks adding new inactive/archived student links while preserving already-linked archived children for historical access. Parent create/edit pickers now avoid offering inactive students as new links, so fresh parent portal access cannot be created for archived profiles through direct admin POSTs.
- Latest Stage 2 admin student/teacher archive workflow integrity pass: ordinary admin edit routes can no longer deactivate student or teacher profiles because that bypasses role cleanup and archive audit logs, and already-inactive archived profiles are locked from ordinary edit/reactivation. Admins must use the existing archive action for deactivation and a future audited correction/reactivation workflow for archived-profile changes.
- Latest Stage 2 admin student/teacher active-master assignment integrity pass: admin student create/update now requires active departments and active courses, and admin teacher create/update now requires active departments. Direct POST/PUT routes can no longer create new operational profiles or move active profiles onto archived/inactive academic master data that the forms already hide.
- Latest Stage 2 legacy admission active-master assignment integrity pass: legacy admin admission create/update now requires active courses, and legacy admission-to-student conversion now requires an active department. Direct POST/PUT routes can no longer create or move legacy enquiries onto inactive courses or convert legacy admissions into archived departments while valid active legacy intake/conversion remains supported.
- Latest Stage 2 manual fee collection active-master integrity pass: admin fee-structure create/update now requires active courses, manual fee collection now requires an active student and a fee structure tied to an active course, and zero-value manual paid receipts are rejected. Direct finance POSTs can no longer create live fee setup or paid receipt history for archived students/courses while valid active manual collection remains supported.
- Latest Stage 2 library issuable-copy integrity pass: library issue selectors and direct book-level issue routes now exclude damaged/lost copies before choosing an available copy. Direct issue-by-title no longer fails or targets a non-issuable first copy when a clean copy exists, preserving circulation accuracy while keeping damaged/lost copies out of borrower-facing issue flow.
- Latest Stage 2 asset category active-master integrity pass: asset register creation and consumable stock-item creation now require active asset categories. Direct admin POSTs can no longer attach new operational assets or inventory stock to archived/inactive category masters while uncategorized and active-category records remain supported.
- Latest Stage 2 career-event attendance timing integrity pass: CMC attendance marking now requires the event date to have arrived. Direct attendance PATCH requests for future career events are blocked, preventing false participation history before the event happens while same-day and past-event attendance marking remains supported.
- Latest Stage 2 hostel room active-block integrity pass: hostel room creation and room update routes now reject inactive hostel blocks before validating operational room changes. Direct room setup routes can no longer add new capacity or alter room status/fees under archived hostel blocks, while existing allocation, transfer, fee, outpass, complaint, and room-capacity guards remain intact.
- Latest Stage 2 fee-demand partial-payment integrity pass: academic fee-demand create/update routes can no longer mark a demand as partially paid without linked payment activity, and inactive/archived students' zero-balance demands cannot be manually closed as paid through the standard demand workflow. Direct finance routes now preserve ledger truth by requiring actual payment/proof activity before partial-payment state and active student profiles before manual paid closure.
- Latest Stage 2 fee-payment proof duplicate-reference integrity pass: student fee proof submission and admin proof verification now reject transaction references already linked to a paid fee receipt. This prevents stale or repeated proof rows from creating duplicate paid receipts or reducing demand balances twice when a bank reference has already been verified.
- Latest Stage 2 applicant scholarship disbursement integrity pass: applicant scholarship disbursement now reuses the award eligibility contract at payment time, blocking stale awards when schemes are inactive, required proof is missing, structured eligibility no longer holds, or the applicant has been rejected/withdrawn. Direct disbursement routes can no longer pay stale admission scholarship rows that would fail a fresh award decision.
- Latest Stage 2 admission payment amount/reference integrity pass: applicant admission payment submission now rejects amounts above the selected installment and transaction references already tied to verified admission payments. Staff verification revalidates the same installment amount and duplicate-reference rules for stale pending rows before creating verified admission payment history.
- Latest Stage 2 placement offer-selection integrity pass: placement lifecycle updates now require a positive offered package before moving a student application to `selected`. Direct CMC/admin placement status routes can no longer create official selected placement records without a financial offer amount, while vacancy and final-decision locks remain intact.
- Latest Stage 2 library membership profile-type integrity pass: library membership setup now requires `student` memberships to use users with linked student profiles, `teacher` memberships to use linked teacher profiles, and `staff` memberships to avoid academic-profile users. Direct membership POSTs can no longer relabel student/teacher borrowers into the wrong policy bucket to distort borrowing limits, NOC checks, or active issue counts.
- Latest Stage 2 legacy PMC timetable finality pass: legacy Program Chair auto-schedule acceptance now checks each suggested batch against published timetable versions before creating draft entries. Batch-specific published timetables can no longer be modified through the older auto-schedule route; changes must go through the PMC timetable revision/version workflow.
- Latest Stage 2 PMC assessment-scheme workflow repair: legacy Program Chair/PMC curriculum assessment setup now uses a dedicated `pmc_assessment_component_configs` table/model instead of the official result-score `assessment_components` table. The assessment setup page loads database-backed scheme rows, saves configs with program-subject/term scope validation, blocks total weightage above 100%, and demo seeding now populates IA1/IA2/End-Sem schemes for PMC demo subjects.
- Latest Stage 2 OBE framework history integrity pass: mapped, attained, or survey-used course outcomes can no longer change code, Bloom level, or active state, and mapped/attained program outcomes can no longer change code, category, or active state through direct OBE routes. CO/PO matrix saves now reject rewrites once CO attainment, PO attainment, or OBE survey response history exists, while description-only clarifications remain allowed.
- Latest Stage 2 student quiz result visibility pass: the direct student quiz result route now requires the quiz to remain published before rendering completed results. If a quiz is unpublished for correction or internal review after an attempt, enrolled students can no longer access the result URL even when an attempt exists; normal delayed-result release behavior for published quizzes remains intact.
- Latest Stage 2 inactive-student coursework participation pass: inactive/archived students can still view enrolled published assignment and quiz history, but assignment submission and quiz start/submit POST routes now require an active student profile. Stale enrollments can no longer create new coursework submissions, quiz attempts, scores, or answers for archived students through direct URLs.
- Latest Stage 2 fee proof ledger integrity pass: admin verification of unlinked student fee-payment proofs now applies the verified amount to the student's active fee demands oldest-first, matching manual receipt behavior. This prevents verified receipt history from leaving the same demand balance open for duplicate collection.
- Latest Stage 2 CMC career-event registration history pass: student cancellation now retains career event registration rows with `cancelled` status, cancellation actor, and cancellation timestamp instead of deleting history. Cancelled rows no longer consume event capacity, can be reactivated by a fresh registration before deadline, remain visible to CMC as cancelled history, and cannot be marked attended through direct attendance routes.
- Latest Stage 2 admission application-window integrity pass: application windows with applicant intake history (`current_applications > 0`) can no longer be deleted or rebound to a different batch/open-close date through staff routes. Capacity cannot be reduced below applications already received, while safe description, active/inactive, and capacity-increase edits remain available.
- Latest Stage 2 admission assessment-session integrity pass: selection sessions can no longer be directly updated after leaving `scheduled` state; sessions with assigned candidates, panels, scores, artifacts, lifecycle events, or reschedule history cannot be deleted; completed/cancelled sessions reject candidate assignment/removal and attendance changes; session completion now requires scheduled/ongoing status and no pending candidate attendance.
- Latest Stage 2 admission scoring finality pass: legacy session score-save now preflights all submitted rows, accepts scores only for applicants marked present in the session, refuses to overwrite finalized/overridden/locked scores, and writes `score_status`, `locked_at`, and `locked_by` metadata when leadership marks scores final.
- Latest Stage 2 admission call-letter dispatch integrity pass: session call-letter dispatch now runs only for scheduled/ongoing sessions, skips rejected/withdrawn/enrolled applicants and absent/excused candidates, writes call-letter action URLs, and suppresses duplicate call-letter notifications for the same applicant/session.
- Latest Stage 2 admission lead follow-up finality pass: completed lead follow-ups can no longer be completed again through direct legacy follow-up routes, preserving original `completed_at` timestamps and preventing repeated completion history rewrites.
- Latest Stage 2 specialization master-data integrity pass: admin specialization delete now blocks deletion when students are assigned, preventing the `students.specialization_id` null-on-delete behavior from silently stripping specialization history from enrolled profiles. Empty setup specializations remain removable.
- Latest Stage 2 admission scoring-rubric expansion lock: admin and admission staff scoring-parameter create routes now reject adding new scoring parameters to selection steps that already have sessions or scores, so historical applicant scores cannot be reinterpreted by expanding the rubric after assessments begin.
- Latest Stage 2 admission legacy follow-up scope pass: legacy follow-up calendar, completion, scheduling, and lead assignment routes now respect Admission hierarchy visibility. Managers/counsellors cannot inspect or mutate sibling/out-of-scope lead follow-ups through direct URLs, while leadership/admin visibility remains intact.
- Latest Stage 2 asset intake/assignment condition integrity pass: newly registered assets marked `damaged` or `needs_repair` now enter `maintenance` instead of the available pool, and stale direct assignment attempts for damaged/repair assets are blocked even if the asset status says available.
- Latest Stage 2 consumable stock receipt integrity pass: inventory receive now rejects duplicate receive reference numbers for the same inventory item before incrementing stock, preventing repeated purchase/receipt submissions from double-counting consumable inventory.
- Latest Stage 2 library reservation active-title fulfilment pass: admin reservation fulfilment now revalidates that the reserved book title is still active before creating a live issue, and the fulfilment copy query also requires an active title. Stale pending reservations for deactivated titles remain pending for cancellation/correction instead of circulating archived catalog items through direct fulfilment routes.
- Latest Stage 2 student assignment submission content-integrity pass: student assignment submissions now require either non-blank answer text, an uploaded file, or an existing retained file before creating/updating a submitted record. Direct POSTs can no longer create empty coursework submissions or wipe a text-only submission into a blank submitted state.
- Latest Stage 2 student quiz duration integrity pass: quiz submission now enforces the attempt-level `duration_minutes` limit using the stored `started_at` timestamp, even while the global quiz window remains open. Direct submit routes can no longer complete an expired personal attempt, write answer rows, or assign a score after the allowed duration has passed.
- Latest Stage 2 internship company-name integrity pass: CMC internship registration now uses the canonical active `companies.name` whenever a `company_id` is selected, preventing direct POSTs from linking an internship to one company partner while storing a forged or mismatched display name. Free-text company names remain available when no company partner is selected.
- Latest Stage 2 student exam-registration active-profile guard: student exam registration POST routes now require an active linked student profile before creating or resetting pending registrations. Inactive/archived students can retain and view academic history, but direct registration attempts no longer open Exam Cell queues even when fees, enrollment, and attendance would otherwise pass.
- Latest Stage 2 study-material content integrity pass: teacher study-material uploads now require a real content payload before publishing: a file, an external URL, or non-blank description. Direct POSTs can no longer create title-only published materials that appear on student course pages as empty resources.
- Latest Stage 2 subject-announcement content integrity pass: teacher subject-announcement POST routes now reject whitespace-only titles/bodies and persist trimmed announcement content, preventing blank student-visible course communications while preserving active-teacher and assigned-subject guards.
- Latest Stage 2 teacher assignment creation integrity pass: teacher assignment creation now rejects whitespace-only titles and past due dates before creating student-visible coursework, while stored assignment title, description, and instructions are normalized. Direct POSTs can no longer create blank or already-expired assignments for enrolled students.
- Latest Stage 2 assignment grading finality pass: teacher assignment grading now rejects already graded submissions through the standard grading route, preserving original marks, feedback, grader, and graded timestamp. Grade corrections now require a future audited correction workflow instead of silent POST retries.
- Latest Stage 2 teacher attendance date integrity pass: teacher attendance submission now rejects future dates through direct POSTs, preventing future class attendance from being prewritten into student, parent, Dean, PMC, and eligibility reports. Existing active-teacher, roster, and canonical attendance-status guards remain intact.
- Latest Stage 2 teacher attendance schedule integrity pass: teacher attendance GET/POST routes now require active timetable entries for the selected teacher and matching scheduled day before showing rosters or writing attendance. Direct POSTs can no longer mark inactive class entries or write attendance on unscheduled dates for a class.
- Latest Stage 2 admin attendance schedule integrity pass: admin attendance mark/store routes now reject future dates, inactive timetable entries, and dates that do not match the class schedule before writing official attendance. Admin roster/status validation remains intact while direct finance/academic reports no longer receive unscheduled admin attendance rows.
- Latest Stage 2 admin exam-result timing integrity pass: admin exam-result save now rejects future-dated exams before writing marks, grades, or absence state. Official result rows can no longer be created for exams that have not occurred, while existing roster, marks bounds, absent-state, and published-result locks remain intact.
- Latest Stage 2 teacher exam-result timing integrity pass: teacher exam-result save now rejects future-dated exams before writing marks or absence state. Teachers can still enter results for completed, unpublished exams they teach, while roster, marks bounds, absent-state, active-teacher, and published-result locks remain intact.
- Latest Stage 2 admin exam-schedule active-master integrity pass: admin exam create/update routes now require active subjects, and exam creation requires active classrooms when a room is selected. Direct POST/PUT routes can no longer schedule new official exams against archived subjects or inactive rooms while existing published/history locks remain intact.
- Latest Stage 2 admin exam-schedule semester-window integrity pass: admin exam create/update routes now reject exam dates outside the selected semester's configured start/end date range. Direct POST/PUT routes can no longer attach official exams to an academic period while scheduling them outside that period.
- Latest Stage 2 admin enrollment active-profile/master integrity pass: manual enrollment now requires an active student, and bulk enrollment now requires an active course before creating legacy enrollment or canonical `student_subject_enrollments` rows. Direct POSTs can no longer reactivate archived students into current academics or create bulk active subject history under inactive course masters.
- Latest Stage 2 student/teacher timetable publication-visibility pass: legacy student and teacher daily timetable views now show only active `published` timetable entries, and linked timetable versions must also be published when present. Draft/generated timetable rows remain available to PMC planning workflows but no longer leak into student or faculty daily schedules before official publication.
- Latest Stage 2 student dashboard timetable visibility pass: the student dashboard Today's Classes feed now uses student-specific enrolled subjects plus published timetable/version filters instead of a course-level weekly grid. Dashboard class rows no longer expose unenrolled subjects, draft timetable entries, or rows from draft timetable versions, and the card now renders faculty and room from loaded database relationships.
- Latest Stage 2 parent attendance timetable visibility pass: parent dashboard attendance priority and child attendance detail now count only active published timetable entries, and linked timetable versions must also be published. Draft timetable attendance and attendance tied to draft timetable versions no longer distort parent-facing attendance percentages or appear in subject-wise parent reports.
- Latest Stage 2 teacher dashboard timetable visibility pass: the teacher dashboard weekly schedule, today's-class priority, and weekly load now use only active published timetable entries, and linked timetable versions must also be published. Draft/generated PMC timetable rows remain available to planning workflows but no longer appear as faculty daily work or workload counts before official publication.
- Latest Stage 2 attendance write-side timetable publication lock: admin and teacher attendance mark/store routes now accept only active published timetable entries, and linked timetable versions must also be published. Direct attendance POSTs can no longer create official attendance history from draft timetable entries or draft timetable versions, while existing roster, schedule-date, active-teacher, and status validation remain intact.
- Latest Stage 2 admin attendance reporting publication boundary: admin attendance HTML reports, CSV exports, and PDF report data now include only attendance tied to active published timetable entries, and linked timetable versions must also be published. Stale draft-timetable attendance rows no longer distort official admin attendance reporting or exports.
- Latest Stage 2 student attendance reporting publication boundary: student attendance summary and subject session pages now include only enrolled-subject attendance tied to active published timetable entries, and linked timetable versions must also be published. Stale draft-timetable, draft-version, or unenrolled-subject attendance rows no longer distort student-facing percentages or session lists.
- Latest Stage 2 student exam-registration attendance eligibility boundary: student exam-registration list and direct registration checks now calculate attendance eligibility only from active published timetable entries, and linked timetable versions must also be published. Draft/generated timetable attendance can no longer create false low-attendance blockers or warnings for official exam registration.
- Latest Stage 2 student attendance-condonation publication boundary: student condonation create/store eligibility now uses only attendance tied to active published timetable entries, and linked timetable versions must also be published. Draft/generated timetable absences can no longer create false low-attendance eligibility or formal condonation requests.
- Latest Stage 2 teacher mentor/student attendance publication boundary: teacher mentor dashboards, mentee detail attendance summaries, and teacher student-list attendance metrics now use only active published timetable entries, and linked timetable versions must also be published. Draft/generated timetable attendance no longer distorts mentor intervention views or teacher-facing student attendance summaries.
- Latest Stage 2 admin analytics attendance publication boundary: Admin institutional analytics now calculates overall attendance, program attendance, and at-risk attendance lists only from active published timetable entries, and linked timetable versions must also be published. Draft/generated timetable attendance no longer distorts executive analytics or at-risk student lists.
- Latest Stage 2 HOD attendance publication boundary: HOD dashboard attendance KPIs and department-performance subject attendance now count only active published timetable entries, and linked timetable versions must also be published. Draft/generated timetable attendance no longer triggers false HOD attendance priorities or distorts departmental performance tables.
- Latest Stage 2 Dean attendance publication boundary: legacy Dean dashboard attendance KPIs, at-risk student list, and Dean attendance-by-program page now count only active published timetable entries, and linked timetable versions must also be published. Draft/generated timetable attendance no longer triggers false Dean risk priorities or lowers official program attendance.
- Latest Stage 2 Program Chair attendance publication boundary: legacy Program Chair dashboard attendance KPIs, low-attendance subject list, dashboard at-risk signals, and linked at-risk student drill-down now count only active published timetable entries, and linked timetable versions must also be published. Draft/generated timetable attendance no longer creates false Program Chair intervention priorities.
- Latest Stage 2 Academics service attendance publication boundary: Academics Command Center attention queues and Program Leadership student-success services now count attendance risk only from active published timetable entries, with linked timetable versions also required to be published. Draft/generated timetable attendance no longer creates management attention items or Program Leadership student-risk cards.
- Latest Stage 2 Course Delivery/PMC service attendance publication boundary: Course Delivery attendance interventions and PMC operating student-monitoring services now count attendance risk and recent exceptions only from active published timetable entries, with linked timetable versions also required to be published. Draft/generated timetable attendance no longer creates Course Delivery or PMC student-risk cards.
- Latest Stage 2 academic calendar update test stability pass: academic calendar update regression now creates an explicitly future event before exercising the past-event edit lock, removing a timing-dependent factory edge case while keeping the real historical-lock behavior intact.
- Latest Stage 2 timetable-version publication boundary for management services: Academics attention, Program Leadership, Course Delivery, and PMC Operating services now treat timetable entries linked to draft/unpublished timetable versions as unpublished even when the entry row status is `published`. Official published-slot/session/workload metrics require both the entry and linked version to be published, while draft-version rows remain visible in draft/publish-pending queues for PMC planning review.
- Latest Stage 2 legacy timetable reporting services publication boundary: Faculty workload, classroom capacity/utilization, load balancing, and teacher workload warning services now use only active published timetable entries with published linked timetable versions. Draft/generated timetable rows no longer inflate workload, room utilization, load-balancing, or teacher-warning metrics. This pass also corrected reversed slot-duration calculations and made classroom capacity use actual student count with `intake_capacity` fallback instead of assuming a nonexistent `Batch::student_count` column.
- Latest Stage 2 timetable PDF publication boundary: legacy timetable PDF generation for batch and teacher exports now includes only active published timetable entries whose linked timetable version is either absent or published. Draft rows and rows linked to draft timetable versions are excluded from official PDF HTML before DomPDF rendering, preventing planning timetables from leaking into official exports.
- Latest Stage 2 legacy timetable import/copy integrity pass: Program Chair import validation now checks the submitted `term_id` instead of a nonexistent target-term field, copy preview/execute validate both source and target terms explicitly, and imported/copied legacy rows receive required semester/course bridge values. Import and copy replacement now delete only empty draft target rows; published, version-linked, attendance-bearing, or substitution-bearing timetable rows are locked and preserved for PMC revision/version workflows.
- Latest Stage 2 legacy timetable planning-audit integrity pass: Program Chair soft-constraint audits now inspect only active planning rows, accept Eloquent collections correctly, and use the real `teacher_availability.availability` field so unavailable faculty assignments are detected. Legacy timetable capacity checks now use actual enrolled student count with `intake_capacity` fallback instead of assuming a nonexistent `Batch::student_count` column.
- Latest Stage 2 student fee-proof duplicate-reference integrity pass: student fee payment proof submission now rejects transaction references that are already pending verification, not only references already tied to paid receipts. This prevents the same bank/UPI reference from entering the Accounts verification queue multiple times across different fee demands or unlinked proof submissions.
- Latest Stage 2 Exam Cell result timing integrity pass: Exam Cell marks-save and publish-result routes now reject future-dated exams before writing marks, absence state, publication timestamps, or student result notifications. This aligns the CoE/Exam operational route with the existing Admin and Teacher result-entry guards so official result history cannot be created before the exam happens.
- Latest Stage 2 applicant admission-payment duplicate-reference integrity pass: applicant admission fee proof submission now rejects transaction references that are already pending verification, not only references already tied to verified admission payments. This prevents the same bank/UPI reference from entering the Admission payment queue across multiple installments before staff review.
- Latest Stage 2 applicant document requirement-scope integrity pass: applicant self-service document uploads now require the selected RequiredDocument to be active and belong to the applicant's own program. Direct POSTs can no longer attach inactive or wrong-program document requirements to an application, keeping document readiness/enrollment checks aligned with the configured application checklist.
- Latest Stage 2 manual fee receipt duplicate-reference integrity pass: admin manual fee collection now rejects transaction IDs already linked to paid fee receipts before creating a new receipt or reducing demand balances. Manual fee collection also normalizes UPI/NEFT/RTGS methods to the stored online method, matching student proof verification and avoiding database-level payment method failures.
- Latest Stage 2 admission refund hierarchy-scope integrity pass: Admission refund listing, create/store, show, approve, reject, and process routes now respect Admission hierarchy visibility. Managers/counsellors can no longer inspect or mutate sibling/out-of-scope applicant refunds through direct URLs, while visible subordinate refunds remain actionable.
- Latest Stage 2 applicant scholarship hierarchy-scope integrity pass: applicant scholarship award, cancellation, disbursement, and disbursement queue totals now respect Admission hierarchy visibility. Managers/counsellors can no longer award, cancel, disburse, or see sibling/out-of-scope applicant scholarship commitments through direct routes, and rejected/withdrawn applicants can no longer receive fresh scholarship awards.
- Latest Stage 2 legacy admission conversion finality pass: legacy admin admission conversion now blocks rejected or withdrawn admissions from being converted into active student records through direct POSTs. Final negative admission outcomes must be reopened through a future audited correction workflow before student creation, while shortlisted/applied admissions continue to use the existing conversion path.
- Latest Stage 2 legacy admission admitted-status integrity pass: legacy admissions can no longer be created, edited, or direct-status-updated to `admitted` without going through Convert to Student. The only supported admitted transition now creates the linked active student record and stores `converted_student_id`, preventing official admitted rows with no student lifecycle record.
- Latest Stage 2 official timetable PDF publication boundary: admin official timetable PDF export now builds its grid with published-only timetable entries and excludes entries linked to draft/unpublished timetable versions. Admin timetable planning screens keep using the broader planning grid, but official exported timetables no longer leak draft/generated PMC planning rows.
- Latest Stage 2 fee payment proof demand-ownership integrity pass: admin fee proof verification now rejects malformed/stale proof rows linked to another student's fee demand. A verified receipt can no longer be created for one student while reducing a different student's open demand balance through a direct verification route.
- Latest Stage 2 financial scholarship read-access integrity pass: legacy academic scholarship index, create, show, and edit routes now enforce the same scholarship-management policy as write routes. Broad academic roles such as Program Chair can no longer directly view financial scholarship records or forms unless they also hold scholarship-management authority.
- Latest Stage 2 student scholarship queue access-control pass: admin student-scholarship application listing, shortlist, approval, rejection, disbursement, and proof-download routes now require scholarship-management authority. Broad academic roles inside the admin route group can no longer inspect or mutate student financial-aid applications through direct URLs.
- Latest Stage 2 admin fee-management access-control pass: admin fee structure, manual collection, receipt/report/export, payment-proof listing, proof download, verification, and rejection routes now require fee-management authority through the existing fee access policy. Broad academic roles inside the admin route group can no longer inspect or mutate fee ledgers or student payment proofs through direct URLs, while accounts officers retain queue verification access.
- Latest Stage 2 admin student-document request access-control pass: admin document request listing, approval, rejection, fulfillment, and ready-document download routes now require student-document management authority. Broad academic roles inside the admin route group can no longer issue, reject, fulfill, or download official student documents through direct URLs, while existing active-student and NOC clearance guards remain intact.
- Latest Stage 2 global admin leave access-control pass: global admin leave listing, detail, approval, rejection, and cancel/delete routes now require global leave-management authority. Program Chair/HOD users continue using their scoped leave review routes, but can no longer inspect or mutate all-program leave records through the broad admin route group.
- Latest Stage 2 global admin grievance access-control pass: global admin grievance listing, detail, and update routes now require global grievance-management authority. Program Chair/HOD users continue using scoped grievance workflows, but can no longer inspect or mutate all-program grievances through the broad admin route group.
- Latest Stage 2 global bulk-mail access-control pass: admin bulk-mail page, audience preview/count endpoint, and send action now require global bulk-communication authority. Broad academic roles inside the admin route group can no longer preview or send all-student/applicant/role email blasts through direct URLs.
- Latest Stage 2 role-assignment access-control pass: scoped program role assignments and global user-role management now require explicit security-admin authority (`admin` or `director`) at controller level. Broad academic roles inside the admin route group can no longer list, create, revoke, or expire role assignments through direct URLs, while existing assignment history, actual Spatie role synchronization, and final-admin protections remain intact.
- Latest Stage 2 role-permission access-control pass: role hierarchy, permission matrix, and feature-access matrix routes now require explicit security-admin authority (`admin` or `director`) at controller level. Broad academic roles inside the admin route group can no longer inspect or mutate role capability configuration through direct URLs, and denied PUT attempts leave permission/feature-access rows unchanged.
- Latest Stage 2 system-configuration access-control pass: admin settings, branding update, API docs, and organization reporting hierarchy routes now require explicit system-configuration authority (`admin` or `director`) at controller level. Broad academic roles inside the admin route group can no longer inspect or mutate institute-wide settings or reporting lines through direct URLs, and denied hierarchy mutations leave active reporting rows unchanged.
- Latest Stage 2 global admin read-surface access-control pass: admin global search, activity log, and institutional analytics now require explicit read authority at controller level. Program Chair/HOD-style broad academic users can no longer search all students/teachers/notices, inspect global activity logs, or open institution-wide admin analytics through direct URLs; Dean Academics retains institutional search/analytics access but not global audit-log access.
- Latest Stage 2 global admin export access-control pass: admin student and attendance CSV exports now require global academic export authority (`admin`, `director`, or `dean_academics`), while fee CSV export remains limited to fee-management authority. Static export routes were moved before student/fee resource wildcards so direct export URLs reach their guarded controllers instead of being swallowed by parameter routes.
- Latest Stage 2 global reporting/export access-control pass: placement CSV export now requires placement-export authority (`admin`, `director`, `dean_academics`, or `cmc`), institutional KPI and AICTE report/PDF routes require regulatory-report authority (`admin`, `director`, or `dean_academics`), and admin email delivery logs require `admin` or `director`. Broad academic roles can no longer inspect or download these global reporting surfaces through direct URLs.
- Latest Stage 2 admin placement/company access-control pass: admin placement-drive and company routes now require placement operations authority (`admin`, `director`, `dean_academics`, or `cmc`) at controller level. Program Chair/HOD-style broad academic users can no longer list, create, edit, delete, add students to drives, or change placement application status through `/admin` direct URLs, while CMC retains placement operating access and existing lifecycle guards still enforce recruiter/drive/application finality.
- Latest Stage 2 official notice and faculty-report access-control pass: admin notice management routes now require official-notice authority (`admin`, `director`, or `dean_academics`), and global admin faculty workload reporting requires global faculty-report authority (`admin`, `director`, or `dean_academics`). Program Chair/HOD-style broad academic users keep scoped notice/workload surfaces but can no longer mutate official institute notices or inspect global faculty workload through direct `/admin` URLs.
- Latest Stage 2 academic identity master access-control pass: admin student and teacher master-record routes now require academic identity authority (`admin`, `director`, or `dean_academics`) at controller level. Program Chair/HOD-style broad academic users can no longer list, create, inspect, edit, or archive global student/teacher profiles through `/admin` direct URLs, while scoped academic and teacher workflows remain separate.
- Latest Stage 2 parent identity master access-control pass: admin parent master-record routes now require academic identity authority (`admin`, `director`, or `dean_academics`) at controller level. Program Chair/HOD-style broad academic users can no longer list, create, inspect, edit, link/unlink, or archive parent portal profiles through `/admin` direct URLs, preserving family portal linkage history and keeping scoped parent-facing workflows separate.
- Latest Stage 2 academic scheduling setup access-control pass: admin classroom, timetable-slot, and global admin timetable routes now require academic scheduling authority (`admin`, `director`, or `dean_academics`) at controller level. Program Chair/HOD/Exam/Accounts/CMC-style users continue using their scoped operating surfaces but can no longer inspect or mutate global scheduling setup records through broad `/admin` direct URLs.
- Latest Stage 2 global exam access-control pass: admin exam schedule, exam result-entry, and global result-report routes now require exam-management authority (`admin`, `director`, `dean_academics`, or `exam_cell`) at controller level. Program Chair/HOD/Accounts/CMC-style users continue using their scoped academic surfaces but can no longer inspect or mutate global exam/result records through broad `/admin` direct URLs.
- Latest Stage 2 global enrollment access-control pass: admin enrollment listing, manual enrollment, bulk enrollment, and deletion routes now require global enrollment authority (`admin`, `director`, or `dean_academics`) at controller level. Program Chair/HOD/Exam/Accounts/CMC-style users continue using scoped academic workflows but can no longer add, bulk-add, inspect, or drop all-student enrollment history through broad `/admin` direct URLs.
- Latest Stage 2 academic-structure access-control pass: admin department, course, program, batch, term, subject, academic-year, and semester setup routes now require academic-structure authority (`admin`, `director`, or `dean_academics`) at controller level. Program Chair/HOD/Exam/Accounts/CMC-style users continue using scoped workflows but can no longer inspect or mutate global academic contract setup through broad `/admin` direct URLs.
- Latest Stage 2 global attendance access-control pass: admin attendance dashboard, entry lookup, mark/store, report, PDF report, and export routes now require global attendance authority (`admin`, `director`, or `dean_academics`) at controller level. Program Chair/HOD/Exam/Accounts/CMC-style users continue using scoped or role-specific attendance surfaces but can no longer inspect, probe, export, or mutate all-student attendance through broad `/admin` direct URLs.
- Latest Stage 2 official admin report access-control pass: program specialization setup now uses the academic-structure authority, official admin grade cards/consolidated academic reports/timetable PDFs require official academic document authority (`admin`, `director`, `dean_academics`, or `exam_cell`), and admin fee-receipt PDFs require official financial document authority (`admin`, `director`, `dean_academics`, or `accounts_officer`). Broad scoped roles can no longer generate global official PDFs through direct `/admin` report URLs.
- Latest Stage 2 legacy admin admission access-control pass: legacy `/admin` applicant and admission/enquiry routes now require explicit legacy admission-operation authority at controller level. Program Chair/HOD/Exam/Accounts/CMC-style users inside the broad admin route group can no longer inspect applicant/admission records, create applicants/enquiries, save applicant notes, change legacy admission status, or convert admissions to students through direct URLs; Admin, Director, Dean Academics, and Admission leadership roles remain authorized.
- Latest Stage 2 legacy admin admission-configuration access-control pass: legacy `/admin/admission-config/*` routes now require explicit admission-configuration authority at controller level. Broad scoped roles inside the admin route group can no longer inspect or mutate admission form sections, required documents, default documents, selection steps, scoring parameters, or admission fee-installments through direct URLs; Admin, Director, Dean Academics, and Admission leadership/management roles remain authorized.
- Latest Stage 2 asset-management access-control pass: admin asset register, asset-category setup, asset intake, consumable stock-item setup, stock receive/issue, asset assignment, and asset return routes now require institutional asset-management authority (`admin` or `director`) at controller level. Dean/Program Chair/HOD/Exam/Accounts/CMC-style broad admin users can no longer inspect or mutate institutional asset custody or inventory movements through direct `/admin/assets` URLs, while existing asset condition, stock reference, quantity, and timing guards remain intact.
- Latest Stage 2 transport-operations access-control pass: admin transport dashboard, route/stop setup, vehicle setup/update, student transport assignment, and assignment-end routes now require transport-operations authority (`admin` or `director`) at controller level in addition to the existing department-feature toggle. Dean/Program Chair/HOD/Exam/Accounts/CMC-style broad admin users can no longer inspect or mutate transport operations through direct `/admin/transport` URLs, while existing active-student, active-route/stop/vehicle, capacity, duplicate-assignment, and date guards remain intact.
- Latest Stage 2 library-operations access-control pass: admin library catalog, book setup/update, circulation issue/return, reservation fulfillment/cancellation, membership setup, fine listing, and fine payment routes now require library-operations authority (`admin`, `director`, or `librarian`) at controller level in addition to the existing department-feature toggles. Dean/Program Chair/HOD/Exam/Accounts/CMC-style broad admin users can no longer inspect or mutate library circulation, membership, reservation, or fine records through direct `/admin/library` URLs, while existing active-borrower, reservation queue, membership-limit, fine, and NOC clearance guards remain intact.
- Latest Stage 2 hostel-operations access-control pass: admin hostel block/room setup, allocation/vacate/transfer, fee generation/payment/waiver, outpass approval/rejection/return, and complaint update routes now require hostel-operations authority (`admin`, `director`, or `hostel_warden`) at controller level in addition to existing department-feature toggles. Dean/Program Chair/HOD/Exam/Accounts/CMC-style broad admin users can no longer inspect or mutate hostel operations through direct `/admin/hostel` URLs, while existing active-student, active-block/room, capacity, fee, outpass, and complaint finality guards remain intact.
- Latest Stage 2 admin audit-log access-control pass: admin audit-log index, search, and detail routes now require global activity-log authority (`admin` or `director`) at controller level. Dean/Program Chair/HOD/Exam/Accounts/CMC-style broad admin users can no longer inspect role/security/change audit history through direct `/admin/audit-log` URLs.
- Latest Stage 2 department-hierarchy admin-alias access-control pass: global `/admin/department-hierarchy/*` write aliases now require system-configuration authority (`admin` or `director`) before reaching scoped department hierarchy mutation logic. Legitimate department owners continue using `/department-hierarchy/*` scoped governance routes, while non-system department owners can no longer mutate hierarchy through broad admin aliases by direct URL.
- Latest Stage 2 inactive-department governance integrity pass: inactive departments are now excluded from manageable department hierarchy/governance lists and cannot be configured, have feature flags changed, or start department impersonation sessions through direct governance/hierarchy routes. Active historical memberships remain stored, but operational department control is closed once the department is inactive.
- Latest Stage 2 admission seat-matrix integrity pass: Admission seat matrix creation/update now requires active programs, active batches belonging to the selected program, and total seats that are not lower than the sum of category/quota seats. Direct routes can no longer attach a seat matrix to another program's batch or publish impossible capacity math that would distort waitlist, offer, and enrollment control. `BatchFactory` now generates deterministic unique batch codes to keep high-volume full-suite verification stable.
- Latest Stage 2 admission selection-session scope integrity pass: Admission selection session create/update now require the selected step to be active and belong to the chosen program, and any selected batch must be an active batch under that same program. Manual candidate assignment now accepts only shortlisted applicants in the session program/batch, preventing direct routes from binding assessments, call letters, or attendance to wrong-program/wrong-batch applicants.
- Latest Stage 2 admission enrollment academic-scope integrity pass: Admission enrollment now lists only active specializations for the applicant's program, validates submitted specializations against that program, and validates selected terms against the applicant's program/batch both at controller validation and inside `EnrollmentService`. Direct enrollment POSTs or service calls can no longer create active students with cross-program specializations or wrong-batch terms.
- Latest Stage 2 accounts demand-letter finality pass: Accounts demand-letter PDF generation is now limited to active students with open outstanding fee demands (`pending`, `partially_paid`, or `overdue`). Fully paid demand history and inactive/archived-student demands remain reportable but no longer produce fresh official demand letters through direct URLs.
- Latest Stage 2 admission reporting hierarchy-scope integrity pass: Admission reporting dashboard and PDF export now share one hierarchy-scoped report-data builder. Program, source, category, YoY, counsellor, geography, seat-compliance, and enrollment counts are derived only from the authenticated user's visible Admission leads/applicants, preventing counsellors/managers from seeing peer or sibling pipeline analytics through reports or export routes.
- Latest Stage 2 legacy PMC report attendance/publication boundary pass: Program Chair/PMC attendance-defaulter and term-summary reports now count attendance only through active published timetable entries, with linked timetable versions also required to be published. The attendance-defaulter report now groups subjects through `timetable_entries.subject_id` instead of a nonexistent attendance column, and non-oversight Program Chair users without active program assignments no longer fall back to all programs.
- Latest Stage 2 Exam Cell exam-setup academic-contract pass: Exam Cell create/update now validates active program, active subject, active classroom, subject-to-program ownership, term-to-program ownership, and a resolvable semester before writing exam rows. Exam Cell forms now expose semester selection, omitted legacy semester input is safely resolved from existing exam/term/current semester, and explicit semester-term mismatches are rejected so wrong-program exams cannot corrupt eligibility, hall-ticket, result-entry, or reporting workflows.
- Latest Stage 2 Exam Cell scheduling-contract integrity pass: Exam Cell create/update now reject exam dates outside the selected/resolved semester window, end times that are not after start times, and passing marks greater than total marks. This keeps CoE/Exam setup from creating impossible exam contracts that would later distort hall-ticket, result-entry, and reporting workflows.
- Latest Stage 2 Admin exam program-contract integrity pass: global Admin exam create/update now derives and persists `program_id` from the selected active subject, and exams with result/registration history treat program changes as locked academic-contract changes. Admin exams can no longer be created or resaved with a missing/stale program link that would distort eligibility, CoE reports, student admit-card checks, or program-level academic analytics.
- Latest Stage 2 student admit-card active-student boundary pass: student admit-card listing and direct PDF download now require the linked student profile to remain active in addition to approved registration, fee clearance, attendance clearance, program match, and subject enrollment. Stale approved exam registrations for inactive/archived students no longer produce fresh official exam-entry documents.
- Latest Stage 2 student-facing published-exam visibility pass: student dashboard upcoming exams now exclude exams whose results are already published, matching the dedicated exam-registration surface. Admit-card listing/download also reject result-published exams even if stale approved registration rows remain, so published-result history cannot generate fresh exam-entry actions or cards.
- Latest Stage 2 student academic-summary attendance publication boundary pass: student academic summary attendance KPI now counts only attendance tied to active published timetable entries, and linked timetable versions must also be published. Draft timetable absences and published rows attached to draft timetable versions no longer lower the student-facing summary card attendance percentage.
- Latest Stage 2 student API attendance publication boundary pass: `/api/v1/student/attendance` now returns only attendance tied to active published timetable entries, with linked timetable versions also required to be published. Draft timetable rows and rows attached to draft timetable versions no longer appear in mobile/API attendance records or lower API attendance percentages.
- Latest Stage 2 student API fee-ledger integrity pass: `/api/v1/student/fees` now uses `fee_demands` as the canonical fee balance source whenever demand rows exist, including active penalties and fully paid demand history. Legacy fee-structure totals remain only as a no-demand fallback, so mobile/API fee balances no longer diverge from student/parent web dashboards.
- Latest Stage 2 student API fee-penalty semantics pass: `/api/v1/student/fees` now matches the web fee pages by adding penalties only from active open demands (`pending`, `partially_paid`, `overdue`) while keeping fully paid demand final amounts in billed history. Paid-history penalty values no longer inflate API total due or open balance.
- Latest Stage 2 student API result-enrollment scope pass: `/api/v1/student/results` now requires each published result to belong to a currently active student subject enrollment or legacy active/enrolled enrollment for that exam subject/term. Stale published result rows for unenrolled subjects no longer appear in mobile/API result history.
- Latest Stage 2 student fee-payment proof reservation pass: student-submitted pending fee proofs now reserve the student's open fee balance until accounts review, preventing additional linked or unlinked proof submissions from collectively exceeding active demand balance before verification/rejection.
- Latest Stage 2 student document/NOC priority guidance pass: the student document dashboard no longer promotes a ready NOC as immediately downloadable when current clearance has become invalid. It now reuses the same active-student/NOC clearance checks as the download routes and shows the exact blocker instead of a misleading download priority.
- Latest Stage 2 student career-event attendance finality pass: student career-event re-registration now blocks existing registrations with recorded attendance, preventing direct register POSTs from resetting attended history back to `false` while still allowing cancelled non-attended registrations to reactivate through the normal capacity/deadline checks.
- Latest Stage 2 teacher dashboard pending-grading roster pass: the teacher dashboard pending-grading KPI now counts only submitted assignment rows from students currently in the assignment roster, matching the roster-filtered submissions page and preventing rogue/stale submissions from inflating the daily priority card.
- Latest Stage 2 teacher assignment-list source integrity pass: the teacher assignment index now honors its subject filter and computes the submitted-count badge from the same live roster boundary as the submissions page, so out-of-roster stale submissions and unassigned-subject assignments no longer distort the teacher's source list.
- Latest Stage 2 teacher student-list roster integrity pass: the teacher student list now builds its roster per published timetable entry, matching legacy semester enrollments and canonical term enrollments through the same semester-to-term fallback used by attendance marking. Teachers with no published entries get an empty list, and same-subject students from unrelated terms no longer appear.
- Latest Stage 2 student marks-appeal UI/finality pass: the new marks-appeal selector now excludes results that already have an appeal, while duplicate direct POSTs still return the existing duplicate-appeal validation response. Inactive students can view appeal history, but the index no longer shows the misleading new-appeal action.
- Latest Stage 2 fee-payment transaction-reference integrity pass: student payment-proof submission and admin/accounts proof verification now compare transaction references case-insensitively against pending proofs and paid receipts. Case-only variants of the same UTR/reference can no longer create duplicate pending proof rows or duplicate paid fee receipts.
- Latest Stage 2 library reservation duplicate-title pass: student library reservation requests now reject titles the same student already has actively issued or overdue, preventing duplicate borrower/title claims from entering the reservation queue before the current copy is returned.
- Latest Stage 2 student transport current-assignment visibility pass: student transport pages now show a current assignment only when the student profile is active, the assignment period is current, the route and stop are active, and any linked vehicle is active. Stale direct data remains in transport history but no longer appears as active service.
- Latest Stage 2 student scholarship application UI/finality pass: inactive student profiles can still view scholarship schemes and application history, but the scholarship page now hides application forms/actions and shows locked guidance, matching the existing direct POST guard that blocks new scholarship applications for inactive students.
- Latest Stage 2 student fee balance guidance pass: the student fee dashboard top balance card now includes open hostel fee demands alongside academic fee demand balance, so a student with pending hostel dues no longer sees misleading "Fully paid - No dues" guidance while hostel fees remain outstanding.
- Latest Stage 2 student fee-payment proof availability pass: student payment-proof submission is now offered only when the student has open academic fee demands. Active students with only hostel dues see explicit guidance that hostel dues are handled through the hostel/accounts queue, and direct create-route access redirects instead of opening an unusable academic-demand proof form.
- Latest Stage 2 parent hostel-fee visibility pass: parent dashboard finance priority, child fee balance, open-demand counts, and child fee detail pages now include pending hostel fee demands alongside academic fee demands. Guardians can see hostel-specific month, room, amount, due date, and overdue/paid/waived status instead of receiving misleading no-dues guidance for hostel-only balances.
- Latest Stage 2 student API hostel-fee visibility pass: `/api/v1/student/fees` now includes hostel fee demands in `total_due`, `balance`, and a dedicated `hostel_balance`/`hostel_demands` payload, aligning mobile/API fee balances with the student and parent web portals while preserving paid academic receipt filtering.
- Latest Stage 2 student dashboard hostel-fee priority pass: the student dashboard fee warning, KPI, and Today's Priority now include pending hostel fee demands alongside academic fee demands, so hostel-only dues surface on the first student screen instead of showing no urgent fee action.
- Latest Stage 2 admission payment transaction-reference integrity pass: applicant admission payment proof submission and staff verification now compare transaction references case-insensitively against pending and verified admission payments. Case-only UTR/reference variants can no longer create duplicate pending admission payment rows or allow stale pending proofs to be verified against an already verified reference.
- Latest Stage 2 NOC hostel-fee clearance coverage pass: student NOC priority/download regression coverage now proves ready NOC documents remain blocked when hostel fee demands are still pending, matching the shared clearance service used by student and admin document-download paths.
- Latest Stage 2 admission registration-fee reference integrity pass: applicant self-service and admission staff registration-fee recording now reject receipt/reference numbers already linked to another applicant, case-insensitively. Case-only registration-fee UTR/reference variants can no longer mark a second application as paid.
- Latest Stage 2 admission registration-fee final-state pass: admission staff registration-fee pages now lock rejected, withdrawn, and enrolled applicants, and direct staff POSTs can no longer record fresh registration-fee details once an application is already in a final admission state.
- Latest Stage 2 admission registration-fee hierarchy-scope pass: staff registration-fee show/store routes now use Admission hierarchy visibility, so managers can record fees only for applicants assigned to their visible subtree and direct URLs cannot expose or mutate sibling counsellor applicants.
- Latest Stage 2 admission application-PDF hierarchy-scope pass: staff application-form PDF generation now applies Admission hierarchy visibility before rendering submitted applicant PDFs. Out-of-scope counsellors/managers can no longer download another team member's applicant application PDF through a direct URL.
- Latest Stage 2 admission call-letter hierarchy-scope pass: staff assessment call-letter PDF generation now applies Admission hierarchy visibility before resolving active session assignment and rendering the PDF, preventing out-of-scope counsellors/managers from downloading another team member's call letter through a direct URL.
- Latest Stage 2 admission enrollment hierarchy-scope pass: enrollment create/store/show and enrollment-letter routes now apply Admission hierarchy visibility. Out-of-scope counsellors/managers can no longer enroll, inspect confirmations, or download official enrollment letters for another team member's applicant through direct URLs.
- Latest Stage 2 admission enrollment-index hierarchy-scope pass: enrollment confirmation lists and enrollment summary cards now use the same Admission hierarchy visibility as enrollment create/show/letter routes. Out-of-scope counsellors/managers no longer see sibling/peer completed enrollment rows or leaked global enrollment counts from the index page.
- Latest Stage 2 admission assessment-scoring hierarchy-scope pass: assessment score sheets, score save routes, existing score lookups, and applicant scorecards now apply Admission hierarchy visibility. Out-of-scope counsellors/managers can no longer view sibling applicants on scoring sheets, save scores for them through direct POSTs, or inspect their applicant scorecards.
- Latest Stage 2 admission offer-letter hierarchy-scope pass: staff offer-letter list rows, offer summary cards, direct show/PDF routes, staff accept/decline actions, waitlist auto-promotion, and offer generation now respect Admission hierarchy visibility. Scoped staff can still work unassigned selected applicants for compatibility, but cannot view, mutate, or generate offers for sibling-assigned applicants.
- Latest Stage 2 admission merit-list hierarchy/authority pass: merit-list index/show/category/export rows now respect Admission hierarchy visibility, while merit generation, single decisions, and bulk decisions require Admission approval authority. Scoped staff can view their assigned and unassigned applicants for compatibility but cannot regenerate merit lists or action decisions through direct POST routes.
- Latest Stage 2 admission selection-session candidate-scope pass: selection-session detail pages now show scoped assigned candidates, scoped stats, and scoped available-candidate lists. Candidate assignment, removal, attendance marking, auto-assign, and call-letter dispatch now respect Admission hierarchy visibility, preserving unassigned-candidate compatibility while blocking sibling-assigned applicant mutation through direct routes.
- Latest Stage 2 PMC timetable rollback lifecycle pass: Dean/Admin timetable rollback now moves or rebuilds the operational `timetable_entries` onto the newly published rollback version instead of creating only an empty version/workflow header. This preserves student/faculty visibility after rollback while keeping old published versions archived and avoiding duplicate timetable rows under the existing room/teacher/course uniqueness constraints.
- Latest Stage 2 teacher exam-result scope pass: teacher exam result entry now requires a matching active published timetable assignment for the same teacher, subject, program, and term/semester. A teacher assigned to a subject in one program/term can no longer open or POST marks for another program's exam that happens to reuse the same subject ID through a direct route.
- Latest Stage 2 student scholarship disbursement-reference integrity pass: student scholarship disbursement now trims and checks disbursement references case-insensitively before writing committed disbursement state. Case-only UTR/reference variants can no longer be reused across different student scholarship awards.
- Latest Stage 2 hostel fee generation integrity pass: monthly hostel fee generation now treats `student_id + month` as the billing identity instead of `allocation_id + month`, preventing duplicate monthly hostel dues after room transfers. Generation also skips allocations that start after the billing month, preventing pre-allocation charges from inflating student/parent balances or blocking NOC clearance.
- Latest Stage 2 manual fee-payment transaction-reference integrity pass: direct admin/accounts manual fee collection now trims and checks paid transaction references case-insensitively, matching student proof submission and accounts proof verification. Case-only UTR/reference variants can no longer create duplicate paid fee receipts through manual collection routes.
- Latest Stage 2 applicant scholarship disbursement/finality pass: applicant scholarship disbursement now trims and checks disbursement references case-insensitively before writing committed disbursed state, and enrolled applicants are treated as closed for fresh applicant-scholarship award/disbursement routes. Case-only UTR/reference variants can no longer duplicate applicant scholarship disbursements, and stale awards cannot be disbursed after enrollment completion.
- Latest Stage 2 Exam Cell hall-ticket publication-boundary pass: Exam Cell hall-ticket lists and direct hall-ticket PDF downloads now close once an exam result is published, matching student admit-card behavior. Stale approved registrations can no longer produce fresh hall tickets after official result publication.
- Latest Stage 2 teacher result-roster canonical enrollment pass: teacher exam result entry now builds its roster from both legacy semester enrollments and canonical term-wise `student_subject_enrollments`, while preserving program/subject/term scope and published-timetable assignment checks. Teachers can record marks for properly allocated term-course students, and direct result POSTs for wrong-term canonical students remain blocked.
- Latest Stage 2 PMC curriculum write-scope integrity pass: legacy Program Chair/PMC curriculum subject and faculty-assignment write routes now enforce the actor's assigned program scope, require active subjects to belong to the selected program, and allow faculty assignment only for active curriculum subjects in the selected program/term. Direct POST/DELETE routes can no longer mutate another program's curriculum or assign faculty to unmapped/inactive subjects.
- Latest Stage 2 PMC elective-window state/scope integrity pass: legacy Program Chair/PMC elective-window creation and status routes now enforce assigned program scope, finalized windows cannot be reopened or downgraded through direct POSTs, and closed windows with submitted/allocated/waitlisted choices cannot be reopened. PMC must create a new revision/add-drop window instead of rewriting finalized choice history.
- Latest Stage 2 PMC timetable change-decision finality pass: PMC timetable change requests can now be decided only while still requested/pending/open/revision-requested. Approved, rejected, published, frozen, or otherwise reviewed timetable change decisions are locked against direct PATCH rewrites; further changes must start a new timetable change request.
- Latest Stage 2 fee-demand penalty lifecycle pass: the standard academic fee-demand penalty action now applies only to active students with open overdue balances, includes overdue partial-payment balances, skips zero-balance/paid/inactive/already-penalized records, and moves penalized demands into the `overdue` state so accounts, parent, student, NOC, and API surfaces read a consistent overdue ledger.
- Latest Stage 2 admission fee-installment program/batch contract pass: legacy admin admission configuration and dedicated Admission fee-installment routes now require selected batches to belong to the configured program and reject duplicate installment numbers within the same program/batch scope. Direct POST/PUT routes can no longer attach admission fee milestones to another program's batch or create ambiguous duplicate payment milestones that distort applicant readiness and payment verification totals.
- Latest Stage 2 application-window intake contract pass: Admission application-window create/update routes now require selected batches to belong to the selected program, and public application listing/show/register flows ignore inactive-program windows and stale windows whose batch belongs to another program. Direct/stale data can no longer create applicants under one program while assigning them to another program's batch.
- Latest Stage 2 public registration window-selection pass: public apply listing, show, and registration POST now share the same available-window contract, including active program, valid batch ownership, open date range, and remaining capacity. Registration no longer picks an older full window when a later valid window has capacity, so applicants are assigned to the same intake that the public form displays.
- Latest Stage 2 lead-conversion intake contract pass: Admission lead conversion now requires an active target program and a target batch that belongs to that program with an upcoming/active status. Direct lead-conversion POSTs can no longer create draft applicants in inactive programs, closed batches, or batches from another program.
- Latest Stage 2 legacy fee-collection balance pass: manual admin fee collection now enforces remaining fee-structure balance when no active fee-demand ledger exists. Prior paid receipts against the selected fee structure are counted before accepting another manual payment, preventing legacy fee collection from over-collecting beyond the configured structure amount while preserving demand-ledger behavior.
- Latest Stage 2 academic term lifecycle integrity pass: admin term creation now requires an active program, an upcoming/active batch, a unique term number within the batch, and a term number within the program configured total terms. Direct current-term switching now rejects terms under inactive programs or closed/completed batches, protecting downstream course allocation, timetable, exams, fees, and promotions from stale term pointers.
- Latest Stage 2 student dashboard attendance-visibility pass: the student dashboard attendance percentage and low-attendance alerts now use only active enrolled subjects with published timetable entries and published timetable versions. Draft timetable rows, draft-version rows, and out-of-scope timetable history can no longer drive student-facing dashboard percentages or alerts.
- Latest Stage 2 student API current-fee fallback pass: `/api/v1/student/fees` now uses current academic-year fee structures for the no-demand fallback and counts paid receipts against those structures for headline `total_paid`/`balance`, while preserving full paid receipt history in the payment list. Old academic-year fee structures and receipts can no longer inflate the current mobile/API due or balance before demand-ledger generation.
- Latest Stage 2 student API attendance-enrollment visibility pass: `/api/v1/student/attendance` now requires the attendance timetable entry to be published, in a published version, in the student's current program/batch/term scope, and tied to an actively enrolled subject. Published timetable rows for subjects the student is not currently allocated to no longer appear in mobile/API attendance history or percentages.
- Latest Stage 2 shared attendance-calculation integrity pass: `Student::calculateAttendancePercentage()` now uses the same published/enrolled/current-scope attendance boundary as the hardened student dashboard/API. Term promotion generation and PMC student-success risk refreshes can no longer be driven by draft timetable rows, draft timetable versions, or attendance for subjects the student is not currently allocated to.
- Latest Stage 2 PMC student-success result-visibility pass: PMC v0.057 student-success signal refresh now calculates average marks and exam/internal assessment absences only from published official exam results. Draft/unpublished failed or absent marks can no longer create retention-risk reasons, parent escalation, or intervention pressure before Exam Cell publication.
- Latest Stage 2 CoE official-result metric pass: CoE operating metrics now count published exams and transcript result records using the authoritative `exams.published_at` boundary instead of result remarks or raw result rows. Draft/unpublished result entries can still support marks-entry operations but no longer inflate official publication/transcript readiness metrics.
- Latest Stage 2 academic weak-performance signal pass: Dean attention queues, Dean program-risk scoring, PMC student monitoring, and Program Leadership student-success queues now use only published official exam results for weak-performance signals. Draft/unpublished low marks can no longer create Dean/PMC/program risk pressure before Exam Cell publication, while published failing results still surface normally.
- Latest Stage 2 academic result-publication queue pass: the Academics command-center `result_publish_pending` queue now includes only completed exams with entered marks that are still unpublished. Already published exams no longer remain in publish-review queues after official result publication, while unpublished marked exams still surface for CoE follow-up.
- Latest Stage 2 academic marks-entry queue pass: the Academics command-center `exam_marks_pending` queue now includes only completed unpublished exams without result rows. Already published exams are treated as closed for marks-entry queues even if stale data has missing result rows, while unpublished completed exams still surface for CoE marks-entry follow-up.
- Latest Stage 2 academic approval attention-scope pass: the Academics command-center pending/overdue approval queues now apply visible program scope for non-oversight users across Curriculum Change and Applicant approvals. Program Chair/PMC scoped command views no longer expose foreign-program approval queue rows, while Dean/Admin/all-scope users retain full approval visibility.
- Latest Stage 2 admission payment verification finality pass: staff verification of pending Admission payments now revalidates applicant and installment eligibility at verification time. Rejected, withdrawn, or enrolled applicants cannot have stale pending Admission payments verified through the standard queue, and inactive or program/batch-mismatched fee installments are blocked before committed payment state is written.
- Latest Stage 2 academic calendar form-access pass: academic calendar create/edit form routes now use the same academic-planning policy as store/update/delete. Roles such as accounts officer can still access permitted academic pages, but direct GETs can no longer expose operational calendar mutation forms unless the user has academic planning authority.
- Latest Stage 2 semester academic-year contract pass: admin semester create/update now require a unique semester number within the selected academic year and dates fully inside that academic year. Direct POST/PUT routes can no longer create duplicate semester numbers or out-of-year semester windows that would distort exam scheduling, timetable setup, reports, or student academic views.
- Latest Stage 2 batch academic-year contract pass: admin batch create/update now require active programs, validate optional academic-year linkage, keep batch dates inside the selected academic year, and lock academic-year/date reshaping once students, terms, admissions, timetable, or fee history depend on the batch. Direct POST/PUT routes can no longer create inactive-program batches or invalid batch windows that later distort intake, term, timetable, exam, fee, or admission workflows.
- Latest Stage 2 scoped role-assignment integrity pass: admin scoped-role assignment creation now requires active programs, rejects batch-only ambiguous scopes, and requires selected batches to belong to the selected program. Direct POST routes can no longer create misleading Program Chair/HOD/department role scopes that combine one program with another program's batch and confuse Academics/PMC visibility.
- Latest Stage 2 global user-role scope integrity pass: admin global user-role assignment now rejects program-scoped submissions because Spatie roles grant global authorization regardless of the stored `program_id`. The global role form now directs program-level academic access to Scoped Role Assignments, preventing direct POSTs from creating apparently scoped but actually global Dean/Exam/CMC/Admin-style access.
- Latest Stage 2 role-permission/feature-access configuration integrity pass: admin role-permission matrix updates now require active program scopes, and role feature-access updates reject unknown feature codes from direct POSTs. Security-admin users can no longer attach permissions to inactive programs or persist arbitrary backdoor-style feature keys outside the registered feature catalog.
- Latest Goal 1 security/access-control release-readiness pass: security-admin surfaces for role assignments, global user roles, permission matrix, feature-access matrix, system settings, org hierarchy, department-hierarchy admin aliases, and audit logs now have positive Director/Admin coverage, non-security denial coverage, invalid scope/key rejection coverage, and direct-route regression tests. `PROJECT_COMPLETION_BACKLOG.md` marks Security, Roles, Policies, Direct Route Access as `Release-ready`.
- Latest Goal 2 financial-integrity slice 1 pass: admin/accounts manual fee collection now subtracts pending student payment proofs from the active fee-demand balance before accepting a manual receipt. This closes the accounts-side over-collection gap where student proof submission reserved balance on the student portal but did not reduce manual collection availability until accounts verified or rejected the proof. `PROJECT_COMPLETION_BACKLOG.md` marks Fees, Payments, Refunds, Scholarships, NOC as `In progress` with the next slice focused on refunds, scholarship finality/reversal, and cross-surface balance agreement.
- Latest Goal 2 financial-integrity slice 2 pass: Admission refund processing now trims payout UTRs and rejects case-insensitive duplicates already linked to another refund. This closes a duplicate committed refund-reference path where a case/whitespace variant could be submitted after another refund had already been processed with the same UTR.
- Latest Goal 2 financial-integrity slice 3 pass: student web fee status now matches the `/api/v1/student/fees` no-current-academic-year fallback by loading applicable course fee structures when no academic year is marked current. The web page no longer shows zero/no fee structure while the API reports current fee dues from the same source records.
- Latest Goal 2 financial-integrity slice 4 pass: student scholarship and applicant scholarship disbursement now reject references that become blank after trimming. Spaces-only references can no longer move scholarship awards/applications into committed disbursed state without an auditable bank/reference value.
- Latest Goal 2 financial-integrity slice 5 pass: student non-cash academic fee-payment proofs now require a transaction reference at submission, and accounts verification revalidates the same rule for stale pending proofs. Online/NEFT/RTGS/DD-style proof rows can no longer be verified into paid receipts without an auditable transaction/reference value.
- Latest Goal 2 financial-integrity release-readiness pass: the remaining Admission refund correction, accounts outstanding/reporting, parent/student/API fee-balance, hostel-dues, and NOC-clearance surfaces were re-audited against current code and tests. Parent and API balances use active academic fee demands plus pending hostel dues, accounts outstanding/export uses active fee demands with export audit, and NOC approval/download rechecks open academic and hostel balances. `PROJECT_COMPLETION_BACKLOG.md` marks Fees, Payments, Refunds, Scholarships, NOC as `Release-ready`.
- Latest Goal 3 official academic-records release-readiness pass: Academic transcript PDF issuance now blocks fresh official transcript creation while any enrolled published-exam subject still has a pending/missing result row. Transcript preview can still show pending academic status, existing issued snapshots remain stable for re-download, and student transcript downloads continue to require an issued snapshot. `PROJECT_COMPLETION_BACKLOG.md` marks Official Academic Records as `Release-ready`.
- Latest Goal 4 Admission final-closure release-readiness pass: applicant assessment self-service now filters available reschedule slots to the applicant's own program/batch scope and the reschedule service rejects direct POSTs to open slots scoped to another program or batch. Existing final-state locks, own-assignment checks, payment/reference finality, offer/seat/handoff, and hierarchy-scope coverage remain intact. `PROJECT_COMPLETION_BACKLOG.md` marks Admission OS Final Closure as `Release-ready`.
- Latest Goal 5 PMC timetable/course-allocation release-readiness pass: PMC timetable publish now retires superseded operational `timetable_entries` by archiving and deactivating entries attached to older published versions before the newly published version becomes official. Dean/Admin rollback now refuses draft/non-published source versions, and PMC reconciliation repair/delivery-tracker checks only operate on currently published official timetable versions instead of archived draft history. This prevents stale archived timetable rows from remaining student/faculty visible through legacy operational timetable scopes. `PROJECT_COMPLETION_BACKLOG.md` marks PMC Timetable / Course Allocation as `Release-ready`.
- Latest Goal 6 Academics Dean OS release-readiness pass: Dean approval cockpit decisions are now final after approved/rejected/returned/escalated/cancelled states, with reasons required for rejected, returned, and escalated decisions. Dean planning cycles now enforce status transitions so published/closed/cancelled plans cannot be silently downgraded or revised in place, and approved meeting minutes cannot be re-approved to create duplicate follow-up actions. This hardens Dean planning, review, action, and approval governance while preserving existing Dean v0.07/v0.08 routes. `PROJECT_COMPLETION_BACKLOG.md` marks Academics Dean OS as `Release-ready`.
- Latest Goal 7 CoE / Exam OS release-readiness pass: CoE marks/results failed-result metrics and source queues now require the related exam to be published before counting below-pass marks. Draft/unpublished failed marks can still support internal marks-entry workflows, but they no longer inflate CoE official risk or appear in CoE failed-result source lists before result publication. Existing Exam Cell contracts for exam setup, marks entry, publication, hall tickets, appeals/anomalies, transcripts, and official reports remain intact. `PROJECT_COMPLETION_BACKLOG.md` marks CoE / Exam OS as `Release-ready`.
- Latest Goal 8 IQAC OS release-readiness pass: OBE/CO/PO/PSO/matrix/attainment/survey write routes now enforce academic program scope through `AcademicAccessPolicyService::canManageScope`, validate subject-program and term-program consistency, and reject cross-program CO-PO/PSO matrix targets. IQAC managers can no longer mutate unassigned programs through direct OBE URLs while Dean/Admin/all-scope users retain oversight. Existing OBE evidence/history locks, source-backed IQAC operating dashboards, feedback-quality lists, and program-scoped IQAC visibility remain intact. `PROJECT_COMPLETION_BACKLOG.md` marks IQAC OS as `Release-ready`.
- Latest Goal 9 Program Leadership OS release-readiness pass: Program Leadership portfolio and quality-signal program queries now apply scoped visibility against `programs.id`, and program visibility is derived from assigned program, batch, term, and subject scopes. Course coordinators and semester coordinators no longer get empty/misleading Program Leadership views when their operating assignment is term- or subject-scoped, while hidden programs remain excluded. Existing student-success and course-delivery publication boundaries continue to prevent draft timetable attendance and unpublished exam results from creating Program Leadership risk signals. `PROJECT_COMPLETION_BACKLOG.md` marks Program Leadership OS as `Release-ready`.
- Latest Goal 10 Course Delivery OS release-readiness pass: Course Delivery student visibility now composes exact student IDs from broad academic scopes, active assigned-subject enrollments, and own mentees instead of using a broad `program_id OR subject enrollment` query. Subject-assigned teachers/course-delivery users can no longer see same-program attendance-intervention students who are not enrolled in their assigned subject, while Dean/department/program oversight and mentor visibility remain intact. Existing draft timetable, draft timetable-version, and unpublished-session boundaries continue to protect Course Delivery dashboards and source lists. `PROJECT_COMPLETION_BACKLOG.md` marks Course Delivery OS as `Release-ready`.
- Latest Goal 11 Student Portal release-readiness pass: Student Course Hub faculty-name cards now use only active published timetable entries, and any linked timetable version must also be published. Draft timetable staffing and published rows attached to draft timetable versions no longer leak faculty assignment information to student-facing course cards. Existing student dashboard, attendance, timetable, results, course content, assignments/quizzes, discussions, feedback, documents/NOC, fees, and archived-student write-lock guards remain intact. `PROJECT_COMPLETION_BACKLOG.md` marks Student Portal as `Release-ready`.
- Latest Goal 12 Teacher Portal release-readiness pass: Teacher assignment/material/announcement/feedback subject scopes now use only active published timetable entries whose linked timetable version is also published. Draft timetable subjects and published rows under draft versions no longer authorize teachers to publish student-facing learning content or view feedback. Existing teacher dashboard, attendance, exams/results, student list, grading roster, inactive-teacher write locks, leave, and mentor guards remain intact. `PROJECT_COMPLETION_BACKLOG.md` marks Teacher Portal as `Release-ready`.
- Latest Goal 13 Parent Portal release-readiness pass: Parent Portal routes for dashboard, children, attendance, results, fees, and notices were audited against current controller/view/test behavior. Parent-facing attendance remains limited to enrolled subjects on active published timetable entries with published linked versions, results remain limited to published exams for enrolled subjects, fees use active academic demands plus hostel dues and paid-only receipts, notices use the shared audience/date visibility scope, and focused coverage now proves direct attendance/results/fees URLs for unlinked students return forbidden. `PROJECT_COMPLETION_BACKLOG.md` marks Parent Portal as `Release-ready`.
- Latest Goal 14 Operations Modules release-readiness pass: Library, Hostel, Transport, CMC/Placement, Assets/Inventory, and Notifications/Notices were audited against current routes/controllers/tests. Asset inventory issuing now rejects duplicate issue references per stock item, matching the existing duplicate receive-reference protection so repeated direct POSTs cannot decrement stock twice for the same operational issue reference. Existing operations guards cover library borrower/reservation/fine finality, hostel allocation/fee/outpass/complaint lifecycle, transport active-student/capacity/end-date checks, CMC placement/career/internship lifecycle locks, asset custody/date/condition checks, and notification/notice audience/archive ownership. `PROJECT_COMPLETION_BACKLOG.md` marks all Goal 14 operations modules as `Release-ready`.
- Latest Goal 15 demo/browser/final-readiness pass: demo seeder wiring, `APP_DEBUG=false` behavior, long-sidebar scroll CSS, applicant mobile sidebar markup, seeded demo primary role pages, route smoke coverage, and localhost browser rendering were audited. The local SQLite database was brought current with pending additive migrations after browser verification exposed a missing `notifications.deleted_at` column in the running demo database; code already had the migration, so this was a local readiness drift. Applicant mobile sidebar now uses `sidebar-mobile`, avoiding accidental fixed desktop sidebar behavior on mobile. Focused tests prove seeded demo users can open primary portal pages without Laravel/Whoops traces, and browser smoke verified admin dashboard, Admission dashboard, Dean OS, PMC command, CoE, IQAC, Program Leadership, Course Delivery, Exam Cell, Accounts, CMC, Dean dashboard, and Program Chair dashboard render on `localhost:8001` without service errors/debug traces. Sidebar runtime CSS checks confirmed desktop and mobile menu bodies use `overflow-y: auto`. `PROJECT_COMPLETION_BACKLOG.md` now marks Demo Seed Data And Browser Readiness as `Release-ready`, and all 15 bounded completion goals are currently release-ready.
- Dean Academics, Academic Department Owner, Director, and Admin retain full PMC oversight; PMC Head/Manager/Officer operate within PMC scope. Existing v0.02/v0.03/v0.04 PMC routes and legacy `chair.*` routes remain stable.
- Current verification baseline: Goal 15 focused final-readiness regression (`ProductionReadinessTest|LayoutProfileNavigationTest|ErrorPageTest|LaunchRouteSmokeTest`) passed 37 tests / 115 assertions; expanded seeded/layout/route smoke (`ProductionReadinessTest|DemoCredentialsTest|LayoutProfileNavigationTest|ErrorPageTest|LaunchRouteSmokeTest|RoleDashboardTest`) passed 68 tests / 204 assertions; full `php artisan test` passed 1345 tests / 8536 assertions. Browser smoke also passed on `localhost:8001` for the primary admin-accessible role/module pages listed above. The previous grievance route list and frontend build baselines remain valid unless those areas are touched again. Historical focused gates remain recorded above for prior hardening passes.

## Development Rules
- Keep schema additive and SQLite-test compatible.
- Keep all existing routes stable unless adding compatible links.
- Extend `AcademicsOperatingDemoSeeder` for every new Academics/PMC surface.
- Use compact, professional, data-dense UI.
- Growing tables should have pagination and export/current-view links.
- Dashboard metrics should link to filtered source lists or relevant workbench pages.
