# SchoolManagement Codex Project Context

This is the short active project memory. Detailed historical sprint notes are archived at `docs/archive/CODEX_PROJECT_CONTEXT_FULL_ARCHIVE_2026-06-20.md`.

## Working Rules

- Verify current code before relying on this file.
- Work one bounded goal at a time.
- Do not reopen the whole app unless the user explicitly asks for a global audit.
- Patch only verified critical/high gaps for the active goal.
- Use additive migrations only.
- Keep existing routes stable unless adding compatible redirects/links.
- Main pages must be database-backed or show useful operational empty states.
- Update demo seeders when new visible workflows need data.
- Update this file only after focused/adjacent verification passes.
- Run full `php artisan test` only at planned stage gates or after shared auth/layout/schema changes.

## Runtime

- App path: `C:\Users\mohd.naved\Documents\SchoolManagement\college-mgmt`.
- Local demo server: `http://localhost:8001`.
- PHP test runtime: `C:\tmp\php-8.5.7\php.exe`.
- PHPRC: `C:\tmp\php-8.5.7-codex-ini`.
- Test command:

```powershell
$env:PHPRC='C:\tmp\php-8.5.7-codex-ini'; C:\tmp\php-8.5.7\php.exe artisan test
```

### Local Browser Runbook

For browser testing in this Codex desktop workspace, the verified working local URL is:

```text
http://127.0.0.1:8000
```

Start the Laravel app from the `public` directory with Laravel's router script. Do not use `php -S 127.0.0.1:8000 -t public public\index.php`; that serves assets such as `/css/app.css` through the app and makes pages look broken/unstyled.

```powershell
cd C:\Users\mohd.naved\Documents\SchoolManagement\college-mgmt\public
$phpDir = Split-Path (Get-Command php).Source
php -d "extension_dir=$phpDir\ext" -d extension=mbstring -d extension=pdo_sqlite -d extension=sqlite3 -d extension=openssl -S 127.0.0.1:8000 -t . ..\vendor\laravel\framework\src\Illuminate\Foundation\resources\server.php
```

Use the same extension flags for local Artisan/Tinker commands when using the system `php`; plain `php artisan` may fail with missing `mb_strimwidth()` or SQLite driver errors.

```powershell
$phpDir = Split-Path (Get-Command php).Source
php -d "extension_dir=$phpDir\ext" -d extension=mbstring -d extension=pdo_sqlite -d extension=sqlite3 -d extension=openssl artisan <command>
```

PMC timetable local-data note:

- The correct official PMC master timetable route is `/academics/pmc/official-timetable`.
- If the page is styled but empty, verify canonical published rows before assuming a UI bug.
- The local ignored SQLite database once had `academic_pmc_timetable_generation_items` seeded as `official_status = draft` with no `timetable_version_id`, even though `timetable_versions.id = 1` was `published`.
- Repair used for local testing only: scope the seed run titled `PMC v0.041 Balanced Draft`, copy `timetable_version_id`, `program_id`, `batch_id`, `term_id`, and `subject_id` from the run/course group/version, mark only `scheduled`/`published`/`locked` items as `official_status = published`, and leave `unscheduled` diagnostics as `draft`.
- Expected local verification after repair: 3 official published canonical sessions, including 2 parallel sessions in the Tuesday `PMC v041 Period 1` slot.

## Release Control Files

Use these files instead of rereading long history:

- `PROJECT_COMPLETION_BACKLOG.md`: master release checklist by module/workflow family.
- `USER_ROLE_UX_AUDIT.md`: frontend/UX role-readiness checklist and fixed/future-polish status.
- `FRONTEND_COMPLETION_BACKLOG.md`: frontend safety and redesign readiness plan.
- `KPI_DRILLDOWN_AUDIT.md`: cross-department KPI/card-to-list consistency audit.
- `ADMISSION_KPI_DRILLDOWN_AUDIT.md`: Admission-specific KPI drilldown audit.
- `REAL_USER_COMPLETION_BACKLOG.md`: active post-release real-user journey backlog.
- `WEBSITE_UX_EXECUTION_PLAN.md`: whole-site workflow-first UX execution method and screen acceptance checklist.
- `WEBSITE_UX_INVENTORY.md`: whole-site role workflow inventory and immediate UX execution queue; use it when continuing frontend usability work without a specific role named.
- `WEBSITE_ROLE_UX_REDESIGN_MATRIX.md`: role-level usability redesign matrix for the next frontend phase; use it when pages are technically working but still hard for real users to operate.
- `RELEASE_CONTROL.md`: current execution protocol and next-goal template.

## Current Verified Baseline

- Latest full PHP suite: `1548 tests / 14152 assertions` passed.
- Latest frontend build: `npm run frontend:build` passed.
- Latest desktop frontend smoke: `npm run frontend:smoke` passed `127 tests / 3733 assertions`.
- Latest mobile frontend smoke: `npm run frontend:smoke:mobile` passed `29 tests / 1384 assertions`.
- Fast UX closure Batches 1-6 are marked fixed/verified in `USER_ROLE_UX_AUDIT.md` for sampled release-blocking scope.
- Next UX phase is role-level usability redesign, not another broad safety pass. `WEBSITE_ROLE_UX_REDESIGN_MATRIX.md` distinguishes "page opens/tests pass" from "real user can operate the role easily" and starts with Admission Applicant, then Admission Counsellor/Telecaller, then Admission Manager/Head. Documentation validation passed with `git diff --check`.
- Role-level UX redesign slice 1 started for Admission Applicant: applicant dashboard/checklist now show owner context (`Your action`, `Admission team`, `Complete`) for next action and readiness rows, explain the admission path order, and replace the batch dash fallback with `Batch not assigned yet`. Focused `AdmissionApplicantUxGuidanceTest` passed `8 tests / 105 assertions`; adjacent applicant/portal checks passed `22 tests / 828 assertions`; static scan on edited applicant dashboard/checklist views passed.
- Role-level UX redesign slice 2 started for Admission Counsellor/Telecaller: Counsellor Desk next-call/applicant/assessment rows now use readable missing-data labels and scoped no-work guidance; Calling Desk active-call and queue rows show readable phone/program context and each queue row links to the exact lead/applicant source record with `Open record`. Focused `AdmissionCounsellorTelecallerUxGuidanceTest` passed `6 tests / 83 assertions`; adjacent Admission role/frontend/v0.038 checks passed `35 tests / 657 assertions`; static scan on edited Counsellor/Calling Desk views passed.
- Role-level UX redesign slice 3 started for Admission Manager/Head: Command Center SLA Breaches now opens the exact SLA queue; Command Center call-pressure rows use readable phone/program fallbacks and no-calls guidance; Workbench KPI cards now link to workload, SLA, conversion-report, and reminder source pages. Focused `AdmissionSupervisorUxGuidanceTest` passed `7 tests / 120 assertions`; adjacent Admission head/manager/frontend/KPI/v0.039 checks passed `46 tests / 1055 assertions`; static scan on edited Command Center/Workbench views passed.
- Role-level UX redesign slice 4 started for Student: Student dashboard now labels each priority with `Owner` and `Source`, replaces weak dash fallbacks with readable labels (`Batch not assigned yet`, `Term not published yet`, `Mentor not assigned yet`, `Faculty not assigned`, `Room not assigned`), and links the no-classes state to subject registration. Focused `StudentDashboardGuidanceTest` passed `7 tests / 43 assertions`; `PortalDashboardUxGuidanceTest` passed `3 tests / 51 assertions`; adjacent portal/student checks passed `40 tests / 843 assertions`; static scan and `git diff --check` on edited Student files passed.
- Role-level UX redesign slice 5 started for Teacher: Teacher dashboard now labels teaching priority with `Owner` and `Source`, uses readable timetable missing-data labels (`Subject not assigned`, `Course not assigned`, `Room not assigned`), and fixes the blank-grid case so no real timetable entries show a useful `No published timetable for your profile yet` state with a profile link. Focused `TeacherDashboardGuidanceTest` passed `4 tests / 32 assertions`; adjacent teacher/portal checks passed `49 tests / 1025 assertions`; static scan and `git diff --check` on edited Teacher files passed.
- Role-level UX redesign slice 6 started for Parent: Parent dashboard now labels priority with `Owner` and `Source`, replaces weak `N/A` child summary labels with readable states (`No records`, `Not published`, `Program not assigned yet`, `Enrollment not issued yet`, `No due date published`), and explains no-linked-child setup through admin profile linking. Focused parent/portal checks passed `5 tests / 67 assertions`; adjacent portal checks passed `19 tests / 710 assertions`; static scan and `git diff --check` on edited Parent files passed.
- Role-level UX redesign slice 7 started for Dean Academics: Dean OS dashboard now labels today priority with `Owner` and `Source`, attention rows show owner/source/due context, program/action rows use readable fallbacks (`Program not assigned`, `Owner not assigned`) and title-cased statuses, and empty Dean attention/actions states explain next workflow. Focused Dean UX/frontend/KPI checks passed `13 tests / 230 assertions`; adjacent Dean v0.07/v0.08 checks passed `19 tests / 118 assertions`; static scan on the edited Dean dashboard passed.
- Role-level UX redesign slice 8 started for PMC: PMC Command now labels owner/source context for daily operations, attention rows show owner/source/due information, approval/attention empty states explain next operating paths, and PMC Timetable OS gives owner/source context plus non-dead-end guidance for missing generation runs, notifications, and constraints. Focused PMC UX/frontend/KPI checks passed `18 tests / 511 assertions`; adjacent PMC v0.04/v0.041 checks passed `8 tests / 200 assertions`; static scan and `git diff --check` on edited PMC files passed.
- Role-level UX redesign slice 9 started for CoE / Exam: CoE OS dashboard and source lists now label CoE owner/source context, source rows show owner/source badges, and empty filtered source lists explain no-blocker, missing source-record, published-result, eligibility, registration, and transcript-readiness boundaries. Focused CoE UX/frontend/KPI checks passed `9 tests / 288 assertions`; adjacent CoE v0.03/Exam Cell checks passed `26 tests / 272 assertions`; static scan and `git diff --check` on edited CoE files passed.
- Role-level UX redesign slice 10 started for IQAC: IQAC OS dashboard and source lists now label quality owner/source context, source rows show owner/source badges, and empty filtered lists explain no-gap, missing OBE/attainment/feedback/audit/corrective-action records, evidence availability, action status, and target/threshold boundaries. Focused IQAC UX/frontend/KPI checks passed `9 tests / 273 assertions`; adjacent IQAC v0.04 passed `6 tests / 32 assertions`; static scan and `git diff --check` on edited IQAC files passed.
- Role-level UX redesign slice 11 started for Program Leadership: Program Leadership OS dashboard and source lists now label assigned program owner/source context, source rows show owner/source badges, and empty filtered lists explain no-risk, missing portfolio/delivery/student/quality/escalation records, scope assignment, owner action, evidence, delivery progress, and escalation boundaries. Focused Program Leadership UX/frontend/KPI checks passed `8 tests / 177 assertions`; adjacent Program Leadership v0.05 passed `9 tests / 34 assertions`; static scan and `git diff --check` on edited Program Leadership files passed.
- Role-level UX redesign slice 12 started for Course Delivery: Course Delivery OS dashboard and source lists now label assigned faculty/mentor/course-coordinator owner context, source rows show owner/source badges, and empty filtered lists explain no-exception, missing timetable/attendance/engagement/feedback/mentor records, faculty assignment, published timetable, attendance evidence, material updates, feedback signals, and mentor follow-up boundaries. Focused `AcademicsCourseDeliveryUxGuidanceTest` passed `3 tests / 113 assertions`; adjacent Course Delivery frontend/KPI checks passed `5 tests / 74 assertions`; adjacent Course Delivery v0.06 passed `7 tests / 25 assertions`; static scan and `git diff --check` on edited Course Delivery files passed.
- Role-level UX redesign slice 13 started for Accounts: Accounts dashboard now labels finance owner/source context and source-links Admission collected and overdue demand cards; Fee Collections, Admission Payment Verification, Reconciliation, and Outstanding pages now explain workflow order, show visible filter/source context, expose owner/source row badges, replace mojibake currency/separators with `Rs.` text, and provide actionable no-match states. Focused `AccountsDashboardGuidanceTest` passed `13 tests / 122 assertions`; adjacent accounts frontend check passed `1 test / 39 assertions`; adjacent `FeePaymentTest` and `FeeDemandTest` passed `68 tests / 407 assertions`; static scan and `git diff --check` on edited Accounts files passed. Browser reached `/accounts/dashboard` as a non-Accounts session and showed a restricted page without debug text or console errors; positive Accounts rendering is covered by role-auth feature tests.
- Role-level UX redesign slice 14 started for Admin / Director: Admin dashboard now labels Admin/Director owner/source context, uses readable `Rs.` money labels and ASCII-safe chart/date labels, and fixes the Upcoming Exams subject fallback that was causing a dashboard 500. Focused `AdminOperationsFrontendBetaReadinessTest --filter=admin` passed `11 tests / 808 assertions`; adjacent `FrontendReadinessTest`, `AdminSystemConfigurationAccessControlTest`, `AdminRolePermissionAccessControlTest`, `AdminUserRoleIntegrityTest`, and `RoleRedirectTest` passed `79 tests / 1179 assertions`; browser verified `http://localhost:8001/admin/dashboard` with owner/source labels visible, no debug text, and zero console warnings/errors; static Admin dashboard scan and `git diff --check` passed.
- Role-level UX redesign slice 15 started for Operations / Notices: Admin Notice Board now explains the notice workflow, labels Admin/Director ownership and official notice source data, supports search/audience/status filters with visible result totals, uses readable audience/status/expiry labels, links view/edit/archive actions, and explains create/edit/detail visibility, student-email dispatch, and published-notice lock rules. Focused `NoticeVisibilityIntegrityTest` passed `10 tests / 82 assertions`; adjacent `AdminOperationsFrontendBetaReadinessTest --filter=admin` passed `11 tests / 808 assertions`; static notice-template scan passed. Browser reached `/admin/notices?search=no-matching-notice-title` as a non-admin session and showed the friendly restricted page without debug text or console errors; positive Admin rendering is covered by role-auth feature tests.
- Role-level UX redesign slice 16 started for Shared Notification Inbox: `/notifications` now filters account-owned messages by search/read status/type, preserves query pagination, shows owner/source context, matching totals, per-row source labels, filtered empty-state guidance, and detail-page source/read/action guidance. Focused `SharedNotificationInboxUxTest` plus `NotificationTest` passed `13 tests / 66 assertions`; adjacent `PortalFrontendBetaReadinessTest` passed `13 tests / 680 assertions`; browser verified `/notifications?search=no-matching-message` with the real inbox, owner label, filtered empty state, no debug text, and zero console warnings/errors; static notification-template scan and `git diff --check` passed.
- Whole-site UX inventory added: `WEBSITE_UX_INVENTORY.md` now provides the role workflow inventory, current release-safe/walkthrough-needed state, immediate UX execution queue, and per-slice evidence template. It is linked from `WEBSITE_UX_EXECUTION_PLAN.md` and this context file.
- Operations / Assets UX drilldown slice: Asset Register top KPI cards now link to matching asset register/status drilldowns or the consumable stock section, and the first viewport labels owner/source context for Admin/Director operations. Focused `AssetWorkflowTest` passed `22 tests / 152 assertions`; adjacent `AdminOperationsFrontendBetaReadinessTest` passed `11 tests / 808 assertions`; `git diff --check` passed for changed files. Browser check in the current PMC session confirmed `/admin/assets` shows a friendly restricted page without debug text or console warnings/errors; positive Admin rendering is covered by the feature test.
- Admission Head bounded role slice: `AdmissionHeadReadinessTest` passed `4 tests / 239 assertions`; adjacent Admission frontend/KPI/v0.039 checks passed `27 tests / 437 assertions`.
- Admission Manager/Counsellor Lead/Officer bounded role slice: `AdmissionManagerOfficerReadinessTest` passed `5 tests / 199 assertions`; adjacent Admission frontend/KPI/v0.039 checks passed `27 tests / 437 assertions`.
- Admission Counsellor/Telecaller bounded role slice: `AdmissionCounsellorTelecallerReadinessTest` passed `5 tests / 113 assertions`; adjacent Admission frontend/KPI/v0.038/v0.039 checks passed `34 tests / 513 assertions`.
- Admission Applicant Portal bounded role slice: applicant navigation now exposes owned Admission Operations, Offer Letters, and Notifications self-service routes; consent updates from applicant self-service are recorded with `applicant_portal` source. `AdmissionApplicantReadinessTest` passed `4 tests / 124 assertions`; adjacent portal/applicant/v0.039 checks passed `40 tests / 779 assertions`.
- UX v0.02 started role-by-role. Admission Applicant dashboard/checklist now use workflow-first guidance: next required action, blocker count, readiness progress, linked KPIs, quick links, and source-backed task table. `AdmissionApplicantUxGuidanceTest` passed `3 tests / 38 assertions`; adjacent applicant/frontend checks passed `26 tests / 850 assertions`; localhost browser check passed for `/applicant/dashboard` and `/applicant/checklist` with no console errors or debug text.
- UX v0.02 Admission Counsellor/Telecaller slice: Counsellor Desk now shows recommended daily workflow and queue guidance; Calling Desk now explains call sequence, disposition/outcome purpose, script coverage, next-action behavior, and no-call empty state. `AdmissionCounsellorTelecallerUxGuidanceTest` passed `2 tests / 31 assertions`; adjacent Admission counsellor/frontend/KPI/v0.038 checks passed `33 tests / 612 assertions`.
- UX v0.02 Admission Head/Manager supervisor slice: Command Center now explains supervisor control cycle; Manager Workspace explains team operating loop; Workbench explains queue triage order and source queue intent. `AdmissionSupervisorUxGuidanceTest` passed `3 tests / 45 assertions`; adjacent Admission head/manager/frontend/KPI/v0.039 checks passed `39 tests / 914 assertions`.
- UX v0.02 Admission assessment/offer-seat slice: Assessment Control Room explains assessment-day control sequence; Assessment Scheduling explains logistics workflow; Selection Committee explains decision sequence; Offer/Seat Control explains offer-to-enrollment control sequence. `AdmissionAssessmentOfferSeatUxGuidanceTest` passed `4 tests / 57 assertions`; adjacent Admission assessment/offer regressions passed `53 tests / 361 assertions`.
- UX v0.02 Admission communication/automation/report slice: Communication Hub explains safe-send sequence; Bulk Communication explains filter-preview-consent-send workflow; Communication Safety explains consent/template/audience/blocked/quiet-hour gates; Automations explain trigger-condition-preview-execution workflow; Reports explain funnel/source/program/compliance/counsellor interpretation flow. `AdmissionCommunicationAutomationReportsUxGuidanceTest` passed `4 tests / 58 assertions`; adjacent Admission frontend/KPI/v0.037/v0.038/v0.039 checks passed `43 tests / 593 assertions`.
- UX v0.02 Admission detail/timeline slice: Lead detail explains ownership, source/priority, contact/follow-up, quality-flag, and conversion order; Applicant detail explains action-center blockers, profile/document/payment/counselling/status review order; Conversation Timeline explains event reading order and empty-state next step. `AdmissionDetailTimelineUxGuidanceTest` passed `3 tests / 39 assertions`; adjacent Admission frontend/scoped-role/v0.036 checks passed `32 tests / 696 assertions`.
- UX v0.02 Dean Academics slice: Dean dashboard explains daily command sequence; planning explains annual/semester/readiness approval order; reviews explain review-to-action governance; approval cockpit explains evidence/decision/finality flow; risk explains snapshot/mitigation sequence; operating surfaces explain filter/sort/saved-view/export workflow; analytics explains report-pack sequence. `AcademicsDeanUxGuidanceTest` passed `3 tests / 54 assertions`; adjacent Dean frontend/KPI/v0.07/v0.08 checks passed `28 tests / 274 assertions`.
- UX v0.02 PMC slice: PMC Command explains daily operating sequence; v0.04 surfaces explain record workflow and readiness blocker handling; v0.041 timetable dashboard explains student basket -> group -> faculty -> locked slot -> generate/validate/approve/freeze order; v0.041 source surfaces explain filter/diagnose/update/recheck/export flow before generation or publish. `AcademicsPmcUxGuidanceTest` passed `4 tests / 109 assertions`; adjacent PMC frontend/KPI/v0.04/v0.041 checks passed `21 tests / 571 assertions`.
- UX v0.02 CoE / Exam slice: CoE dashboard explains exam readiness -> marks/results -> hall-ticket -> transcript -> appeals flow; CoE source lists explain filter, blocker review, source workflow, export, and official/published-boundary checks. `AcademicsCoeUxGuidanceTest` plus adjacent CoE/KPI checks passed `18 tests / 334 assertions`.
- UX v0.02 IQAC slice: IQAC dashboard explains OBE readiness -> attainment -> feedback -> audit evidence -> corrective action flow; IQAC source lists explain filter, quality-gap review, source workflow, evidence/action owner checks, and export. `AcademicsIqacUxGuidanceTest` plus adjacent IQAC/KPI checks passed `17 tests / 319 assertions`.
- UX v0.02 Program Leadership slice: Program Leadership dashboard explains portfolio -> delivery gaps -> student risk -> quality signals -> Chair escalation flow; Program Leadership source lists explain scoped signal-to-action workflow. `AcademicsProgramLeadershipUxGuidanceTest` plus adjacent Program Leadership/KPI checks passed `19 tests / 228 assertions`.
- UX v0.02 Course Delivery slice: Course Delivery dashboard explains course load -> today sessions -> attendance risk -> engagement/material gaps -> mentor actions; Course Delivery source lists explain signal-to-source-record, follow-up/material update, closure evidence, and export flow. `AcademicsCourseDeliveryUxGuidanceTest` plus adjacent Course Delivery/KPI checks passed `17 tests / 230 assertions`.
- UX v0.02 Student/Teacher/Parent portal slice: Student dashboard explains priority -> classes -> due work -> fees/blockers -> help workflow; Teacher dashboard explains timetable -> attendance -> grading -> materials -> mentoring workflow; Parent dashboard explains priority -> child alerts -> attendance/results/fees -> notices -> institute contact workflow. `PortalDashboardUxGuidanceTest` plus adjacent portal ownership/scope checks passed `70 tests / 1058 assertions`.
- UX v0.02 Admin/Accounts/CMC/Operations slice: Admin dashboard explains institute KPI -> attendance/fees -> quick action -> audit/security -> report flow; Accounts explains payment verification -> outstanding -> scholarships -> reconciliation -> reports; CMC explains active drives -> drive/event creation -> applications -> placement rate -> analytics; Library/Hostel/Transport/Assets entry pages explain their operating sequences. `AdminOperationsUxGuidanceTest` plus adjacent operations checks passed `131 tests / 1647 assertions`.
- UX v0.02 final frontend closure: Applicant registration-fee guidance is state-aware, so draft applicants can submit fee details while submitted applicants see status-tracking guidance instead of a dead-end CTA. Focused Admission regression passed `ApplicantRegistrationFeeTest` + `AdmissionOsV003Test` (`14 tests / 108 assertions`). Final gates passed on the same code baseline: `npm run frontend:build`, `npm run frontend:smoke` (`127 tests / 3733 assertions`), `npm run frontend:smoke:mobile` (`29 tests / 1384 assertions`), and full `php artisan test` (`1548 tests / 14152 assertions`).
- UX hardening follow-up: Admission dashboard Recent Interactions empty state now explains the first staff action and links to the applicant list instead of showing generic `No interactions logged yet.` text. Focused `AdmissionFrontendBetaReadinessTest` passed `18 tests / 358 assertions`; scan confirmed the old text is absent from Admission views.
- UX hardening follow-up: Teacher mentor detail empty states now explain first-message, first-review meeting, attendance-source, and published-result expectations instead of generic "no data" text. Focused `TeacherScopeWorkflowTest --filter=mentor_detail_empty_states` passed `1 test / 13 assertions`; adjacent `TeacherScopeWorkflowTest` + `PortalDashboardUxGuidanceTest` passed `31 tests / 260 assertions`.
- UX hardening follow-up: Student exam registration now explains that missing attendance records require Exam Cell eligibility verification instead of showing vague `No records yet` text. Focused `StudentExamRegistrationWorkflowTest` passed `9 tests / 47 assertions`; adjacent portal checks passed `19 tests / 721 assertions`.
- UX hardening follow-up: Student subject registration empty states now explain PMC term allocation, elective-choice next steps, and closed/unpublished elective windows instead of vague "no subjects" copy. Focused `StudentSubjectRegistrationWorkflowTest` passed `8 tests / 49 assertions`; adjacent student/portal checks passed `38 tests / 838 assertions`.
- UX hardening follow-up: Teacher assignment list and submission empty states now explain assignment creation, filter clearing, student follow-up, and grading next steps instead of vague "No assignments/submissions yet" copy. Focused `TeacherScopeWorkflowTest --filter=assignment_empty_states` passed `1 test / 10 assertions`; adjacent teacher/portal checks passed `44 tests / 923 assertions`.
- UX hardening follow-up: Student course hub empty states now explain that announcements, materials, and assignments appear only after faculty/program office publication instead of vague "No announcements/materials/assignments yet" copy. Focused `StudentCourseContentAccessTest --filter=course_hub_empty_states` passed `1 test / 10 assertions`; adjacent student/portal checks passed `39 tests / 848 assertions`.
- UX hardening follow-up: Teacher materials and announcements empty states now explain upload/post next steps and filter clearing instead of vague "No materials/announcements" copy. Focused `TeacherScopeWorkflowTest --filter=material_and_announcement_empty_states` passed `1 test / 10 assertions`; adjacent teacher/portal checks passed `45 tests / 933 assertions`.
- UX hardening follow-up: Student assignment list empty state now explains that assignments appear only after faculty publish them for enrolled subjects and suggests clearing dashboard filters. Focused `StudentCourseContentAccessTest --filter=assignment_list_empty_state` passed `1 test / 5 assertions`; adjacent student/portal checks passed `40 tests / 852 assertions`.
- UX hardening follow-up: Student course announcements/materials detail pages now explain that content appears after faculty/program office publication instead of vague "No announcements/materials yet" copy. Focused `StudentCourseContentAccessTest --filter=course_material_and_announcement_empty_pages` passed `1 test / 8 assertions`; adjacent student/portal checks passed `41 tests / 861 assertions`.
- UX hardening follow-up: Student discussion and mentor empty states now explain when to start a subject thread, how mentors are assigned, and when to message or request meetings. Focused discussion and mentor filters each passed `1 test / 11 assertions`; adjacent student/portal checks passed `43 tests / 880 assertions`.
- UX hardening follow-up: Student document-request and fee-payment-proof empty states now explain page purpose, post-submission tracking, and next action links. Focused `PortalFrontendBetaReadinessTest --filter=student_document_and_payment_empty_states_explain_next_actions` passed `1 test / 14 assertions`; adjacent `PortalFrontendBetaReadinessTest` + `PortalOwnershipBoundaryTest` + `StudentDocumentRequestGuidanceTest` passed `25 tests / 723 assertions`; browser check as `arjun.k@demo.edu` verified `/student/documents`, `/student/fee-payment`, and the Fee Status drilldown with no console errors or debug text.
- UX hardening follow-up: Student marks-appeal and attendance-condonation empty states now explain official-result/attendance eligibility, review ownership, and next action links. Focused appeal and condonation filters passed `2 tests / 16 assertions`; adjacent `StudentMarksAppealWorkflowTest` + `StudentTeacherAttendanceCanonicalWorkflowTest` + `PortalFrontendBetaReadinessTest` passed `31 tests / 804 assertions`; browser check as `arjun.k@demo.edu` verified `/student/appeals`, `/student/condonation`, and the unique `Check Eligible Subjects` CTA with no debug text.
- UX hardening follow-up: Student hostel outpass, hostel complaint, and transport empty states now explain request purpose, warden/transport-office ownership, and where status/history appears. Focused `HostelWorkflowGuidanceTest --filter=student_hostel_outpass_and_complaint_empty_states_explain_next_steps` passed `1 test / 12 assertions`; focused `TransportWorkflowTest --filter=student_transport_empty_states_explain_assignment_source_and_next_step` passed `1 test / 7 assertions`; adjacent `HostelWorkflowGuidanceTest` + `TransportWorkflowTest` + `PortalFrontendBetaReadinessTest` passed `57 tests / 979 assertions`; browser check as `arjun.k@demo.edu` verified `/student/transport`, `/student/hostel/outpass`, and `/student/hostel/complaints` with no debug text or console errors.
- UX workflow-discoverability follow-up: `WEBSITE_UX_EXECUTION_PLAN.md` now defines role journey evidence, screen acceptance checklist, issue triage labels, shared UI pattern targets, and verification evidence requirements. Admission Dashboard now opens with a linked daily operating order for call next, documents, payments, assessments, and seats; shared admin layout script section comments are ASCII-safe. Focused `AdmissionFrontendBetaReadinessTest` passed `22 tests / 427 assertions`; adjacent `AdmissionKpiDrilldownConsistencyTest` passed `2 tests / 47 assertions`; `git diff --check` passed for changed files.
- UX workflow-discoverability follow-up: Admission Offer/Seat Control empty states now explain the source workflow and next action links for offer rounds, waitlist, seat holds, joining-kit blockers, and deferrals; Admission Document Queue uses ASCII-safe applicant/program separators. Focused `AdmissionAssessmentOfferSeatUxGuidanceTest` passed `5 tests / 78 assertions`; adjacent `AdmissionFrontendBetaReadinessTest` passed `22 tests / 428 assertions`; browser rendered `/admission/offer-seat-control` with title `Offer And Seat Control`, guidance visible, no debug text, and no console errors.
- UX hardening follow-up: Student timetable empty state now explains subject-registration, PMC timetable publication, and academic-office section/group allocation paths, and timetable table fallback labels no longer render corrupted dash text. Focused `StudentTimetableWorkflowTest --filter=student_timetable_empty_state_explains_registration_and_publication_next_steps` passed `1 test / 6 assertions`; adjacent `StudentTimetableWorkflowTest` + `PortalDashboardUxGuidanceTest` + `PortalFrontendBetaReadinessTest` passed `19 tests / 734 assertions`; browser check as `arjun.k@demo.edu` verified `/student/timetable` with no debug text, console errors, or mojibake dash.
- UX hardening follow-up: Student leave empty state now explains planned/medical/urgent leave submission and program-office/academic-reviewer ownership, and pending leave rows show `No reviewer remarks yet` instead of corrupted dash text. Focused `StudentLeaveWorkflowTest --filter=student_leave_empty_state_and_pending_review_guidance_are_actionable` passed `1 test / 10 assertions`; adjacent `StudentLeaveWorkflowTest` + `PortalDashboardUxGuidanceTest` + `PortalFrontendBetaReadinessTest` passed `32 tests / 825 assertions`; browser check as `arjun.k@demo.edu` verified `/student/leave` with no debug text, console errors, or mojibake dash.
- UX hardening follow-up: Student library empty states now explain borrowed-book due dates, reservation rules, catalog setup ownership, borrowing history, and fine/NOC implications; student library currency and fallback labels no longer render corrupted characters. Focused `LibraryCirculationWorkflowTest --filter=student_library_empty_states_explain_borrowing_reservations_and_history` passed `1 test / 12 assertions`; adjacent `LibraryCirculationWorkflowTest` + `PortalFrontendBetaReadinessTest` passed `41 tests / 879 assertions`; browser check as `arjun.k@demo.edu` verified `/student/library` with no debug text, console errors, or mojibake text.
- UX hardening follow-up: Parent fee empty states now explain that accounts must raise fee demands before due/penalty/open-balance rows appear, and that only verified payments appear in paid receipt history. Focused `ParentPortalGuidanceTest --filter=parent_fee_page_empty_state_explains_accounts_demand_and_verified_receipts` passed `1 test / 9 assertions`; adjacent `ParentPortalGuidanceTest` + `PortalFrontendBetaReadinessTest` + `PortalOwnershipBoundaryTest` passed `27 tests / 751 assertions`; browser direct access from the current non-parent session showed the friendly 403 without debug text or console errors.
- UX hardening follow-up: Parent attendance/results empty states now explain that attendance appears only after published timetable rows and faculty marking, and results appear only after official exam publication; draft/out-of-scope academic data remains hidden. Focused `ParentPortalGuidanceTest --filter=parent_attendance_and_results_empty_states_explain_publication_boundaries` passed `1 test / 10 assertions`; adjacent `ParentPortalGuidanceTest` + `PortalFrontendBetaReadinessTest` + `PortalOwnershipBoundaryTest` passed `28 tests / 761 assertions`; browser direct access from the current non-parent session showed the friendly 403 without debug text or console errors.
- Real-user completion slice 1: Admission applicant-to-enrollment browser walkthrough verified applicant login and main pages: dashboard, checklist, application, documents, fees, status, admission operations, offer letters, and notifications. Mobile browser check found and fixed the top-card submitted-state CTA from generic `Continue` to `Track Status`; no debug traces, console errors, or horizontal mobile overflow remained. Focused applicant tests passed `16 tests / 207 assertions`; adjacent applicant/portal/Admission checks passed `28 tests / 725 assertions`. Next real-user slice is Admission staff daily conversion journey in `REAL_USER_COMPLETION_BACKLOG.md`.
- Real-user completion slice 2: Admission staff daily conversion browser walkthrough as Admission Manager verified Command Center, Manager Workspace, Workbench, Reminders, Document Queue, Leads, and Applicants. Browser found and fixed the Command Center workload KPI mismatch: lead workload now displays as `Lead Workload` and links to `/admission/leads` instead of the applicant list. Browser verification passed with no debug text; focused staff tests passed `16 tests / 404 assertions`; adjacent Admission frontend/v0.039/head checks passed `29 tests / 626 assertions`. Next real-user slice is Admin setup journey in `REAL_USER_COMPLETION_BACKLOG.md`.
- Admission UX follow-up: Attention Queues now honor selected queue drilldowns from Workbench/Command cards, show displayed total plus active filters, hide unrelated queue sections, and provide queue-specific empty guidance. `AdmissionSupervisorUxGuidanceTest` passed `7 tests / 113 assertions`; adjacent `AdmissionFrontendBetaReadinessTest` passed `23 tests / 457 assertions`; browser verified `/admission/attention?queue=unassigned_hot_leads&priority=low&program_id=999999` with no debug text or console errors.
- Admission UX follow-up: Bulk Communication now gives staff no-preview guidance, audience filter summary, recipient program/status/email context, and Communication Safety links before compose/send. `AdmissionCommunicationAutomationReportsUxGuidanceTest` passed `5 tests / 117 assertions`; adjacent `AdmissionFrontendBetaReadinessTest` passed `23 tests / 457 assertions`; browser verified `/admission/bulk-communication?filter_status=submitted` with no debug text or console errors.
- Real-user completion slice 3: Admin setup journey now has a shared `Admin setup sequence` strip on dashboard and setup pages for academic year, departments, programs, batches, terms, users/roles, permissions, and settings. Browser session as a non-admin staff user confirmed admin setup URLs return the friendly 403 without debug text. Focused admin setup/security tests passed `20 tests / 394 assertions`; adjacent admin frontend/readiness tests passed `78 tests / 1851 assertions`. Next real-user slice is Student daily portal journey in `REAL_USER_COMPLETION_BACKLOG.md`.
- Real-user completion slice 4: Student daily portal journey now has clickable dashboard KPI source drilldowns: Attendance opens attendance details, SGPA/CGPA open results, and Fee Outstanding opens fee details. Focused student/portal tests passed `40 tests / 864 assertions`; adjacent student workflow checks passed `45 tests / 250 assertions`. Next real-user slice is Teacher daily teaching journey in `REAL_USER_COMPLETION_BACKLOG.md`.
- Real-user completion slice 5: Teacher daily teaching journey now has clickable dashboard KPI source drilldowns: Weekly Load and My Classes Today open the teacher timetable; Mark Attendance opens attendance marking as a full card action. Focused teacher/portal checks passed `59 tests / 1031 assertions`; adjacent teacher workflow checks passed `30 tests / 151 assertions`. Next real-user slice is Parent monitoring journey in `REAL_USER_COMPLETION_BACKLOG.md`.
- Real-user completion slice 6: Parent monitoring journey now has clickable child KPI source drilldowns: Attendance opens linked-child attendance, SGPA opens linked-child results, and Fee Balance opens linked-child fees. Focused parent/portal checks passed `38 tests / 838 assertions`; adjacent attendance/results/fee checks passed `80 tests / 497 assertions`. Next real-user slice is Dean academic governance journey in `REAL_USER_COMPLETION_BACKLOG.md`.
- Real-user completion slice 7: Dean academic governance journey now has a source-backed `Critical Attention` dashboard drilldown to `/academics/dean-os/attention/critical_attention`, matching the aggregate critical/high Dean attention count. Focused Dean checks passed `31 tests / 334 assertions`; adjacent Academics checks passed `17 tests / 85 assertions`. Next real-user slice is PMC timetable journey in `REAL_USER_COMPLETION_BACKLOG.md`.
- Real-user completion slice 8: PMC timetable journey now has a source-backed `Quality Score` dashboard drilldown to `/academics/pmc/timetable-quality`, so PMC can inspect quality/generator evidence before publish/freeze. Focused PMC checks passed `21 tests / 635 assertions`; adjacent PMC regressions passed `14 tests / 103 assertions`. Next real-user slice is CoE exam journey in `REAL_USER_COMPLETION_BACKLOG.md`.
- Real-user completion slice 9: CoE exam journey now preserves KPI queue context on source-list reset; `Reset queue` clears search/status while keeping the active metric filter. Focused CoE checks passed `18 tests / 338 assertions`; adjacent Academics checks passed `23 tests / 119 assertions`. Next real-user slice is IQAC quality journey in `REAL_USER_COMPLETION_BACKLOG.md`.
- Real-user completion slice 10: IQAC quality journey now preserves KPI queue context on source-list reset; `Reset queue` clears search/status while keeping the active metric filter. Focused IQAC checks passed `17 tests / 323 assertions`; adjacent Academics checks passed `23 tests / 119 assertions`. Next real-user slice is Accounts finance journey in `REAL_USER_COMPLETION_BACKLOG.md`.
- Real-user completion slice 11: Accounts finance journey now has source-linked primary dashboard KPIs for demand reports, paid collections, outstanding balances, and overdue-demand queues; overdue finance priority opens the exact overdue-demand drilldown. Focused Accounts UX checks passed `11 tests / 77 assertions`; adjacent finance integrity checks passed `116 tests / 662 assertions`. Browser reached `/accounts/dashboard` as a non-accounts session and confirmed friendly restricted behavior without debug text; seeded accounts login switch was blocked by the current browser session logout form. Next real-user slice is Operations journey in `REAL_USER_COMPLETION_BACKLOG.md`.
- Real-user completion slice 12: Operations/CMC journey now has source-linked CMC KPI cards for active drives, selected placements, and analytics. Added `status=active` for CMC drives so dashboard count, list, and export match upcoming plus ongoing drives. Focused CMC/operations KPI checks passed `12 tests / 116 assertions`; adjacent operations lifecycle/frontend checks passed `111 tests / 1657 assertions`.
- Real-user completion slice 13: Program Leadership Active Students KPI now opens the scoped `student-success?metric=active_students` source list. The active-student rows are only included for that filtered drilldown, so default student-success previews remain focused on risk/weak-performance queues and preserve published-only boundaries. Focused Program Leadership/KPI checks passed `15 tests / 173 assertions`.
- Real-user completion slice 14: Admin dashboard primary KPI cards now open matching admin source pages for students, teachers, departments, programs, attendance, fees, notices, and exams. Focused admin/KPI checks passed `7 tests / 284 assertions`; adjacent admin frontend/redirect checks passed `76 tests / 1839 assertions`. Browser reached `/admin/dashboard` as the current non-admin session and confirmed friendly restricted-page behavior with clean console; seeded admin browser switching was not available from that restricted page.
- Real-user completion slice 15: PMC Scoped Programs KPI now opens `academics.pmc.programs?metric=active_programs`, a scoped PMC source list backed by `AcademicPmcOperatingService::scopedPrograms()` instead of global Admin Programs. Focused PMC KPI/UX/timetable checks passed `13 tests / 303 assertions`.
- Whole-site UX execution follow-up: `WEBSITE_UX_EXECUTION_PLAN.md` now defines phased whole-site UX execution, per-screen redesign contracts, first-viewport rules, and intent-based navigation rules. Portal navigation was tightened: Teacher Leave is under Daily Work, Teacher Feedback under Reports, and Parent sidebar links to the first linked child's Attendance, Results, and Fees pages. Focused checks passed: `PortalFrontendBetaReadinessTest` `13 / 676`; `FrontendReadinessTest` `11 / 947`.
- Admission navigation follow-up: Admission sidebar now separates communication work, finance/refunds, reports, and integration governance instead of placing Bulk Communication, Consent/Safety, Refunds, Reports, and Integration Health under one Reports group. Focused checks passed: `AdmissionFrontendBetaReadinessTest` `23 / 470`; `FrontendReadinessTest` `11 / 950`. Browser rendered `/admission/dashboard` with the new groups and no console/debug errors.

## Release-Ready Module Status

Current release-control status is in `PROJECT_COMPLETION_BACKLOG.md`. As of the latest verified baseline, the bounded completion goals there are marked `Release-ready` unless a new regression/feature scope is opened.

Release-ready families include:

- Security, roles, policies, direct route access.
- Fees, payments, refunds, scholarships, NOC.
- Official academic records: exams, results, grade cards, transcripts, hall tickets.
- Admission OS final closure.
- PMC timetable/course allocation.
- Academics Dean OS.
- CoE / Exam OS.
- IQAC OS.
- Program Leadership OS.
- Course Delivery OS.
- Student Portal.
- Teacher Portal.
- Parent Portal.
- Admin master data / academic setup.
- Library.
- Hostel.
- Transport.
- CMC / Placement / Alumni.
- Assets / Inventory.
- Notifications / Notices / Bulk Mail.
- Demo seed data and browser readiness.

## Demo Users

Common demo users:

- Admin: `admin@college.com` / `password`; `admin@demo.edu` is also used in tests.
- Director: `director@college.com` / `password`.
- Dean Academics: `dean@college.com` / `password`.
- PMC Head / Program Chair: `chair@college.com` / `password`.
- PMC Manager: `pmc.manager@college.com` / `password`.
- CoE / Exam Cell: `exam@college.com` / `password`.
- IQAC Head: `iqac.head@college.com` / `password`.
- Program Leader / HOD: `hod@college.com` / `password`.
- Faculty mentor: `faculty.mentor@college.com` / `password`.
- Teacher: `anjali@demo.edu` / `password`.
- Student: `arjun.k@demo.edu` / `password`.
- Parent: `parent@demo.edu` / `password`.
- Applicant: `priya.sharma@applicant.demo` / `password`.
- Accounts: `accounts@college.com` / `password`.
- CMC: `cmc@college.com` / `password`.

## How To Start Future Work

For every new request:

1. Identify the active bounded goal and relevant release-control file.
2. Inspect current routes/controllers/models/views/tests for only that scope.
3. List implemented workflows and verified gaps.
4. Fix only critical/high gaps selected for the slice.
5. Add or update focused tests.
6. Run focused tests, then adjacent regression.
7. Run full suite only at planned stage gate.
8. Update `PROJECT_COMPLETION_BACKLOG.md` or the relevant audit file.
9. Update this context only after tests pass.
10. Stop and report changed files, tests, and remaining gaps.

## Future-Polish Items, Not Current Blockers

- Latest focused frontend hardening: Dean dashboard dormant `Summary only` fallback removed, CMC event registrations show `Venue pending` instead of `TBD`, and `AcademicsDeanUxGuidanceTest` + `CmcDashboardGuidanceTest` passed `13 tests / 159 assertions`.
- Latest focused Accounts UX hardening: Accounts report empty tables now explain required program/batch/student/fee-demand setup instead of generic `No data.` text, and `AccountsDashboardGuidanceTest` passed `12 tests / 85 assertions`.
- Latest focused Teacher portal UX hardening: Teacher `My Students` now shows `No attendance marked yet` instead of generic `No records` for roster students without attendance, and `TeacherStudentListTest` passed `3 tests / 17 assertions`.
- Latest focused legacy Dean UX hardening: `dean.academics` top-performer empty state now explains that published official result data is required, and `DeanDashboardGuidanceTest` passed `6 tests / 32 assertions`.
- Latest focused Admission leads hardening: Leads index/filter pages and Lead Analytics no longer allow hidden 500s in tests; lead filters show matching records/counts, analytics uses portable converted-lead SQL and explains missing source/program/trend data; `LeadTest` passed `14 tests / 73 assertions`.
- Latest focused Admission application-window hardening: Application Windows no longer accepts hidden 500s in tests and now verifies visible operating content; `ApplicationWindowTest` passed `21 tests / 119 assertions`.
- Latest focused Admission offer-letter hardening: Offer Letter staff/applicant pages and PDF endpoints no longer accept hidden 500s in tests; `OfferLetterTest` passed `20 tests / 84 assertions`, adjacent `AdmissionFrontendBetaReadinessTest` passed `17 tests / 352 assertions`, and a test scan found no remaining `[200, 500]` tolerant feature assertions.
- Latest focused Admission receipt-branding hardening: Admission fee receipt PDF footer now uses configured institution profile details instead of visible placeholder address text; `AdmissionFeeReceiptBrandingTest` passed `2 tests / 8 assertions`, and the adjacent receipt route check in `AdmissionFlowTest` passed `1 test / 3 assertions`.
- Latest focused Admission applicant-detail hardening: empty profile, document, counselling, and team-note sections now explain the missing data and next staff action instead of generic empty-state text; `AdmissionDetailTimelineUxGuidanceTest` passed `4 tests / 60 assertions`, and adjacent `AdmissionFrontendBetaReadinessTest` passed `17 tests / 352 assertions`.
- Latest focused Teacher feedback hardening: Teacher course feedback empty states now explain published timetable assignment, teacher-profile linkage, student submission timing, and anonymous aggregate boundaries; focused teacher feedback checks passed `3 tests / 21 assertions`, adjacent teacher/portal checks passed `52 tests / 957 assertions`, and browser verified teacher dashboard -> navigation -> My Feedback with clean console.
- Latest focused Teacher leave hardening: Teacher leave page now explains leave purpose, academic-admin review, pending cancellation, active-profile eligibility, and reviewer remarks state; focused teacher leave check passed `1 test / 13 assertions`, adjacent leave/profile/portal checks passed `33 tests / 819 assertions`, and browser verified teacher dashboard -> Leave with clean console.
- Latest focused Student admit-card hardening: Admit Cards now explain CoE scheduling, approved exam registration, attendance/fee clearance, unpublished exam boundary, and Exam Cell follow-up; focused admit-card checks passed `2 tests / 16 assertions`, adjacent student exam/portal checks passed `28 tests / 753 assertions`, and browser direct non-student access showed friendly 403 without debug text.
- Latest focused Student admit-card PDF hardening: official Admit Card PDF output now uses ASCII-safe separators and readable missing-data labels for student identity, program/batch, academic year, subject, exam date/time, venue, and marks instead of mojibake/dash placeholders or `TBA`. Focused PDF label coverage passed `1 test / 16 assertions`; full `StudentAdmitCardWorkflowTest` passed `7 tests / 55 assertions`; adjacent student portal frontend checks passed `3 tests / 72 assertions`; static scan found no `N/A`, mojibake markers, dash/bullet entities, em dash, or `TBA` in the admit-card PDF template.
- Latest focused Grade Card PDF hardening: official Grade Card / Mark Sheet output now uses ASCII-safe separators and readable missing-data labels for student identity, enrollment, program/course, semester, subject, marks, percentage, and grade instead of mojibake/dash placeholders. Focused grade-card render coverage passed `1 test / 13 assertions`; full `StudentResultsWorkflowTest` passed `5 tests / 45 assertions`; adjacent `AdminOfficialReportAccessControlTest` passed `3 tests / 18 assertions`; static scan found no `N/A`, mojibake markers, dash/bullet entities, or em dash in the grade-card template.
- Latest focused Admin consolidated academic report PDF hardening: official Consolidated Academic Report output now uses ASCII-safe separators and readable missing-data labels for student identity, enrollment, program/course, department, semester, subject, marks, percentage, grade, points, and status instead of mojibake/dash placeholders or `N/A`. Focused consolidated-report render coverage passed `1 test / 12 assertions`; full `AcademicPublishedResultsReportingIntegrityTest` passed `7 tests / 56 assertions`; adjacent `AdminOfficialReportAccessControlTest` passed `3 tests / 18 assertions`; static scan found no `N/A`, mojibake markers, dash/bullet entities, or em dash in the consolidated report template.
- Latest focused Academic fee-demand UX hardening: `/academic/fee-demands` list/detail pages now use `Rs.` financial labels, readable missing student/program/term/due-date/payment labels, and actionable empty demand/payment-history guidance instead of `N/A`, mojibake currency, or dash placeholders. Focused fee-demand fallback coverage passed `1 test / 20 assertions`; full `FeeDemandTest` passed `24 tests / 91 assertions`; adjacent `FeePaymentTest` passed `44 tests / 316 assertions`; static scan found no `N/A`, mojibake markers, dash/bullet entities, em dash, or `TBA` in the two fee-demand templates.
- Latest focused Academic term-promotion UX hardening: `/academic/term-promotions` list/show/edit pages now use readable progression labels and no-record guidance for term results, attendance, current-term assignment, and target-term setup instead of `N/A`, mojibake select labels, or dash placeholders. Focused term-promotion fallback coverage passed `1 test / 19 assertions`; full `TermPromotionTest` passed `17 tests / 63 assertions`; adjacent `StudentPromotionStatusWorkflowTest` passed `2 tests / 12 assertions`; static scan found no `N/A`, mojibake markers, dash/bullet entities, em dash, or `TBA` in the term-promotion templates.
- Latest focused Admin Library operations UX hardening: `/admin/library`, `/admin/library/issues`, and `/admin/library/memberships` now use readable circulation/membership labels for missing book, accession, borrower, member, email, and fine data plus actionable no-issue/no-membership guidance instead of `N/A`, mojibake currency, or bare dash placeholders. Focused library fallback coverage passed `1 test / 19 assertions`; full `LibraryCirculationWorkflowTest` passed `29 tests / 231 assertions`; adjacent `AdminOperationsFrontendBetaReadinessTest` passed `10 tests / 744 assertions`; static scan found no `N/A`, mojibake markers, dash/bullet entities, em dash, or `TBA` in the three library templates.
- Latest focused CMC placement-drive UX hardening: `/admin/placement-drives`, `/admin/placement-drives/{placementDrive}`, and edit title now use readable drive/application labels for missing package, dates, location, CGPA, vacancies, eligibility, student, course, and offered-package data plus ASCII-safe `Rs.` and title separators. Focused placement fallback coverage passed `1 test / 17 assertions`; full `PlacementLifecycleIntegrityTest` passed `19 tests / 140 assertions`; adjacent `AdminOperationsFrontendBetaReadinessTest` passed `10 tests / 744 assertions`; static scan found no `N/A`, mojibake markers, dash/bullet entities, em dash, `TBA`, or rupee symbol in placement-drive templates.
- Latest focused Admin Hostel operations UX hardening: `/admin/hostel/complaints`, `/admin/hostel/outpasses`, and `/admin/hostel/blocks/{block}/rooms` now use readable labels for missing student, block, room, assignee, return mark, and fee data plus actionable empty states for complaint submission, outpass filtering, and room setup. Focused hostel daily-page coverage passed `1 test / 25 assertions`; full `HostelWorkflowGuidanceTest` passed `28 tests / 230 assertions`; adjacent `AdminOperationsFrontendBetaReadinessTest` passed `10 tests / 744 assertions`; static scan found no `N/A`, mojibake markers, dash/bullet entities, em dash, `TBA`, rupee symbol, or old no-record text in the corrected hostel templates. Browser localhost verification was attempted through the in-app Browser but blocked by Browser URL policy, so no alternate browser workaround was used.
- Latest focused Admin Assets operations UX hardening: `/admin/assets` now explains empty register/filter, active custody, consumable-stock setup, and stock-movement audit states, and uses readable labels for missing category, location, custodian, movement reference, and issued-to data. Focused `AssetWorkflowTest` passed `21 tests / 139 assertions`; adjacent `AdminOperationsFrontendBetaReadinessTest` passed `10 tests / 744 assertions`; static assets-template scan and `git diff --check` passed.
- Latest focused Admin Transport operations UX hardening: `/admin/transport` now explains empty vehicle setup, filtered active-assignment no-match, and route-stop setup states, and uses readable labels for missing student, enrollment, route, and vehicle assignment data. Focused `TransportWorkflowTest` passed `18 tests / 119 assertions`; adjacent `AdminOperationsFrontendBetaReadinessTest` passed `10 tests / 744 assertions`; static transport-template scan and `git diff --check` passed. Browser opened the route in the current non-admin session and verified a friendly restricted page with no debug text or console errors; positive admin rendering is covered by PHPUnit.
- Latest focused Teacher timetable UX hardening: `/teacher/timetable` now explains that only published assigned timetable rows are visible, points teachers to profile linkage, subject allocation, and PMC/academic-office publication prerequisites, and uses readable subject/batch/room fallback labels. Focused `StudentTimetableWorkflowTest` passed `4 tests / 31 assertions`; adjacent `TeacherDashboardGuidanceTest` passed `4 tests / 20 assertions`; `TeacherProfileMissingGracefulTest` passed `3 tests / 30 assertions`; static teacher-template scan and `git diff --check` passed.
- Latest focused Teacher attendance UX hardening: `/teacher/attendance/mark` now explains the mark-attendance sequence, missing published-class prerequisites, and student roster allocation blockers, while replacing `N/A`/mojibake-prone schedule labels with readable text. Focused `AttendanceWorkflowTest` passed `4 tests / 13 assertions`; adjacent `TeacherProfileMissingGracefulTest` passed `3 tests / 30 assertions`; `PortalFrontendBetaReadinessTest` passed `13 tests / 667 assertions`; static attendance-template scan and `git diff --check` passed.
- Latest focused Teacher exams UX hardening: `/teacher/exams` and `/teacher/exams/{exam}/results` now explain exam setup, roster review, absent/marks entry, and Exam Cell publication-lock boundaries, while replacing dash/mojibake-prone subject/date/term/student labels with readable text. Focused `ExamResultTest` passed `7 tests / 23 assertions`; adjacent `TeacherScopeWorkflowTest` passed `30 tests / 231 assertions`; `PortalFrontendBetaReadinessTest` passed `13 tests / 667 assertions`; static exam-template scan and `git diff --check` passed.
- Latest focused Teacher assignments UX hardening: `/teacher/assignments`, `/teacher/assignments/create`, and `/teacher/assignments/{assignment}/submissions` now explain published-subject assignment creation, student publication, active-roster submission tracking, follow-up, and grading sequence, while replacing dash/mojibake-prone subject, due-date, student, marks, and feedback labels with readable text. Focused `TeacherScopeWorkflowTest` passed `30 tests / 239 assertions`; adjacent `PortalFrontendBetaReadinessTest` passed `13 tests / 667 assertions`; `TeacherProfileMissingGracefulTest` passed `3 tests / 30 assertions`; static assignment-template scan, Browser restricted-page check, and `git diff --check` passed.
- Latest focused Teacher materials UX hardening: `/teacher/materials` and `/teacher/materials/create` now explain published-subject material upload, file/link/description requirements, student visibility, and publish timing, while replacing dash/mojibake-prone subject and attachment-state labels with readable text. Focused `TeacherScopeWorkflowTest` passed `30 tests / 249 assertions`; adjacent `PortalFrontendBetaReadinessTest` passed `13 tests / 667 assertions`; `TeacherProfileMissingGracefulTest` passed `3 tests / 30 assertions`; static materials-template scan and `git diff --check` passed.
- Latest focused Teacher announcements UX hardening: `/teacher/announcements` now explains published-subject posting, student course-feed visibility, and pinned-notice intent, while replacing dash/mojibake-prone subject selector/list labels with readable text. Focused `TeacherScopeWorkflowTest` passed `30 tests / 256 assertions`; adjacent `PortalFrontendBetaReadinessTest` passed `13 tests / 667 assertions`; `TeacherProfileMissingGracefulTest` passed `3 tests / 30 assertions`; static announcement-template scan and `git diff --check` passed.
- Latest focused Teacher roster UX hardening: `/teacher/students` now explains the published-timetable roster source, adds visible roster search/clear/filter summary controls, clarifies no-semester and no-roster states, uses readable student/course/department/enrollment labels, and removes the admin-detail link from the teacher-facing roster. Focused `TeacherStudentListTest` passed `3 tests / 26 assertions`; adjacent `PortalFrontendBetaReadinessTest` passed `13 tests / 667 assertions`; adjacent `TeacherScopeWorkflowTest` passed `30 tests / 256 assertions`; static roster-template scan and `git diff --check` passed.
- Latest focused Teacher mentor UX hardening: `/teacher/mentor` and `/teacher/mentor/{student}` now explain the assigned-mentee workflow, message/meeting sequence, attendance and published-result review, and escalation path, while replacing `N/A` and dash/bullet placeholders with readable fallback labels. Focused `TeacherScopeWorkflowTest` passed `30 tests / 264 assertions`; adjacent `PortalFrontendBetaReadinessTest` passed `13 tests / 667 assertions`, `TeacherProfileMissingGracefulTest` passed `3 tests / 30 assertions`, and `StudentMentorWorkflowTest` passed `3 tests / 26 assertions`; static mentor-template scan and `git diff --check` passed.
- Latest focused Teacher Course Feedback UX hardening: `/teacher/feedback` now explains teaching-allocation source, anonymous aggregate review, delivery-improvement use, and escalation for recurring low feedback, while adding readable subject/code fallbacks and clearer comment labels. Focused `StudentCourseFeedbackWorkflowTest` passed `9 tests / 66 assertions`; adjacent `TeacherScopeWorkflowTest` passed `30 tests / 264 assertions`, `PortalFrontendBetaReadinessTest` passed `13 tests / 667 assertions`, and `TeacherProfileMissingGracefulTest` passed `3 tests / 30 assertions`; static feedback-template scan and `git diff --check` passed.
- Latest focused Teacher Leave Create UX hardening: `/teacher/leaves/create` now explains leave type selection, date-overlap blocking, review context, pending-cancellation rules, reviewed-history behavior, future-date/day-calculation behavior, and coverage notes before submission. Focused `StudentLeaveWorkflowTest` passed `17 tests / 131 assertions`; adjacent `PortalFrontendBetaReadinessTest` passed `13 tests / 667 assertions`, `TeacherProfileMissingGracefulTest` passed `3 tests / 30 assertions`, and `PortalDashboardUxGuidanceTest` passed `3 tests / 49 assertions`; static leave-template scan and `git diff --check` passed.
- Latest focused Teacher Profile UX hardening: `/teacher/profile` now explains teacher-editable versus admin-owned fields, uses readable missing-data labels, and shows inactive-profile update-lock guidance with disabled controls. Focused `InstitutionalProfileUpdateIntegrityTest` passed `3 tests / 35 assertions`; adjacent `TeacherProfileMissingGracefulTest` passed `3 tests / 30 assertions`, `LayoutProfileNavigationTest` passed `3 tests / 21 assertions`, and `PortalFrontendBetaReadinessTest` passed `13 tests / 667 assertions`; static profile-template scan and `git diff --check` passed.
- Latest focused Student promotion hardening: Promotion Status now explains Academics/PMC term-end review ownership, result/attendance/clearance prerequisites, academic-office follow-up, and pending review state; focused promotion checks passed `2 tests / 12 assertions`, adjacent student academic/portal checks passed `19 tests / 711 assertions`, and browser unauthenticated access redirected cleanly to login without debug text.
- Latest focused Student placement hardening: Placement empty states now explain CMC drive publication, eligibility/deadline confirmation, application tracking, and CMC outcome updates; focused placement checks passed `2 tests / 14 assertions`, adjacent student placement/career/portal checks passed `45 tests / 866 assertions`, and browser unauthenticated access redirected cleanly to login without debug text.
- Latest focused Student career-readiness hardening: Career Events now explains CMC publication timing, Resume Builder explains placement-ready sections, and visible project/experience/certification fields persist; focused career/resume checks passed `3 tests / 22 assertions`, adjacent student career/placement/portal checks passed `50 tests / 904 assertions`, and browser verified Career Events plus Resume Add Project with clean console.
- Latest focused Student scholarship hardening: Scholarships now shows application-tracking guidance before the first submission and explains office-published scheme availability plus eligibility-data prerequisites; focused scholarship check passed `1 test / 9 assertions`, adjacent student scholarship/career/portal checks passed `60 tests / 979 assertions`, and browser verified `/student/scholarships` with clean console.
- Latest focused Student career-network hardening: Internships and Alumni Network now explain CMC record creation/verification ownership, confirmed internship fields, verified alumni source, and filter/all-program next steps; focused internship/alumni checks passed `2 tests / 13 assertions`, adjacent internship/alumni/placement/portal checks passed `34 tests / 822 assertions`, and browser verified `/student/internships` plus `/student/alumni` with clean console.
- Latest focused Student communication-settings hardening: Notification Settings now uses the Bootstrap portal shell, explains email alerts versus the in-app inbox, links to Inbox and Notice Board, and the student sidebar label distinguishes settings from the notification bell; focused notification checks passed `2 tests / 19 assertions`, adjacent portal/notification checks passed `27 tests / 750 assertions`, and browser verified render plus save interaction on `/student/notifications` with clean console.
- Latest focused Applicant communication-settings hardening: Notification Settings now uses the Bootstrap applicant shell, explains email alerts versus official portal status visibility, links to Status Tracker and Checklist, and the applicant sidebar label distinguishes settings from notifications; focused applicant notification checks passed `2 tests / 19 assertions`, adjacent applicant/frontend checks passed `22 tests / 848 assertions`, and browser verified render plus save interaction on `/applicant/notifications` with clean console.
- Latest focused shared notification inbox hardening: shared `/notifications` inbox/detail now select role-specific layouts, portal layouts include CSRF meta for Mark All Read, and the inbox explains institute messages/action links; focused checks passed `3 tests / 30 assertions`, adjacent portal/notification checks passed `29 tests / 750 assertions`, and browser verified applicant inbox Mark All Read to `0 unread` with clean console.
- Latest focused Admission pipeline hardening: `/admission/pipeline` now explains empty stage meaning, Admission role scope, board limits, and source-list next steps; invalid `object_type` query values return 404 before creating bogus boards. Focused pipeline check passed `1 test / 8 assertions`, adjacent `AdmissionFrontendBetaReadinessTest` passed `19 tests / 366 assertions`, `AdmissionOsV003Test` passed `5 tests / 64 assertions`, and browser verified empty-stage guidance with clean console.
- Latest focused Academics command queue hardening: `/academics/attention/{queue}` now explains valid empty queue meaning and next steps, while invalid queue keys return 404 instead of rendering unknown queue pages. Focused queue check passed `1 test / 7 assertions`, `AcademicsOsV011Test` passed `12 tests / 50 assertions`, Dean frontend readiness passed `7 tests / 141 assertions`, and browser confirmed unauthorized access renders friendly 403/no debug text; positive Dean browser login was blocked by the known Browser virtual clipboard issue.
- Latest focused Dean attention hardening: `/academics/dean-os/attention/{queue}` now explains empty attention queue meaning and next steps instead of only `No records in this queue.` Focused Dean KPI checks passed `3 tests / 26 assertions`, adjacent Dean frontend/v0.07 checks passed `14 tests / 183 assertions`, and browser confirmed unauthenticated access redirects cleanly to login with no debug text; positive Dean browser login remains blocked by the Browser virtual clipboard issue.
- Latest focused PMC source-list hardening: shared PMC operating section pages now explain empty scope/filter results and next steps instead of only `No records match the current PMC scope.` Focused PMC empty-state check passed `1 test / 7 assertions`, PMC KPI/frontend checks passed `14 tests / 391 assertions`, PMC v0.04 regression passed `4 tests / 50 assertions`, and browser confirmed unauthenticated access redirects cleanly to login with no debug text.
- Latest focused Program Chair timetable hardening: legacy substitutions now explain when replacements/cancellations/reschedules appear and link back to the builder instead of only `No records yet.` Focused legacy timetable checks passed `13 tests / 48 assertions`, adjacent PMC frontend/timetable/KPI checks passed `18 tests / 541 assertions`, and browser confirmed unauthenticated access redirects cleanly to login with no debug text or console errors.
- Latest focused academic transcript hardening: filtered Transcript candidates now show visible filter context and explain active-student, program/batch, and published-result prerequisites instead of only `No students found.` Focused transcript workflow checks passed `5 tests / 30 assertions`, adjacent CoE frontend/UX/v0.03 checks passed `14 tests / 274 assertions`, and browser confirmed unauthenticated access redirects cleanly to login with no debug text or console errors.
- Latest focused academic transcript output hardening: Academic Transcript list/detail/PDF now use readable official-record fallbacks and explain transcript readiness through active enrollments, published exams, and recorded marks instead of dash/mojibake placeholders or a terse no-semester message. Focused transcript label coverage passed `1 test / 18 assertions`; full `AcademicTranscriptCanonicalWorkflowTest` passed `6 tests / 48 assertions`; adjacent `AcademicsCoeFrontendBetaReadinessTest` passed `5 tests / 157 assertions`; static scan found no `N/A`, mojibake markers, dash/bullet entities, em dash, or old no-semester text in the transcript templates.
- Latest focused applicant Admission Operations hardening: missing assessment slots and seat/waitlist records now explain staff scheduling, reschedule timing, selection/offer-round timing, and checklist/status/offer next links instead of terse placeholders. Focused applicant UX checks passed `4 tests / 51 assertions`, adjacent applicant/readiness/frontend/v0.039 checks passed `31 tests / 525 assertions`, and browser confirmed unauthenticated access redirects cleanly to login with no debug text or console errors.
- Latest focused applicant fee hardening: missing admission-fee installments now explain Admissions/Accounts milestone publication, admission stage/batch/fee-rule dependency, and link to Status, Registration Fee, and Checklist. Focused applicant action-entry checks passed `4 tests / 18 assertions`, adjacent applicant/readiness/frontend checks passed `21 tests / 842 assertions`, and browser confirmed unauthenticated access redirects cleanly to login with no debug text or console errors.
- Latest focused applicant offer hardening: empty Offer Letters now explains selection/waitlist movement, offer-round publication, seat-hold deadline creation, and fee/checklist readiness, with links to Status, Checklist, Admission Operations, and Fees. Focused applicant UX checks passed `5 tests / 65 assertions`, adjacent offer/applicant/frontend checks passed `47 tests / 593 assertions`, and browser confirmed unauthenticated access redirects cleanly to login with no debug text or console errors.
- Latest focused applicant offer-detail hardening: Offer Letter detail response/status labels now use plain text instead of symbol-prefixed labels, keeping accept/decline, warning, accepted, and declined states readable and ASCII-safe. Combined offer/applicant checks passed `53 tests / 672 assertions`, and browser confirmed unauthenticated access redirects cleanly to login with no debug text or console errors.
- Latest focused applicant documents hardening: Documents now explains when no active program document requirements are published instead of leaving a `0/0 Uploaded` blank area, and links to Checklist, Status, and Dashboard. Focused applicant UX checks passed `6 tests / 76 assertions`, adjacent document/applicant/frontend checks passed `26 tests / 855 assertions`, and browser confirmed unauthenticated access redirects cleanly to login with no debug text or console errors.
- Latest focused applicant application-form hardening: dynamic application section select fields now use readable ASCII-safe `Select an option` placeholder text instead of a symbol placeholder that could render as mojibake. Focused applicant UX checks passed `7 tests / 83 assertions`, adjacent applicant/readiness/status/frontend checks passed `21 tests / 809 assertions`, and browser confirmed unauthenticated access redirects cleanly to login with no debug text or console errors.
- Latest focused applicant separator hardening: Applicant Dashboard, Checklist, and Admission Operations now use ASCII hyphen separators instead of middle-dot separators that could render as `·`; Applicant UX tests now assert corrupted separator byte patterns stay absent. Focused applicant UX checks passed `7 tests / 90 assertions`, adjacent applicant/readiness/status/frontend checks passed `21 tests / 809 assertions`, and browser confirmed clean unauthenticated redirects with no debug text, mojibake, or console errors.
- Latest focused applicant fee due-date hardening: Applicant fee cards now show `Due date not published` instead of `Due: N/A` when a fee installment has no due date, and payment detail fallback says `Installment not linked`. Focused fee/action checks passed `5 tests / 24 assertions`, adjacent applicant/payment/frontend checks passed `35 tests / 903 assertions`, and browser confirmed clean unauthenticated fees redirect with no debug text, mojibake, or console errors.
- Latest focused Admission counsellor workspace hardening: Counsellor Workspace now explains empty assigned lead, applicant, reminder, and attention queues instead of rendering blank tables/lists, and uses `Program not assigned` / `Next action not set` fallbacks instead of `N/A`. Focused counsellor/telecaller UX checks passed `3 tests / 44 assertions`, adjacent Admission frontend/counsellor/v0.031 checks passed `27 tests / 521 assertions`, and browser confirmed clean unauthenticated counsellor-workspace redirect with no debug text, mojibake, or console errors.
- Latest focused Admission manager workspace hardening: Manager Workspace now explains empty team scope, clear unassigned/stale lead queues, missing reminder activity, and clear manager-review queues instead of rendering blank operational panels. Focused supervisor UX checks passed `5 tests / 67 assertions`, adjacent Admission frontend/manager/v0.031 checks passed `27 tests / 609 assertions`, and browser verified seeded manager login, Manager Workspace render, and Reminder Queue drilldown with no debug text or console errors.
- Latest focused Admission officer queue hardening: Document and Payment verification queues now explain scoped empty pending queues, provide Clear Filters/Open Applicants actions, and use `Rs.` plus meaningful missing-data labels instead of vague/corrupted fallbacks. Focused manager/officer readiness checks passed `6 tests / 219 assertions`, adjacent Admission frontend/KPI/v0.039 checks passed `29 tests / 448 assertions`, and browser verified both queue routes with no debug text, mojibake, or console errors.
- Latest focused Admission calling-desk hardening: Calling Desk now explains empty next-call queues and objection trends, with links to callback reminders and leads, instead of rendering a blank right-side table. Focused counsellor/telecaller UX checks passed `4 tests / 59 assertions`, adjacent Admission frontend/telecaller/v0.038 checks passed `31 tests / 550 assertions`, and browser verified Calling Desk plus Review Objections drilldown with no debug text, mojibake, or console errors.
- Latest focused Admission applicant list/detail hardening: staff applicant lists now explain empty scope/filter results with Clear Filters and Open Leads actions; applicant list/detail fallbacks use readable ASCII-safe labels instead of `N/A`, rupee/mojibake symbols, or blank follow-up states. Focused applicant list/detail plus Admission frontend checks passed `31 tests / 674 assertions`; browser verified `/admission/applicants` with scope guidance, no debug text, no `N/A`, no mojibake, and clean console.
- Latest focused Admission payment overview hardening: `/admission/payments/{program}` now uses one scoped payment query for list rows, summary cards, and installment breakdown, preventing lower-role staff totals from including records outside their Admission visibility. Payment overview/history/reject-modal views now use `Rs.` and meaningful missing-data labels instead of `N/A`/mojibake; empty payment lists explain role/program/filter prerequisites and link to Clear Filters, Applicants, and Pending Queue. Focused and adjacent Admission checks passed `46 tests / 739 assertions`; browser verified `/admission/payments/1`, filtered empty state, and Clear Filters interaction with clean console.
- Latest focused Admission refund workflow hardening: refund list/create/detail pages now use ASCII-safe `Rs.` amount labels and readable applicant/payment/bank fallbacks, while filtered empty refund lists explain role scope, status filters, refundable verified payments, and next actions. Focused refund plus Admission frontend checks passed `29 tests / 471 assertions`; browser verified `/admission/refunds?status=processed` and Clear Filters with clean console, no `N/A`, no mojibake, and no old one-line empty state.
- Latest focused Admission scholarship UX hardening: scholarship scheme list/create/edit and disbursement queue now use ASCII-safe `Rs.` amount labels, readable unlimited/missing-data fallbacks, scoped empty-state guidance, and next-step links to Applicants/Schemes. Focused scholarship workflow passed `16 tests / 139 assertions`; adjacent Admission frontend readiness passed `19 tests / 366 assertions`; browser verified `/admission/scholarship-disbursements?program_id=1` plus scoped Clear Filters with clean console, no `N/A`, no mojibake, and visible scoped queue guidance.
- Latest focused Admission waitlist hardening: Waitlist Management now uses readable program/batch filter context, ASCII-safe missing-seat placeholders, source-backed empty-state guidance, and links to Merit List and Seat Matrix. Focused waitlist integrity/UX passed `8 tests / 50 assertions`; adjacent Admission offer-seat UX plus frontend readiness passed `23 tests / 423 assertions`; browser verified `/admission/waitlist/1?batch_id=1` and Clear Filter with clean console, no `N/A`, no mojibake, and source-list guidance.
- Latest focused Admission dashboard hardening: Dashboard follow-up and recent-interaction panels now explain clear 7-day follow-up queue prerequisites, first-interaction setup, and Applicant source-list next steps while using readable applicant/application/staff/note fallbacks instead of `N/A`. Focused Admission frontend readiness passed `20 tests / 377 assertions`; Admission KPI drilldown consistency passed `2 tests / 47 assertions`; browser verified `/admission/dashboard` plus Open Applicants drilldown with clean console, no `N/A`, no mojibake, visible filter summary, and operational source-list guidance.
- Latest focused Admission session-detail hardening: Selection Session detail now uses ASCII-safe time separators and readable panel/evaluator/capacity/coordinator/candidate fallbacks, and empty sessions explain candidate-assignment prerequisites for call letters, attendance, scoring, and check-in with links to shortlisted applicants and Assessment Operations. Focused Admission frontend readiness passed `21 tests / 399 assertions`; adjacent assessment UX passed `4 tests / 57 assertions`; browser verified `/admission/sessions/3` plus Assessment Operations drilldown with clean console, no `N/A`, no mojibake, and no debug text.
- Latest focused Admission scoring hardening: Score Sheet and Applicant Scorecard now use ASCII-safe separators, readable venue/program/applicant/scorer fallbacks, and prerequisite guidance linking attendance, session scoring, and assessment operations. Focused scoring UX passed `1 test / 20 assertions`; adjacent Admission frontend readiness passed `22 tests / 419 assertions`; browser verified `/admission/sessions/3/scores` with clean console, no `N/A`, no mojibake, and no debug text. Direct browser scorecard access for the current scoped session returned the friendly 403, while positive scorecard rendering is covered by focused PHPUnit.
- Latest focused Admission bulk-communication hardening: Bulk Communication now uses readable audience filter labels, ASCII-safe message and recipient fallbacks, and actionable no-match preview guidance with Clear Filters. Focused bulk communication UX passed `1 test / 35 assertions`; adjacent Admission communication/frontend checks passed `6 tests / 116 assertions`; browser verified base and no-match preview states with clean console, no `N/A`, no mojibake, and no debug text.
- Latest focused Admission staff registration-fee hardening: Staff-side Registration Fee recording now uses readable applicant/program/application fallbacks, `Rs.` financial labels, `Select Method`, and a staff recording sequence for identity, reference, proof, duplicate-reference protection, and applicant continuation. Focused staff fee UX passed `1 test / 14 assertions`; full `ApplicantRegistrationFeeTest` passed `10 tests / 59 assertions`; browser direct access for the current scoped session returned the friendly 403 without debug text, `N/A`, or mojibake.
- Latest focused Admission fee-installment hardening: Fee Installments setup now explains admission payment milestone configuration, applicant portal impact, and payment-history lock risk; the page uses `Rs.` amount labels and `Due date not published` instead of corrupted currency/dash placeholders. Focused fee-installment UX passed `1 test / 22 assertions`; adjacent fee-installment configuration subset passed `4 tests / 48 assertions`; browser verified `/admission/fee-installments/1` with clean console, no `N/A`, no mojibake, and no debug text.
- Latest focused Admission seat-matrix hardening: Seat Matrix setup now explains the offer/waitlist/enrollment seat-control sequence and commitment-lock risk before staff configure capacity. Focused seat-matrix UX passed `1 test / 20 assertions`; adjacent `AdmissionSeatMatrixIntegrityTest` passed `6 tests / 47 assertions`; browser verified `/admission/seat-matrices/1` with clean console, no `N/A`, no mojibake, and no debug text.
- Latest focused Admission merit-list hardening: Merit List setup and decision pages now explain the selection-to-offer workflow, regeneration lock risk, active filters, and no-match states; KPI cards link to filtered list views, fallbacks are readable, and the bulk offer generation form includes the required program parameter instead of rendering a hidden 500. Focused merit-list UX passed `1 test / 38 assertions`; adjacent `AdmissionMeritDecisionIntegrityTest` passed `7 tests / 70 assertions`; browser verified `/admission/merit-list/1` and `/admission/merit-list/1/show?decision=selected` with clean console, no `N/A`, no mojibake, and no debug text.
- Latest focused Admission enrollment hardening: Enrollment list/create/detail pages now explain selected-status, verified-payment, mandatory-document, roll-number, student-profile, and Academics-handoff requirements; empty lists direct staff to selected applicants and readable fallbacks replace corrupted separators. Focused enrollment flow passed `1 test / 67 assertions`; adjacent enrollment tests passed `8 tests / 115 assertions`; browser verified `/admission/enrollment` with clean console, no `N/A`, no mojibake, and no debug text.
- Latest focused Admission enrollment-letter hardening: official Enrollment Confirmation Letter output now uses ASCII-safe title/letterhead separators, `Rs.` payment labels, and readable missing-link fallbacks. Focused completed-letter check passed `1 test / 10 assertions`; adjacent enrollment tests passed `8 tests / 122 assertions`; static template scan found no `N/A`, mojibake, dash placeholder, or corrupted currency/bullet patterns.
- Latest focused Admission selection-process hardening: Selection Process and Scoring Parameters setup pages now explain assessment-step, scoring-parameter, session, score-entry, merit-list, rubric, and score-total readiness, with readable empty states and fallbacks. Focused UX check passed `1 test / 33 assertions`; adjacent selection/scoring checks passed `8 tests / 119 assertions`; browser verified steps/parameters pages with clean console, no `N/A`, no mojibake, and no debug text.
- Latest focused Admission email-notification hardening: applicant/staff Admission email subjects and templates now use ASCII-safe separators, `Rs.` amount labels, and readable pending/fallback labels across application, selection, session, payment, offer, enrollment, and follow-up notifications. Focused email render check passed `1 test / 120 assertions`; adjacent Admission communication/frontend checks passed `26 tests / 491 assertions`; static scan of Admission mail classes/templates is clean.
- Latest focused Admission reports hardening: Admission Reports now explains required source data for lead sources, programs, categories, counsellor assignments, geography, and missing seat matrix intake; reporting no longer displays a fake intake of `1` when seat matrix setup is absent. Focused report/communication UX passed `5 tests / 85 assertions`; adjacent reporting/frontend checks passed `24 tests / 433 assertions`; browser verified `/admission/reports` with clean console, no `N/A`, no mojibake, and no debug text.
- Latest focused Admission productivity UX hardening: Counsellor Performance, Objection Analytics, and Parent / Guardian Journeys now explain manager review, objection analysis, and parent decision-maker follow-up workflows; empty target, coaching, objection, and parent-journey states tell staff what source data is missing and where to act next. Focused empty-state coverage passed `1 test / 31 assertions`; full `AdmissionOsV037Test` passed `10 tests / 114 assertions`; adjacent Admission frontend/counsellor checks passed `26 tests / 478 assertions`; browser verified `/admission/objection-analytics` with clean console, visible guidance, no `N/A`, no mojibake, and no debug text.
- Latest focused Admission production-readiness UX hardening: Integrations now explains provider readiness, sandbox tests, webhook receipts, and retry queues; Assessment Normalization now explains chair review, outlier review, evaluator-score prerequisites, and links to Assessment Control Room when scores are absent. Focused empty-state coverage passed `1 test / 23 assertions`; full `AdmissionOsV037Test` passed `11 tests / 137 assertions`; adjacent Admission frontend/assessment checks passed `26 tests / 476 assertions`; browser verified `/admission/integrations` with clean console, visible guidance, no `N/A`, no mojibake, and no debug text.
- Latest focused Admission governance UX hardening: Script Compliance, Automation Simulation, Route Access Audit, and Accessibility Audit now explain review workflows, source-data prerequisites, and release-safety next steps; Script Compliance no longer shows mojibake separators. Focused governance coverage passed `1 test / 42 assertions`; full `AdmissionOsV037Test` passed `12 tests / 179 assertions`; adjacent Admission frontend/communication checks passed `27 tests / 504 assertions`; browser verified `/admission/script-compliance` with clean console, visible guidance, no `N/A`, no mojibake, and no debug text.
- Latest focused Admission saved-view UX hardening: Saved Views now explains the create/apply workflow, selected surface context, reusable filter controls, source work-surface links, and first-view empty state. Focused saved-view coverage passed `1 test / 27 assertions`; full `AdmissionOsV037Test` passed `12 tests / 193 assertions`; adjacent Admission frontend/supervisor checks passed `27 tests / 486 assertions`; browser DOM/console verified `/admission/saved-views?surface=counsellor_desk` with visible guidance, source link, clean console, no `N/A`, no mojibake, and no debug text.
- Latest focused Admission walk-in UX hardening: Walk-in Desk now explains the campus-visit-to-lead workflow, shows active filter/sort context, supports sortable visitor/visit/status headers, scopes conversion reporting to the same Admission visibility and current program/search filters, and shows useful no-match table/report states. Focused operational-table coverage passed `1 test / 29 assertions`; full `AdmissionOsV033Test` passed `2 tests / 36 assertions`; adjacent `AdmissionFrontendBetaReadinessTest` passed `22 tests / 419 assertions`; browser DOM/console verified `/admission/walk-ins?search=no-matching-walk-in-visitor&sort=visitor_name&direction=asc` with visible guidance, clean console, no `N/A`, no mojibake, and no debug text.
- Latest focused Admission application PDF hardening: official Application PDF output now uses readable missing-data labels, ASCII-safe header separators, and `Not provided` for empty dynamic fields instead of `N/A`, bullet/dash entities, or mojibake-prone placeholders. Focused template-render coverage passed `1 test / 16 assertions`; adjacent application PDF route checks passed `3 tests / 22 assertions`; static template scan found no `N/A`, mojibake markers, `&bull;`, `&mdash;`, or em dash placeholders.
- Latest focused Admission fee receipt PDF hardening: official Admission Fee Receipt output now reads actual `payment_mode` / `transaction_reference` fields, uses readable missing-data labels, and guards the `amountInWords()` helper against repeated-render redeclaration. Focused receipt-label coverage passed `1 test / 21 assertions`; full `AdmissionFeeReceiptBrandingTest` passed `3 tests / 29 assertions`; adjacent Admission receipt route check passed `1 test / 3 assertions`; static template scan found no `N/A`, mojibake markers, old bullet/dash placeholders, or legacy `payment_method` / `reference_number` references.
- Latest focused Admission merit output hardening: Merit List PDF and Category-Wise Report now use ASCII-safe titles and readable applicant/application/score/seat/fill-rate fallbacks instead of `N/A`, mojibake separators, or dash placeholders. Focused merit output coverage passed `1 test / 20 assertions`; full `AdmissionMeritDecisionIntegrityTest` passed `8 tests / 90 assertions`; adjacent `AdmissionAssessmentOfferSeatUxGuidanceTest` passed `4 tests / 57 assertions`; static template scan found no `N/A`, mojibake markers, `&bull;`, `&mdash;`, or em dash placeholders in the merit output templates.
- Latest focused Admission offer-letter staff UX hardening: Offer Letter staff list/detail pages now use ASCII-safe title text, explain no-offer states through merit-list/seat-readiness prerequisites, and show readable missing issuer/applicant/date labels on detail pages. Focused offer index/detail checks passed `2 tests / 25 assertions`; full `OfferLetterTest` passed `22 tests / 116 assertions`; static scan found no `N/A`, mojibake markers, old bullet/dash placeholders, or old `No offer letters found.` text in the offer index/detail views.
- Latest focused Admission call-letter PDF hardening: official assessment Call Letter output now uses ASCII-safe comments/separators and readable missing schedule labels for program, batch, time, venue, and reporting time. Focused call-letter scope/render checks passed `2 tests / 13 assertions`; full `AdmissionFlowTest` passed `22 tests / 180 assertions`; adjacent v0.031 call-letter regression passed `1 test / 4 assertions`; static scan found no `N/A`, mojibake markers, `&mdash;`, `&ndash;`, `&bull;`, em dash, or box-drawing placeholders in the call-letter template.
- Latest focused Admission lead-detail UX hardening: Lead Detail now uses ASCII-safe title, phone fallback, and counsellor selector text, and empty notes/follow-up/assignment/communication/call-history sections explain the next staff action. Focused `AdmissionDetailTimelineUxGuidanceTest` passed `5 tests / 87 assertions`; adjacent `AdmissionFrontendBetaReadinessTest` passed `22 tests / 428 assertions`; static template scan and `git diff --check` passed for the touched files.
- Latest focused Admission lead-queue UX hardening: Lead Queue now explains the processing order, uses readable missing phone/program labels, provides no-match next-step links to filters/walk-ins/partners, and fixes escaped shell title text. Focused `LeadTest` plus adjacent `AdmissionFrontendBetaReadinessTest` passed `38 tests / 530 assertions`; browser rendered `/admission/leads?search=no-matching-lead-token` with visible guidance, no debug text, and no console errors.
- Latest focused Admission follow-up calendar UX hardening: Follow-up Calendar now explains monthly callback review, uses readable lead/counsellor/notes fallbacks, and gives empty-month next steps to Leads, Calling Desk, and Reminder Queue. Focused `LeadTest` passed `18 tests / 131 assertions`; adjacent `AdmissionFrontendBetaReadinessTest` passed `22 tests / 428 assertions`; browser rendered `/admission/leads/follow-ups/calendar?month=2026-06` with clean console/no debug text.
- Latest focused Admission reminder queue UX hardening: Reminder Queue now explains filter/send/complete/pause workflow, shows active filters, links reminders to lead/applicant detail records, and gives no-match next steps. Focused `AdmissionFrontendBetaReadinessTest` passed `23 tests / 457 assertions`; adjacent counsellor/v0.033 checks passed `6 tests / 95 assertions`; browser rendered filtered queue with clean console/no debug text.
- Latest focused Admission communication hub UX hardening: Communication Hub now explains empty template/message-log prerequisites and links to Bulk Communication, Communication Safety, and Reminder Queue. Focused `AdmissionCommunicationAutomationReportsUxGuidanceTest` passed `5 tests / 98 assertions`; adjacent `AdmissionFrontendBetaReadinessTest` passed `23 tests / 457 assertions`; browser rendered `/admission/communication` with clean console/no debug text.
- Latest focused Admission workbench UX hardening: Workbench attention cards now link to filtered queue keys, and empty operational panels explain next source workflows for leads, enrollment, documents, payments, assessments, and offer risk. Focused `AdmissionSupervisorUxGuidanceTest` passed `6 tests / 95 assertions`; adjacent `AdmissionFrontendBetaReadinessTest` passed `23 tests / 457 assertions`; browser rendered filtered workbench with clean console/no debug text.
- Latest focused Admin navigation UX hardening: Admin sidebar now separates Teachers, Students, and Parents into a People group while preserving Student Documents, Applications, and Admissions under Students / Applicants. Focused `AdminOperationsFrontendBetaReadinessTest` passed `10 tests / 747 assertions`; shared `FrontendReadinessTest` passed `11 tests / 951 assertions`.
- Latest focused Director/HOD navigation UX hardening: Director Reports now sits under Reports, HOD Leave Approvals under Approvals, and HOD Dept Performance under Reports. Focused `AdminOperationsFrontendBetaReadinessTest` passed `10 tests / 757 assertions`; shared `FrontendReadinessTest` passed `11 tests / 951 assertions`; static manifest check found no empty groups.
- Latest focused Student navigation UX hardening: Academic Summary and Promotion Status now sit under Track, while Settings contains only My Profile and Notification Settings. Focused `PortalFrontendBetaReadinessTest` passed `13 tests / 680 assertions`; shared `FrontendReadinessTest` passed `11 tests / 952 assertions`; static manifest check found no empty groups.
- Latest focused Accounts navigation UX hardening: Accounts Reports now sits under Reports, while Finance keeps Fee Collections, Admission Payments, Outstanding, and Reconciliation. Focused `AdminOperationsFrontendBetaReadinessTest` passed `10 tests / 759 assertions`; shared `FrontendReadinessTest` passed `11 tests / 953 assertions`; static manifest check found no empty groups.
- Latest focused Admin finance/report navigation UX hardening: Admin Fee Report now sits under Reports, while Finance keeps fee setup/collection, accounts, reconciliation, refunds, and scholarship operations. Focused `AdminOperationsFrontendBetaReadinessTest` passed `10 tests / 764 assertions`; shared `FrontendReadinessTest` passed `11 tests / 953 assertions`; static manifest check found no empty groups.
- Latest focused Admin integration-readiness navigation UX hardening: Admin Integration Health now sits with Settings/security controls instead of Reports. Focused `AdminOperationsFrontendBetaReadinessTest` passed `10 tests / 766 assertions`; shared `FrontendReadinessTest` passed `11 tests / 953 assertions`; static manifest check found no empty groups.
- Latest focused CMC navigation UX hardening: Placement Stats now sits under Reports, while Placement keeps Placement Drives, Companies, and Career Events. Focused `AdminOperationsFrontendBetaReadinessTest` passed `10 tests / 768 assertions`; shared `FrontendReadinessTest` passed `11 tests / 953 assertions`; static manifest check found no empty groups.
- Latest focused Admin command/report navigation UX hardening: Admin Command now contains only Dashboard and Global Search, while Analytics and Institutional KPI sit under Reports. Focused `AdminOperationsFrontendBetaReadinessTest` passed `10 tests / 771 assertions`; shared `FrontendReadinessTest` passed `11 tests / 953 assertions`; static manifest check found no empty groups.
- Latest focused Admin placement-report navigation UX hardening: Admin Placement Stats now sits under Reports, while Placement keeps operational company/drive/internship/alumni links. Focused `AdminOperationsFrontendBetaReadinessTest` passed `10 tests / 774 assertions`; shared `FrontendReadinessTest` passed `11 tests / 953 assertions`; static manifest check found no empty groups.
- Latest focused Lead Analytics navigation UX hardening: Admin and Admission now place Lead Analytics under Reports instead of daily Lead operations. Focused `AdmissionFrontendBetaReadinessTest` passed `23 tests / 473 assertions`; `AdminOperationsFrontendBetaReadinessTest` passed `10 tests / 776 assertions`; shared `FrontendReadinessTest` passed `11 tests / 953 assertions`; static manifest check found no empty groups.
- Latest focused Leave Approvals navigation UX hardening: Admin and PMC now place Leave Approvals under Approvals instead of academic delivery/student operational groups. Focused `AdminOperationsFrontendBetaReadinessTest` passed `10 tests / 778 assertions`; `AcademicsPmcFrontendBetaReadinessTest` passed `8 tests / 343 assertions`; shared `FrontendReadinessTest` passed `11 tests / 953 assertions`; static manifest check found no empty groups.
- Latest focused Academics branch governance navigation UX hardening: PMC, CoE, and IQAC now place Academics Governance under Governance instead of daily Command entry points. Focused `AcademicsPmcFrontendBetaReadinessTest` passed `8 tests / 345 assertions`; `AcademicsCoeFrontendBetaReadinessTest` passed `5 tests / 159 assertions`; `AcademicsIqacFrontendBetaReadinessTest` passed `5 tests / 159 assertions`; shared `FrontendReadinessTest` passed `11 tests / 955 assertions`; static manifest check found no empty groups.
- Latest focused Admin academics-governance navigation UX hardening: Admin now places Academics Governance under Governance instead of Academics / Delivery. Focused `AdminOperationsFrontendBetaReadinessTest` passed `10 tests / 780 assertions`; shared `FrontendReadinessTest` passed `11 tests / 955 assertions`; static manifest check found no empty groups.
- Latest focused Dean approval-cockpit navigation UX hardening: Dean now places Approval Cockpit under Approvals instead of Governance. Focused `AcademicsDeanFrontendBetaReadinessTest` passed `7 tests / 143 assertions`; shared `FrontendReadinessTest` passed `11 tests / 955 assertions`; static manifest check found no empty groups.
- Latest focused Admin Grade Reports UX hardening: `/admin/results` now uses result-specific official-record empty-state guidance and readable fallbacks for unpublished marks, pending grades, and pending points instead of attendance wording, mojibake select labels, or corrupted dash placeholders. Focused `AdminOperationsFrontendBetaReadinessTest` passed `11 tests / 799 assertions`; shared `FrontendReadinessTest` passed `11 tests / 955 assertions`; adjacent `AdminExamAccessControlTest` passed `3 tests / 43 assertions`; static scan found no old attendance wording or mojibake markers in the template.
- Latest focused CoE Hall Ticket PDF UX hardening: `departmental.exam-cell.hall-ticket-pdf` now uses readable official-output labels and ASCII-safe separators for missing student, enrollment, program, exam, subject, date, time, venue, marks, and term data. Focused `ExamCellDashboardGuidanceTest` passed `19 tests / 238 assertions`; adjacent `AcademicsCoeFrontendBetaReadinessTest` passed `5 tests / 159 assertions`; static scan found no `N/A`, mojibake markers, HTML dash/bullet entities, or Unicode dash placeholders in the template.
- Latest focused Student Admit Cards UX hardening: `/student/admit-cards` now uses clear exam-readiness labels for missing time, venue, subject, and max marks instead of `TBA`, `General`, or `Not set`. Focused `StudentAdmitCardWorkflowTest` passed `7 tests / 58 assertions`; adjacent `PortalFrontendBetaReadinessTest` passed `13 tests / 680 assertions`; static scan found no weak/corrupted fallback labels in the view.
- Latest focused Student email notification UX hardening: student result-published, low-attendance, notice-published, fee-due, and fee-receipt emails now use readable missing-data labels, `Rs.` financial text, and an ASCII-safe shared email footer instead of `N/A`, corrupted currency, or HTML dash entities. Focused `StudentEmailUxTest` passed `1 test / 17 assertions`; adjacent `PortalFrontendBetaReadinessTest` passed `13 tests / 680 assertions`; adjacent `FeePaymentTest` passed `44 tests / 316 assertions`; static email-template scan passed.
- Latest focused Student mail subject UX hardening: related student result, attendance, fee-due, and fee-receipt Mailable subjects now use ASCII-safe separators and `Rs.` amount text instead of mojibake, Unicode dashes, or rupee symbols. Focused `StudentEmailUxTest` passed `2 tests / 29 assertions`; adjacent `PortalFrontendBetaReadinessTest` passed `13 tests / 680 assertions`; adjacent `FeePaymentTest` passed `44 tests / 316 assertions`; static mail-class scan passed.
- Dedicated non-admin operator roles for Library, Hostel, Transport, and Assets.
- Richer sortable headers/table-density improvements on some operations pages.
- More browser-level create/edit interactions where PHPUnit/frontend smoke already covers page/action-entry safety.

## Archive Rule

Do not append long sprint diaries here. If detailed historical notes are needed, add them to `docs/archive/` and keep this file under roughly 150 lines.

- Latest focused PMC real-world sequencing slice: PMC Timetable OS build sequence now links directly through student baskets, groups, faculty allocation, locked slots, generator, and publish/freeze; launch-control group diagnostics now respect PMC manager academic scope instead of counting unrelated program blockers. Focused `AcademicsPmcTimetableV041Test` passed `6 tests / 161 assertions`; adjacent `AcademicsPmcFrontendBetaReadinessTest` passed `8 tests / 345 assertions`; `git diff --check` passed for changed files.

- Latest focused PMC generator-boundary slice: PMC timetable generation now honors selected `batch_id` and actor PMC academic scope when selecting course groups, preventing another batch group from entering a batch-specific generated timetable. Focused `AcademicsPmcTimetableV041Test` passed `7 tests / 164 assertions`; adjacent `AcademicsPmcFrontendBetaReadinessTest` passed `8 tests / 345 assertions`.

- Latest canonical PMC timetable completion: seeded demo canonical sessions are idempotent and publish-ready, `academics:pmc-repair-canonical-timetable` repairs existing demo/local canonical rows, course delivery and teacher feedback prefer canonical official sessions, and `/academics/pmc/official-timetable` was browser-verified locally with `3 assigned groups`, `2 parallel`, corrected parallel room, and no console errors. Verified chunks: focused timetable/downstream `125 tests / 996 assertions`, course delivery `15 tests / 204 assertions`, attendance/student/admin chunks `43 tests / 324 assertions`, `npm run frontend:build`, `npm run frontend:smoke` `135 tests / 3978 assertions`, and `npm run frontend:smoke:mobile` `29 tests / 1473 assertions`. Full `artisan test` was attempted with the documented PHP runtime and exceeded the 10-minute local timeout before producing a result.
