# SchoolManagement Codex Project Context

## Runtime
- Laravel/PHP app in `C:\Users\mohd.naved\Documents\SchoolManagement\college-mgmt`.
- Local demo server usually runs on `http://localhost:8001`.
- Test runtime: `C:\tmp\php-8.5.7\php.exe` with `PHPRC=C:\tmp\php-8.5.7-codex-ini`.
- Run tests with: `$env:PHPRC='C:\tmp\php-8.5.7-codex-ini'; C:\tmp\php-8.5.7\php.exe artisan test`.

## Demo Users
- Admin: `admin@college.com` / `password`.
- Dean Academics: `dean@college.com` / `password`.
- PMC Head / Program Chair: `chair@college.com` / `password`.
- PMC Manager: `pmc.manager@college.com` / `password`.
- CoE: `exam@college.com` / `password`.
- IQAC Head: `iqac.head@college.com` / `password`.
- Program Leader / HOD: `hod@college.com` / `password`.
- Faculty mentor: `faculty.mentor@college.com` / `password`.

## Completed Department Baselines
- Admission OS completed through v0.039, including hierarchy, workflows, communication safety, assessments, offer/seat control, and Admission-to-Academics handoff.
- Academics OS v0.01 created flexible Academics hierarchy for Dean Office, PMC, CoE/Examination, IQAC, and Program Leadership.
- Academics OS v0.011 added Academics command center and attention workspaces.
- PMC OS v0.02 added initial PMC dashboard: curriculum readiness, faculty allocation, timetable readiness, student monitoring, and basic reports.
- PMC OS v0.03 added PMC command desk, workbench, curriculum governance, faculty workload, timetable control, student success, reviews/actions, saved views, reports, exports, seeder data, and tests.
- PMC OS v0.04 added complete PMC real-world operating workflows for planning, curriculum governance, faculty allocation, timetable governance, delivery, student success, reviews/actions, approvals, automation, analytics, exports, policy audit, seeded demo data, and tests.
- PMC OS v0.041 added realistic pre-timetable and timetable completion: term-wise student course allocation, student baskets, core sections, elective/lab/tutorial groups, exact group faculty assignments, faculty preferences/workload rules, locked/manual slots, constraint-based generator, conflict/quality scoring, planner views, version/freeze/impact workflows, substitution intelligence, notifications, reports, seeder data, and focused tests.
- PMC OS v0.042 upgraded timetable production realism: elective choice-window allocation with capacity/waitlist decisions, auto section/group build runs, enhanced timetable validation for student clashes, faculty availability, adjunct-day constraints, room capacity/type mismatch, daily faculty load, explainable quality scoring, solver attempt diagnostics, publish blocker checks, seeded demo data, browser-verified pages, and focused tests.
- PMC OS v0.043 added timetable publish/freeze lifecycle governance: publish from generation runs, block PMC publish when hard checks exist, Dean/Admin override with reason, official timetable version creation, workflow companion records for published/frozen/unfrozen/rollback states, lifecycle notifications, version board actions, seeded demo data, browser verification, and focused tests.
- PMC OS v0.044 added official scoped timetable views: PMC official audience timetable, student self-view based on exact course-group membership, faculty self-view based on group-faculty assignment, role-safe routes, compact filterable/paginated UI, browser-verified PMC page, and focused tests for student/faculty scope.
- PMC OS v0.045 added conflict resolution governance: detected timetable constraints can be converted into owner-assigned resolution actions, actions carry priority/status/due/evidence/closure notes, closing actions updates publish-check state, planner/reports show resolution actions, demo data includes open and resolved examples, and focused tests verify action lifecycle.
- PMC OS v0.046 added faculty availability governance: faculty/adjunct availability request submission, PMC approval/return/rejection workflow, approved requests apply to timetable faculty preference records used by the generator, PMC and faculty availability pages, seeded request/preference examples, browser verification, and focused tests.
- PMC OS v0.047 added faculty load governance: workload reviews are calculated from exact group faculty assignments, generated timetable items, and approved faculty preferences; reviews classify normal/underload/overload/critical load, support PMC approval/overload decisions with notes, display on load-planning pages, and are seeded/tested.
- PMC OS v0.048 added room/lab/resource readiness governance: reviews are calculated from generated timetable items and classrooms, capacity/lab/high-utilization risks are classified, PMC/Dean decisions require notes for exceptions/revisions, publish checks are updated from open room readiness blockers, reports display refresh/decision controls, and demo data/tests cover the workflow.
- PMC OS v0.049 hardened PMC timetable write scope: high-risk setup actions now enforce program/batch/term/subject write scope for PMC managers/officers/program leaders, while Dean Academics, Academic Owner, Director, Admin, and PMC Head retain override/oversight authority; tests prove in-scope writes pass and out-of-scope writes are blocked.
- PMC OS v0.050 added course basket exception governance before timetable locking: PMC can request add/drop/repeat/backlog/improvement/audit/open-elective exceptions, decisions update student course allocations, Dean-only approval is enforced for improvement/audit/credit-overload cases, exception flags are visible on course allocation pages, and seeded demo/tests cover the workflow.
- PMC OS v0.051 added section/group adjustment governance: PMC can request split/merge/rebalance/student-move/lock/unlock changes, impact and strength changes are recorded, Dean approval is enforced for merge/unlock/capacity-risk cases, approved changes update group strength/status/lock state and membership safely even when duplicate target membership exists, and group builder pages show adjustment controls and review queues.
- PMC OS v0.052 added faculty assignment acknowledgement governance: PMC can request acknowledgement for exact section/group faculty assignments, assigned faculty can accept, accept with constraints, raise concerns, or decline, PMC/Dean can review concerns with audit notes, unresolved acknowledgements update publish checks, and faculty-allocation pages show acknowledgement constraints.
- PMC OS v0.053 removed raw-ID entry from key PMC timetable operating forms by adding database-backed selectors for programs, batches, terms, subjects, students, course groups, faculty, rooms, slots, and timetable versions across allocation, grouping, faculty assignment, locked slots, generator, substitution, change request, availability, and scoped timetable filters.
- PMC OS v0.054 upgraded substitution intelligence from first-active-teacher selection to deterministic ranking by same-slot conflicts, faculty availability, adjunct-day fit, subject expertise, backup assignment, daily/weekly workload, and explainable reasons/conflict checks displayed on the substitution page.
- PMC OS v0.055 added curriculum validation control: refreshable database-backed validations for syllabus version, credit rules, course outcomes, CO-PO mapping, and rollout readiness; curriculum readiness scores update from validations and the curriculum page shows blockers and recommended actions.
- PMC OS v0.056 added concrete PMC planning/readiness workflow: planning cycles for annual/semester/program-term readiness, generated readiness checklist sections, blocker counts, checklist progress updates, planning approval/status changes, and conversion of readiness blockers into accountable PMC work items.
- PMC OS v0.057 added concrete student success intervention workflow: source-backed risk refresh from attendance/exam/grievance/mentor signals, retention-risk plans, intervention lifecycle records, parent/guardian escalation records, quick mentor/parent commands, seeded demo data, rendered student-success UI, and focused tests.
- PMC OS v0.058 added concrete course delivery checkpoint workflow: source-backed delivery refresh from timetable/attendance/marks/feedback/faculty assignment signals, planned-vs-conducted/missed sessions, delivery risk bands/scores, remedial action lifecycle, seeded demo data, rendered delivery UI, and focused tests.
- PMC OS v0.059 added section/group-level course delivery tracking: delivery trackers per exact course group, session delivery logs linked to timetable generation items, risk reasons/actions for missed group sessions and pending faculty logs, seeded demo data, compact delivery UI sections, and focused tests.
- PMC OS v0.060 added advanced PMC review/action governance: minutes approval with auto-created follow-up actions, action dependencies, reminders/escalations, action evidence register, evidence-based closure verification, seeded demo data, rendered review governance UI, and focused tests.
- PMC OS v0.061 added demand-driven timetable generation: weekly session demand records per course group, multi-session scheduling from faculty weekly hours/group rules, session type/index/duration tracking on generated items, operational timetable-entry sync on publish, seeded demand data, rendered timetable demand UI, and focused tests.
- PMC OS v0.062 hardened timetable solver quality: break/lunch slots are excluded from teaching placement, multi-slot labs/tutorials require physically contiguous non-break slot blocks, occupancy blocks every slot in a multi-slot class for faculty/room/group/student checks, break-slot and incomplete-block hard constraints are detected, student compactness uses slot order, seeded demo data shows break-slot diagnostics, and focused tests cover the rules.
- PMC OS v0.063 improved operational timetable quality checks: faculty consecutive-teaching pressure is flagged from actual slot order, student/course-group daily load pressure is detected from configured group limits, avoidable same-day student gaps are flagged, compactness scoring expands multi-slot sessions by ordered slots, seeded reports show the new warnings, and focused tests cover the quality rules.
- PMC OS v0.064 added strategy-aware timetable placement: feasible day/slot/room candidates are now ranked instead of accepting the first feasible slot, strategies influence placement for student compactness, faculty balancing, adjunct priority, and room optimization, generated items store placement score/reasons, solver diagnostics record strategy-aware scoring, seeded demo data reflects the strategy ranking, and focused tests prove compact-student placement behavior.
- PMC OS v0.065 added timetable solver explainability for manual PMC review: generated items store the top alternate day/slot/room candidates with score and reasons, the generator table shows solver score/reasons/alternatives, the generation item model exposes its generation-run relationship, seeded demo rows include alternatives, and focused tests verify alternatives persist and render.
- PMC OS v0.066 made solver alternatives actionable: PMC can apply a stored alternate placement from the generator table, the route validates the stored alternative and draft run state, updates day/slot/room/confidence/metadata, preserves previous placement, refreshes timetable quality/publish checks, writes an audit event, and focused tests cover the workflow.
- PMC OS v0.067 added conflict-safe solver alternative application: applying an alternative now refreshes pre/post quality, rolls back automatically if the move introduces new hard conflicts, records blocked-alternative metadata, allows only Dean/Admin/Academic Owner/Director override with explicit reason, audits override use, and focused tests cover rollback and override paths.
- PMC OS v0.068 added safe manual generated-item moves: PMC can manually move a generated class to a selected day/slot/room from the generator table, the move validates draft run state and scoped access, refreshes quality/publish checks, rolls back if new hard conflicts appear, allows Dean/Admin/Academic Owner/Director override with explicit reason, writes audit events, and focused tests cover safe move, rollback, and override.
- PMC OS v0.069 added source-backed pre-publish timetable impact preview: PMC can refresh impact rows from a generation run before publish/revision, including affected students, faculty, rooms, course groups, manual/alternative changes, conflicts, and notification audience; results are stored in timetable impact records, linked in run metadata, shown on the generator page, audited, seeded-compatible, browser-verified, and tested.
- PMC OS v0.070 made timetable publish impact-safe by automatically refreshing source-backed generation impact preview during every successful publish, embedding the reviewed impact summary into the lifecycle workflow publish summary, preserving the existing hard-conflict gate, and testing the official publish path.
- PMC OS v0.071 added impact-aware publish notification metadata: queued student/faculty timetable publish notifications now carry generation run id, reviewed impact preview, audience count, quality/conflict summary, and operational sync counts so notification rows are auditable and tied to the publish impact review.
- PMC OS v0.072 upgraded timetable notification reports: reports now filter notifications by type/recipient/status and show compact operational context from notification metadata, including audience count, affected student/faculty/room/group counts, operational sync count, generation run, quality score, and hard/soft conflicts.
- PMC OS v0.073 added individual publish notification audit: successful timetable publish now creates per-faculty and per-student queued notification rows from generated timetable items and active course-group memberships, preserving the existing audience-level rows while enabling recipient-level delivery/status tracking.
- PMC OS v0.074 added notification delivery-state operations: PMC can mark timetable notifications queued/sent/read/failed/cancelled from reports, failure/cancellation requires a note, status history is retained in notification metadata, and every status change is audited.
- PMC OS v0.075 added timetable notification retry operations: failed/cancelled notifications can be requeued from reports, retry count/notes/next retry time/status history are retained in metadata, retry actions are audited, and seeded demo data exposes the retry control.
- PMC OS v0.076 added a guided semester launch control on the PMC Timetable OS dashboard: ordered launch stages cover course baskets, sections/groups, faculty allocation, readiness inputs, generation/validation, and publish/notify with blocker counts, next action guidance, and filtered source links.
- PMC OS v0.077 added course-basket readiness diagnostics: PMC now sees total/ready allocations, ungrouped baskets, waitlists, pending exceptions, credit overload baskets, validation-flagged allocations, recommended action text, and launch-stage blocker integration on timetable and student-basket pages.
- PMC OS v0.078 added section/group readiness diagnostics: PMC now sees total/ready groups, unlocked groups, under-minimum groups, over-capacity groups, groups without faculty, ungrouped allocations, pending adjustments, strength mismatches, recommended action text, and launch-stage blocker integration on timetable and group-builder pages.
- PMC OS v0.079 added faculty allocation readiness diagnostics: PMC now sees exact assignment readiness, assigned groups, groups missing primary faculty, groups without backup coverage, pending/missing acknowledgements, missing faculty preferences, unapproved assignments, load-review blockers, overload reviews, recommended action text, and launch-stage blocker integration on timetable and faculty-allocation pages.
- PMC OS v0.080 added readiness-input diagnostics: PMC now sees faculty preference completeness/restriction signals, active/hard/soft locked slots, missing locked-slot context, hard-lock collisions, room-readiness blockers, lab-not-ready counts, capacity exceptions, recommended action text, and launch-stage blocker integration on timetable and locked-slot/readiness pages.
- PMC OS v0.081 added generation/validation diagnostics: PMC now sees latest generation status, scheduled/unscheduled classes, hard conflicts, soft warnings, quality score/band, solver attempts, failed attempts, open resolution actions, blocking publish checks, impact-preview readiness, stale input-source count, recommended action text, and launch-stage blocker integration on timetable and generator pages.
- PMC OS v0.082 added publish/freeze readiness diagnostics: PMC now sees official version status, lifecycle/approval status, missing official version/workflow flags, blocking publish checks, pending change requests, failed/queued notifications, operational sync count, impact records, affected students/faculty, recommended action text, version-board links, failed-notification report links, and launch-stage blocker integration on timetable and version/freeze pages.
- PMC OS v0.083 added substitution emergency diagnostics: PMC now sees today/tomorrow substitution recommendations, uncovered classes, pending and low-score recommendations, failed/queued substitution notices, same-day change requests, repeated faculty and repeated course-group substitution risk, blocker totals, and recommended action text on timetable and substitution-intelligence pages.
- PMC OS v0.084 added course-delivery execution diagnostics: PMC now sees faculty session-log gaps, planned/missed/cancelled/rescheduled sessions, attendance/lesson-plan/material update gaps, missing planned/covered topics, behind groups, overdue reviews, open remedials, blocker totals, and recommended action text on course-delivery and delivery-risk pages.
- PMC OS v0.085 added student-success intervention effectiveness diagnostics: PMC now sees overdue interventions, stale risk-plan reviews, parent-call follow-through, missing parent outcomes, intervention evidence gaps, repeat-risk students, escalated interventions, closure effectiveness rate, blocker totals, and recommended action text on student-success/intervention pages; the legacy student-success page links to the current v0.085 surface.
- PMC OS v0.086 added high-risk PMC route enforcement diagnostics and hardening: generation validation/publish, timetable freeze/unfreeze/rollback, conflict resolution actions, version-linked change requests, and group-linked substitution recommendations now use scope-aware checks where source context exists; the policy audit page shows tracked high-risk routes, scope-aware routes, broad-write routes, missing audit rows, untested rows, and recommended action text.
- PMC OS v0.087 added allocation pressure diagnostics: PMC now sees elective choice-window pressure, submitted/allocated/waitlisted choices, pending choice students, waitlist subjects, add/drop exceptions, repeat/backlog cases, Dean-approval pending cases, duplicate student-subject baskets, active students with no basket, single-course baskets, manual overrides, recent allocation rounds, pressure totals, and recommended action text on the timetable dashboard plus allocation/student-basket pages.
- PMC OS v0.088 added faculty suitability diagnostics: PMC now sees subject expertise gaps, adjunct-day availability risk, restriction notes/unavailable slots, acknowledgement concerns/declines, overload risks, unapproved suitability, backup-only primary gaps, suitable assignment count, blocker totals, and recommended action text on timetable and section/group faculty-allocation pages.
- PMC OS v0.089 made faculty suitability operational instead of informational: suitability is now a readiness checklist item, the launch-control faculty stage includes suitability/expertise blockers, generation surfaces sync a run-scoped `faculty_suitability` publish check, normal PMC publish is blocked when the generated run's exact course groups have suitability blockers, Dean/Admin override behavior remains available, and term/run scoping prevents unrelated faculty preferences or load reviews from blocking the wrong timetable.
- PMC OS v0.090 added student course basket self-service: students can review allocated/waitlisted courses, group/section membership, timetable preview, submit allocation acknowledgements, raise objections/add-drop/waitlist follow-ups, PMC can review those requests, objection/add-drop submissions create allocation-exception records, demo data targets the primary demo student, and focused tests/browser checks verify the flow.
- PMC OS v0.091 added student elective choice self-service: students can view active elective choice windows, rank elective preferences, submit choices with duplicate/max-window validation, choices are stored in `academic_pmc_elective_choices`, PMC allocation consumes submitted choices through the existing allocation processor, demo data includes an open window, and focused tests verify the student-to-PMC allocation path.
- PMC OS v0.092 added PMC data reconciliation and health checks: PMC can refresh and review database-backed checks for generated-to-operational timetable sync, allocated course baskets versus student subject enrollments, group memberships versus approved allocations, scheduled groups versus delivery trackers, and published timetable notification coverage; seeded demo rows expose OK/warning states, lifecycle-linked timetable runs are counted across publish/freeze/revision states, and focused tests verify mismatch detection.
- PMC OS v0.093 made reconciliation actionable: mismatched checks can now run scoped repair actions for generated-to-operational timetable sync, allocation-to-enrollment links, group membership allocation links, scheduled-group delivery trackers, and missing timetable publish notification audit rows; repairs are write-authorized, audited, refresh reconciliation after execution, respect legacy enrollment enum values, and focused tests verify mismatch repair.
- PMC OS v0.094 added source-backed reconciliation drilldown: every refresh stores bounded sample mismatch records for timetable sync, allocation/enrollment links, group membership links, delivery tracker coverage, and publish notification coverage; the reconciliation table renders compact sample mismatches so operators can see representative affected records before running repair.
- PMC OS v0.095 added reconciliation export/audit: PMC users can export the current filtered data reconciliation view as CSV with counts, status, recommendations, and sample mismatches; exports honor status/group filters and write `academic_pmc_export_logs` records for auditability.
- PMC OS v0.096 added scheduled/CLI data reconciliation: `academics:pmc-reconcile-data` refreshes checks through the existing policy-authorized service, supports `--repair` for safe repair actions, uses admin/Dean/PMC actor fallback without bypassing authorization, is scheduled hourly, and is covered by command tests.
- PMC OS v0.097 added reconciliation run observability: scheduled/manual CLI reconciliation executions now persist run history with source, status, repair flag, actor, check/mismatch/critical/repair counts, failure reason, and metadata; the reconciliation page shows recent runs so PMC can audit scheduler health without server log access.
- PMC OS v0.098 completed reconciliation demo readiness: `AcademicsOperatingDemoSeeder` now seeds realistic completed, repair, and failed reconciliation run-history rows so the reconciliation page is database-populated immediately after a fresh demo seed, and focused tests assert the seeded run history renders.
- PMC OS v0.099 completed manual reconciliation audit coverage: UI-triggered Refresh Checks and per-check Repair actions now write `academic_pmc_data_reconciliation_runs` records with actor, source, repair flag, counts, repaired count, route metadata, and check key, so scheduled and manual reconciliation operations share one auditable run history.
- PMC OS v0.100 hardened manual reconciliation failure observability: UI refresh and repair actions now create a running run-history row before work starts and update it to completed or failed with failure reason and exception metadata, so failed manual reconciliation attempts are auditable instead of disappearing into request logs.
- PMC OS v0.101 made reconciliation run history actionable: the data reconciliation page now shows run summary counts for total/completed/failed/running/manual-repair runs, supports `run_status` filtering with visible filter summary, and links failed/running/completed run counts to filtered run-history views.
- PMC OS v0.102 added reconciliation run-history exports: PMC can export the current filtered run-history view as CSV, exports honor `run_status`, include source/status/repair/count/failure details, and write `academic_pmc_export_logs` rows with report key `data_reconciliation_runs`.
- PMC OS v0.103 added reconciliation scheduler health signals: the reconciliation page now shows scheduler health status, last successful run, last failed run, stale running count, and recommended action; stale running runs are seeded for demo warning coverage and running-run notes are visible in the run table.
- PMC OS v0.104 added stale reconciliation run closure: PMC can mark stale running reconciliation runs as failed with a required reason, fresh running runs are protected from premature closure, closure metadata records actor/time/reason, and the run-history table exposes the action only for eligible stale runs.
- PMC OS v0.105 added department activity audit for stale run closure: marking a stale reconciliation run failed now writes a `DepartmentActivityLog` entry with action `academic_pmc_v105_reconciliation_stale_run_closed`, subject run id/type, actor, reason, source, and started-at metadata.
- PMC OS v0.106 made reconciliation audit operationally visible: the data reconciliation page now shows a compact recent audit trail for refresh, repair, stale-run closure, and reconciliation subject activity, including actor, action, reason/details, timestamp, and subject reference.
- PMC OS v0.107 added reconciliation audit export governance: PMC can export the recent reconciliation audit trail as CSV with action filtering, actor/action/details/subject columns, and `academic_pmc_export_logs` records under report key `data_reconciliation_audit`.
- PMC OS v0.108 added operator-facing reconciliation audit filtering: the data reconciliation page now exposes an Audit Action filter, visible audit filter summary, and all-audit reset controls while preserving status/group/run filters.
- PMC OS v0.109 completed demo readiness for reconciliation audit governance: `AcademicsOperatingDemoSeeder` now seeds database-backed refresh, repair, and stale-run closure audit rows tied to demo reconciliation checks/runs, so the audit trail and filters are populated immediately after a fresh seed.
- PMC OS v0.110 added reconciliation audit date-window governance: the audit trail page controls and CSV export now support `audit_from`/`audit_to` filters, visible date filter summaries, export-log capture of action/from/to filters, and empty-state behavior for date windows with no audit events.
- CoE OS v0.03, IQAC OS v0.04, Program Leadership OS v0.05, Course Delivery OS v0.06 are present as operating slices.
- Dean Academics OS v0.07 added Dean command dashboard, attention queues, branch health, program risk, reviews/actions, handoff, calendar, reports, and exports.
- Dean Academics OS v0.08 added planning cycles, advanced reviews/actions, risk governance, approval cockpit, faculty workload, student success, curriculum, exam readiness, quality, induction, analytics, calendar, saved views, and policy audit.

## Current Sprint
- Academics PMC OS v0.110 is the current verified baseline.
- Scope delivered: v0.041 timetable workflow plus v0.042 elective choice allocation/auto grouping/solver validation, v0.043 publish/freeze/rollback lifecycle governance, v0.044 official scoped timetable views, v0.045 conflict resolution actions, v0.046 faculty availability request/approval workflow, v0.047 faculty load review/approval workflow, v0.048 room/lab readiness review/publish-check workflow, v0.049 record-level write-scope enforcement for high-risk timetable setup actions, v0.050 course basket exception governance for add/drop/repeat/backlog/improvement/audit/open-elective workflows, v0.051 section/group adjustment governance for split/merge/rebalance/student-move/lock/unlock workflows, v0.052 faculty assignment acknowledgement governance, v0.053 database-backed selector controls for key PMC timetable forms, v0.054 ranked substitution intelligence with explainable checks, v0.055 curriculum validation control, v0.056 concrete PMC planning/readiness workflow, v0.057 concrete student success intervention workflow, v0.058 concrete course delivery checkpoint/remedial workflow, v0.059 exact section/group/session-level delivery tracking, v0.060 advanced review/action governance with evidence-based closure verification, v0.061 weekly session-demand timetable generation plus operational timetable sync, v0.062 break-safe contiguous multi-slot timetable placement with hard validation constraints, v0.063 operational timetable quality checks for faculty consecutive load, student group daily load, and avoidable student gaps, v0.064 strategy-aware candidate ranking for timetable placement, v0.065 solver alternatives/explainability for manual PMC review, v0.066 apply-alternative workflow with audit/quality refresh, v0.067 conflict-safe alternative apply rollback/override governance, v0.068 safe manual generated-item moves from selected day/slot/room with rollback/override governance, v0.069 source-backed pre-publish timetable impact preview, v0.070 automatic publish-time impact refresh in lifecycle summaries, v0.071 impact-aware publish notification metadata, v0.072 metadata-aware notification reporting with filters, v0.073 individual publish notification audit rows, v0.074 notification delivery-state operations, v0.075 retry operations for failed/cancelled timetable notifications, v0.076 guided semester launch control with ordered stages, blockers, next actions, and source links, v0.077 course-basket readiness diagnostics integrated into launch blockers and student basket pages, v0.078 section/group readiness diagnostics integrated into launch blockers and group-builder pages, v0.079 faculty allocation readiness diagnostics integrated into launch blockers and faculty allocation pages, v0.080 readiness-input diagnostics integrated into launch blockers and locked-slot/readiness pages, v0.081 generation/validation diagnostics integrated into launch blockers and timetable-generator pages, v0.082 publish/freeze readiness diagnostics integrated into launch blockers and version/freeze pages, v0.083 substitution emergency diagnostics integrated into timetable and substitution-intelligence pages, v0.084 course-delivery execution diagnostics integrated into course-delivery and delivery-risk pages, v0.085 student-success intervention effectiveness diagnostics integrated into student-success/intervention pages with a legacy-page link, v0.086 high-risk route enforcement diagnostics plus scope-aware hardening for key timetable lifecycle actions, v0.087 allocation pressure diagnostics integrated into timetable/allocation/student-basket pages, v0.088 faculty suitability diagnostics integrated into timetable/faculty-allocation pages, v0.089 run-scoped faculty suitability publish enforcement integrated into readiness, launch control, generator publish checks, and publish blocking, v0.090 student course basket self-service with acknowledgements, objections, add/drop/waitlist follow-up, PMC review, and demo-student data, v0.091 student elective choice self-service feeding the existing PMC elective allocation processor, v0.092 data reconciliation checks for timetable sync, allocation/enrollment links, group memberships, delivery trackers, and notification coverage, v0.093 write-authorized reconciliation repair actions for fixable data drift, v0.094 source-backed sample mismatch drilldown on reconciliation checks, v0.095 filtered CSV export plus export audit logging for reconciliation reports, v0.096 scheduled CLI reconciliation command with optional repair, v0.097 durable run-history observability for scheduled/manual reconciliation executions, v0.098 seeded demo run-history rows for completed/repair/failed reconciliation runs, v0.099 UI refresh/repair actions logged into the same reconciliation run history, v0.100 failed manual refresh/repair attempts logged with failure reason/exception metadata, v0.101 summary cards plus `run_status` filtering for reconciliation run history, v0.102 filtered CSV export plus export audit logging for reconciliation run history, v0.103 scheduler health signals with stale-running detection and recommended action, v0.104 operator closure for stale running reconciliation jobs with required reason and metadata, v0.105 DepartmentActivityLog audit entries for stale run closures, and v0.106 visible recent reconciliation audit trail on the reconciliation control page.
- Latest v0.110 addition: audit date-window filtering for reconciliation audit trail UI and CSV export.
- Latest cross-app hardening pass: student course-content routes now require active subject enrollment; teacher assignment/material/announcement/exam/attendance writes now enforce taught-subject/student-roster scope; student fee status uses active `fee_demands` plus active penalties as the outstanding source of truth; admin fee dashboard/collection now prefers demand-ledger balances when demands exist, filters selected-student fee structures by course, and blocks direct wrong-course payment submissions.
- Latest CoE/Exam hardening pass: Exam Cell grade sheets, marks entry, hall-ticket lists, and hall-ticket downloads now use subject/term enrollment eligibility instead of program-only rosters, preventing marks or hall tickets for in-program students who are not enrolled in the exam subject.
- Latest student admit-card hardening pass: student admit-card list/download now uses active subject/term eligibility from `student_subject_enrollments` or compatible legacy enrollments, preventing same-program but unenrolled exams from appearing or being downloaded.
- Latest admin communication hardening pass: Admin Bulk Mail now builds query-backed audiences instead of materializing full recipient collections, counts only deliverable unique emails, filters "All Active Students" through active student profiles, filters program/batch sends through active matching students, and chunks queued sends for large recipient sets.
- Latest student subject-registration hardening pass: student self-registration now shows only active subjects for the student's current program/term, writes canonical `student_subject_enrollments`, synchronizes compatible legacy `enrollments` when a matching semester exists, uses `credits` instead of obsolete `credit_hours`, and drops subjects by marking both canonical and legacy rows dropped.
- Latest student timetable hardening pass: student timetable now reads canonical `student_subject_enrollments` plus compatible legacy enrollments, filters active timetable rows by enrolled subject plus student program/batch/current term scope, and groups numeric `day_of_week` values by model day name so valid Monday-Saturday rows render correctly.
- Latest student results/transcript hardening pass: GradeService, student results, and transcript generation now discover semesters and subjects from canonical `student_subject_enrollments` plus compatible legacy enrollments, so PMC student course-basket allocations without legacy rows still produce result, SGPA/CGPA, and transcript reports.
- Latest student/teacher attendance hardening pass: teacher attendance rosters and attendance submission authorization now include canonical active `student_subject_enrollments` matched through timetable `term_id` or compatible semester-to-term mapping, while student attendance summaries and session drilldowns now resolve PMC term-backed timetable rows and block subject drilldowns for unenrolled students.
- Latest student exam-registration hardening pass: student exam registration lists, direct registration actions, and dashboard upcoming-exam cards now filter subject exams through active canonical `student_subject_enrollments` plus compatible legacy enrollments, with term-to-semester mapping for legacy exam records, so same-program but unenrolled subject exams are hidden and direct registration is forbidden.
- Latest academic transcript hardening pass: Academic transcript screen/PDF now use one shared report builder, include only exams for subjects the student is actually enrolled in through canonical `student_subject_enrollments` or compatible legacy enrollments, map PMC terms to legacy semester exam records, and use the current `subjects.credits` field instead of obsolete `credit_hours`.
- Latest admin academic roster hardening pass: admin exam result entry and admin attendance marking now build rosters from canonical active `student_subject_enrollments` plus compatible legacy enrollments, map timetable/exam semester records to PMC terms, reject submitted result/attendance rows for students outside the actual roster, and include term-backed timetable rows in admin attendance reports/exports/PDFs.
- Latest admin enrollment synchronization pass: admin manual and bulk subject-enrollment workflows now keep legacy `enrollments` and canonical `student_subject_enrollments` in sync, map selected semesters to student program/batch terms, use valid canonical enrollment types (`compulsory`/`elective`), reactivate existing rows, validate every bulk subject id, and mark matching canonical rows dropped when a legacy enrollment is removed.
- Latest parent portal academic visibility pass: parent dashboard SGPA/attendance, child attendance, and child results now use canonical enrolled subjects plus compatible legacy enrollments, resolve PMC term-backed timetable/result rows through semester-to-term mapping, and hide rogue same-term attendance/results for unenrolled subjects.
- Latest course feedback hardening pass: student course feedback now lists and accepts feedback only for active canonical `student_subject_enrollments` plus compatible legacy enrollments, fixes the submitted-state rendering bug, blocks direct feedback routes for unenrolled subjects, stores feedback against the resolved feedback term, and teacher feedback aggregation now counts only feedback from actually enrolled students while rendering response counts/comments correctly.
- Latest student course hub hardening pass: student course hub index/detail now uses canonical active `student_subject_enrollments` plus compatible legacy enrollments, removes the invalid `Subject::teachers` dependency that could crash the course list, shows faculty names from active timetable assignments, hides unenrolled subjects, and permits legacy active enrollments on detail pages.
- Latest student discussion-board hardening pass: discussion index/show/store/reply/resolve now share one active canonical-or-compatible-legacy enrollment check, dropped or unenrolled subjects are blocked, legacy active enrollments can use discussions, new questions are term-stamped from the resolved enrollment, and thread owners must still have current subject access before marking discussions resolved.
- Latest student attendance-condonation hardening pass: condonation create/store now calculate eligible low-attendance subjects only from active canonical `student_subject_enrollments` plus compatible legacy enrollments, reject direct requests for unenrolled or >=75% attendance subjects, persist the resolved term, and prevent duplicate open requests for the same subject/term.
- Latest student marks-appeal hardening pass: marks appeal create/store now expose and accept only non-absent results for subjects the student is actively enrolled in through canonical `student_subject_enrollments` or compatible legacy enrollments, blocking direct appeals for rogue unenrolled-subject results or absent results.
- Latest student leave hardening pass: student leave creation now works against the shared teacher/student leave table by making teacher linkage nullable and adding attachment support, stamps student-originated records with `leave_type=student`, computes leave days, and blocks overlapping pending/approved leave ranges while allowing rejected history.
- Latest student placement eligibility hardening pass: student placement listing/apply now enforces drive minimum CGPA using result-derived student CGPA, shows ineligible-drive guidance in the UI, and blocks direct low-CGPA applications while preserving status/deadline/duplicate checks.
- Latest assignment workflow hardening pass: student assignment listing/show/submit now use canonical active `student_subject_enrollments` plus compatible legacy enrollments, teacher submission rosters use the same real enrolled-student source, the submissions view posts grading to the correct route/payload, and grading blocks rogue non-roster submissions plus marks above assignment maximum.
- Latest student quiz hardening pass: quiz listing/show/start/submit/result now use canonical active `student_subject_enrollments` plus compatible legacy enrollments, legacy-enrolled students can complete valid quizzes, and submitted option IDs are accepted only when the option belongs to the current question.
- Latest student materials/announcements hardening pass: direct subject material and announcement pages now accept canonical active `student_subject_enrollments` plus compatible legacy enrollments, keep unpublished materials hidden, and continue blocking students outside the actual subject enrollment.
- Latest student scholarship eligibility pass: scholarship schemes now support structured minimum CGPA, maximum family income, and proof-document requirements; student scholarship lists show eligibility context and block direct ineligible applications; required proof uploads are stored and available to admins through a protected proof-download route.
- Latest admission applicant scholarship hardening pass: applicant scholarship awards now enforce active schemes, program compatibility, structured CGPA/family-income eligibility, verified-document requirements for proof-based schemes, duplicate prevention, and max amount/seat limits from direct staff routes.
- Latest test/data stability pass: `ClassroomFactory` now generates unique `room_number` values to match the database unique constraint and prevent random full-suite failures.
- Latest fee-demand scholarship handoff pass: academic fee-demand generation now carries awarded/disbursed applicant scholarships through completed enrollment confirmations into student fee demands, caps deductions at total fees, and manual demand create/update now reject deductions above total amount to prevent negative final balances.
- Latest library circulation hardening pass: library reservations are now operational for students and staff, student catalog pages show reservation actions/status from the database, staff get a reservation queue with fulfil/cancel actions, direct issue respects pending reservation priority, damaged/lost copies are blocked, and issue due dates cannot exceed membership rules.
- Latest student NOC/library-clearance pass: library NOC eligibility now groups student/teacher borrower checks correctly, unpaid returned fines and unreturned books are scoped to the requested user, and admin NOC document approval/fulfillment is blocked until library clearance is complete.
- Latest admission gateway webhook hardening pass: gateway webhook payment resolution is now provider-scoped, conflicting order/payment identifiers no longer mutate either admission payment, and payment-id-only webhooks cannot verify a payment owned by another configured provider.
- Latest student fee-payment proof hardening pass: student payment proof submissions now use active fee demands as the source of truth, block amounts above the selected demand or total open balance, and prevent duplicate pending proof submissions for the same demand.
- Latest fee-payment proof verification workflow pass: Admin/Accounts now have a payment-proof review queue with filters, proof download, verify, and reject actions; verification creates a fee receipt/payment, updates linked demand balances to partially/fully paid, maps NEFT/RTGS/UPI proof methods safely into stored fee payments, and rejection records staff notes.
- Latest NOC fee-clearance hardening pass: student NOC document approval and fulfillment now require both library clearance and academic fee-demand clearance, blocking active pending/partial/overdue fee balances while allowing fully paid historical demands.
- Latest NOC hostel-clearance hardening pass: student NOC document approval and fulfillment now also require hostel fee clearance, blocking pending hostel fee balances while allowing paid or waived hostel demands.
- Latest student career-event hardening pass: student career-event registration now closes for unpublished, past-date, deadline-past, or full-capacity events while allowing registration on the deadline date; direct POST behavior and listing visibility are regression-covered.
- Latest CMC career-event operations pass: students can cancel open unattended event registrations, attended/closed registrations are protected, and CMC staff can mark attendance from the event registration list with wrong-event URL protection.
- Latest CMC career-event type hardening pass: CMC event create/edit/filter pages now use the database-safe canonical event types (`seminar`, `mock_interview`, `workshop`, `company_visit`, `career_fair`, `other`), invalid legacy hyphenated values are rejected before persistence, and staff-side create/filter behavior is regression-covered.
- Latest CMC career-event deletion hardening pass: CMC can delete empty events, but deletion is blocked once students have registered so registration and attendance history is preserved; the event list disables delete for registered events.
- Latest CMC career-event schedule/capacity hardening pass: new events cannot be created in the past, registration deadlines must be on or before the event date, and event capacity cannot be reduced below the current registration count.
- Latest transport assignment hardening pass: admin transport assignment now rejects inactive stops, assignment dropdowns only offer active routes/stops/vehicles, and assignment end dates cannot be earlier than the assignment start date.
- Latest transport fleet governance pass: admin transport now has a vehicle fleet update workflow; capacity cannot be reduced below active assignments, assigned vehicles cannot be moved to maintenance/inactive, and fleet rows show active assignment counts.
- Latest asset/inventory hardening pass: asset returns now reject return dates before the assignment date; inactive consumable inventory items cannot receive or issue stock through direct routes; the asset register UI shows inactive stock clearly and hides movement actions for inactive items.
- Latest hostel room lifecycle hardening pass: hostel room updates now reject capacity reductions below active occupants and block moving occupied rooms to maintenance/reserved; new allocations and transfers now require an active block plus an available or partially occupied room, closing reserved/inactive-block bypasses from direct routes.
- Latest hostel outpass lifecycle hardening pass: pending outpasses with past out times can no longer be approved; approved future outpasses cannot be marked returned before the approved out time; the hostel outpass list now shows expired/not-out-yet states instead of unsafe action buttons.
- Latest hostel complaint closure hardening pass: resolving or closing hostel complaints now requires resolution notes, reopened/in-progress complaints clear `resolved_at`, and the admin complaint modal labels resolution notes as required for resolved/closed states.
- Latest grievance lifecycle hardening pass: admin grievance resolve/close now requires resolution notes, reopened grievances clear resolution metadata, and students can close only staff-resolved grievances instead of silently closing open/under-review/escalated issues.
- Latest departmental grievance hardening pass: HOD/department grievance resolve/escalate direct routes now enforce lifecycle guards, allowing resolution only for open/under-review/escalated records and escalation only for open/under-review records, preserving resolved/closed history.
- Latest Program Chair grievance hardening pass: Program Chair grievance updates now enforce assigned program scope, require resolution notes before resolving, stamp `resolved_by`, and block resolved/closed grievance history from being changed through direct Program Chair routes; the Program Chair grievance table marks locked history rows as non-editable.
- Latest leave workflow hardening pass: teacher leave creation now blocks overlapping pending/approved ranges; Admin, HOD, and Program Chair leave review routes now allow decisions only on pending applications; Admin cannot delete reviewed leave history; Program Chair leave actions enforce assigned-program scope; and an additive migration guarantees `leave_applications.reviewed_at` exists for HOD/PMC review timestamps.
- Latest attendance condonation hardening pass: student condonation requests now persist the requested session deficit, Program Chair condonation approve/reject routes enforce assigned-program scope and pending-only decisions, approvals cannot condone more sessions than requested, reviewed decisions stamp `reviewed_at`, and the PMC condonation list shows actionable requested-session values from the database.
- Latest elective override hardening pass: Program Chair elective override direct routes now enforce assigned-program scope, active elective-enrollment status, same program/term active elective replacements, duplicate active-enrollment prevention, and audit metadata (`previous_subject_id`, `override_reason`, `overridden_by`, `overridden_at`) on canonical student subject enrollments.
- Latest term promotion hardening pass: term promotion approvals/rejections now operate only on pending or on-hold records, approval requires the student still being in the promotion source term, reviewed promotion history cannot be edited/reapproved/rejected, edit screens no longer bypass transition logic by directly setting approved/rejected, and bulk approval processes only reviewable eligible current-term records.
- Latest student subject-registration hardening pass: student self-service registration no longer reactivates completed subject history, and self-service drops are limited to active elective enrollments in the student's current term; compulsory, completed, or old-term enrollments are protected from direct drop routes while valid drops still synchronize canonical and legacy enrollment rows.
- Latest admin enrollment hardening pass: admin manual and bulk subject enrollment now require semester-to-term mapping, active subjects matching each student's program/term, completed canonical history is not reactivated, bulk enrollment skips unsafe rows and reports when nothing eligible is created, and completed legacy enrollment history cannot be deleted from enrollment management.
- Latest teacher exam-result hardening pass: teacher result entry now validates nested result payloads, rejects marks above the exam total, requires marks unless the student is marked absent, keeps roster-scope enforcement, and clears stored marks when an absent result is submitted.
- Latest admin exam-result hardening pass: admin result entry now accepts the existing UI `marks` payload plus legacy `marks_obtained`, validates marks against exam total, requires marks for non-absent students, clears marks/grade when absent, preserves canonical roster enforcement, and stores grade/remarks consistently.
- Latest marks-appeal review hardening pass: Exam Cell appeal review now locks resolved/rejected history, requires remarks for final decisions, requires revised marks for approvals, blocks revised marks above exam total, stores revised marks only for approved appeals, and updates the linked exam result only through valid approvals.
- Latest exam anomaly hardening pass: Exam Cell anomaly resolution now requires resolution notes, persists those notes, stamps resolver metadata, blocks repeat resolution of already closed anomaly history, and the anomaly detail action form exposes required resolution-note input plus error feedback.
- Latest Exam Cell grade-sheet hardening pass: CoE/Exam Cell marks save now validates marks against exam total, requires marks unless the student is marked absent, keeps enrolled-roster enforcement, and stores absent results with null marks instead of zero so reports distinguish true zero from absence.
- Latest Exam Cell result-publication hardening pass: CoE/Exam Cell result publication now records `published_at`/`published_by`, blocks publication until every eligible roster student has marks or an absence record, blocks unresolved anomaly cases and pending marks appeals, prevents repeat publication, and preserves existing result remarks instead of overwriting moderation notes.
- Latest post-publication result-lock pass: Teacher, Admin, and Exam Cell result-entry save routes now block direct marks/absence/grade/remark edits after `exams.published_at` is set, and their result-entry pages show a locked published-results state so official results can only change through controlled appeal/correction workflows.
- Latest published-exam schedule-lock pass: Admin and Exam Cell exam schedule update/delete routes now block changes after result publication, and exam list/detail/edit pages replace unsafe edit/delete controls with a locked published-exam state so official exam/result history cannot be invalidated by direct schedule mutations.
- Latest official-result appeal gate pass: student marks appeals now expose and accept only published official exam results; draft/unpublished marks remain hidden from appeal creation and direct appeal POSTs are rejected until the Exam Cell publishes the result.
- Latest official-result visibility pass: student results, student dashboard SGPA/CGPA, student academic summary, parent dashboard/results, GradeService reports, academic transcripts, and scholarship CGPA fixtures now treat only exams with `published_at` as student/parent/officially visible results; draft marks remain operational staff data until publication.
- Latest student result API visibility pass: `/api/v1/student/results` now returns only published official exam results, closing the mobile/API path that could expose draft marks even after the web result pages and PDF/report builders were hardened.
- Latest result-publication notification pass: successful Exam Cell result publication now queues idempotent per-student `result_published` notifications with links back to the official results page, so students are actively informed when previously hidden draft marks become official.
- Latest Stage 2 workflow-integrity pass: fee demands are now protected from unsafe financial state changes. Fully paid demands are locked from ordinary edits, non-zero demands cannot be manually marked fully paid, paid/linked demand history cannot be deleted, and only untouched pending demands remain deletable. Admin student/teacher delete actions now archive records by marking profiles inactive, removing active roles, and writing activity logs so academic, fee, attendance, timetable, and result history is preserved.
- Latest Stage 2 academic master-data integrity pass: admin program, batch, term, subject, and department write routes now protect dependent academic history. Programs with students/batches/subjects/exams/timetable/PMC records cannot be deleted or structurally changed; batches cannot be cancelled or reduced below current strength and cannot be deleted with operational dependencies; terms with enrollment/exam/timetable/fee/PMC history cannot be deleted; subjects with enrollment/exam/timetable/course-content/PMC history cannot be deleted or structurally changed; departments with programs/courses/subjects/staff/students/hierarchy records cannot be deleted. Empty setup records remain deletable.
- Latest Stage 2 CMC placement lifecycle integrity pass: Admin and CMC placement drive routes now share lifecycle guards. Drives with applications cannot be deleted, drives cannot be completed while applied/shortlisted/interview applications remain open, selected-offer drives cannot be cancelled, vacancies cannot be reduced below selected offers, direct admin student-add respects drive status/deadline/duplicate/active-student rules, final selected/rejected/withdrawn placement decisions are locked from ordinary status reversal, and selected counts cannot exceed vacancies.
- Latest Stage 2 legacy PMC timetable route hardening pass: legacy Program Chair timetable write routes no longer grant all-program write access to unassigned chairs, scoped chairs are blocked from out-of-program timetable saves, imports, copies, auto-schedule acceptance, and publishes, nested auto-schedule suggestions are checked for batch/subject program ownership, and published timetable versions are locked from legacy save/publish/import/copy/auto-schedule mutation routes so changes must move through the newer PMC revision/version workflows.
- Latest Stage 2 approval final-state hardening pass: legacy HOD and Program Chair approval action routes now verify the approval queue role and pending status before approving or rejecting. Finalized approval records cannot be reapproved/rejected through these direct role-specific routes, and HOD/Program Chair users cannot action another role queue even when the applicant is otherwise in scope.
- Latest Stage 2 academic governance integrity pass: Academic calendar, curriculum-change, and OBE write routes now use explicit academic policy checks instead of relying only on the broad `academic/*` route role list. Accounts users remain able to access their academic fee surfaces but cannot directly mutate academic planning, curriculum, or OBE records. Program Chair/HOD users can still submit curriculum and manage OBE planning data, Dean/Admin-level users review final curriculum decisions, finalized curriculum changes cannot be reapproved/rejected, and OBE outcomes with mapping/attainment/survey history cannot be deleted through direct routes.
- Latest Stage 2 legacy PMC timetable save compatibility pass: scoped legacy Program Chair `chair.timetable.save-slot` now creates operational timetable entries with required legacy `semester_id` and `course_id` bridge values derived from the selected term/program, so guarded legacy save paths no longer fail against the older non-null timetable schema while still preserving program scope and published-version locks.
- Latest Stage 2 academic scholarship lifecycle pass: legacy academic scholarship write routes now require explicit scholarship-management authority instead of the broad academic role set, blocking Program Chair/HOD/Exam-style direct financial-aid mutations. Scholarship create/update now requires a positive percentage or fixed discount, fixed-amount-only scholarships persist safely with `percentage=0`, overlapping active duplicate awards for the same student/name/type are rejected, and delete now archives by setting `status=inactive` instead of destroying scholarship history.
- Latest Stage 2 term-promotion authority pass: academic term-promotion write routes now require explicit promotion-management authority instead of the broad academic role set, blocking Accounts/Exam-style direct promotion mutations. Promotion approval and bulk approval now also require the destination term to be a later term in the same program and batch, preventing stale or malformed promotion records from moving students into unrelated terms.
- Latest Stage 2 manual fee-payment ledger pass: admin manual fee collection now applies paid receipts to active fee demands oldest-first, blocks manual overpayment above the student's active open demand balance, and keeps manual receipt behavior aligned with verified payment-proof demand reduction.
- Latest Stage 2 hostel fee-waiver audit pass: hostel fee waivers now require a staff-entered reason, record waived timestamp and actor, and paid hostel demands record the staff actor while clearing any stale waiver metadata. Waived/paid hostel fee history remains final-state protected and continues feeding hostel/NOC fee-clearance checks.
- Latest Stage 2 student scholarship approval integrity pass: admin scholarship approval now revalidates the application at decision time, blocking inactive schemes, wrong-program schemes, missing required proof files, failed CGPA thresholds, and missing/excess family-income eligibility before any award amount is approved. This closes stale/direct application records that previously could bypass student-side eligibility checks.
- Latest Stage 2 admission refund lifecycle pass: admission refund creation now enforces one active refund per applicant, requires linked payments to belong to the applicant and be verified, and blocks requested/approved amounts above remaining verified refundable balance. Refund approve/reject/process routes now enforce pending/approved lifecycle transitions, require positive approved amounts, and prevent processed refund history from being silently reversed or reprocessed.
- Latest Stage 2 admission payment verification finality pass: admission payment verify/reject routes now operate only on pending payments, preventing verified payments from being rejected/reverified and rejected payments from being revived through direct staff routes. Rejection notifications now use the actual staff verification note, keeping applicant-facing guidance aligned with the stored audit note.
- Latest Stage 2 applicant scholarship disbursement integrity pass: applicant scholarship awards now require positive amounts, disbursement references must be unique, disbursement rechecks applicant/scheme compatibility and current scheme maximum before funds leave the queue, and disbursement notes are appended without destroying original award notes.
- Latest Stage 2 admission document verification finality pass: admission document verify/reject routes now operate only on pending applicant documents, preventing verified documents from being rejected/reverified and rejected documents from being revived through direct queue routes. The applicant-detail document action method also has the same pending-only guard if it is routed later.
- Latest Stage 2 admission offer finality pass: applicant-facing and staff-facing offer accept/decline routes now block stale issued offers when the applicant is already in a final admission state (`rejected`, `withdrawn`, or `enrolled`), preventing old offer links or direct staff routes from changing finalized applicant outcomes.
- Latest Stage 2 admission merit decision lock pass: merit list regeneration is blocked once matching applicants have active issued/accepted offer letters, direct merit decision changes are blocked for entries linked to active offers, and final non-pending merit outcomes cannot be changed to another outcome through ordinary decision routes. Pending entries remain decidable through the existing workflow.
- Latest Stage 2 admission enrollment finality pass: enrollment now locks the applicant row during confirmation, generates deterministic applicant-based enrollment numbers instead of `max(id)+1`, rejects duplicate roll numbers within the same batch before creating student/handoff side effects, and regression-covers repeat enrollment so completed confirmations cannot create duplicate students or handoff records.
- Latest Stage 2 student scholarship disbursement integrity pass: student scholarship disbursement now stores a structured unique disbursement reference, revalidates scheme activity, program eligibility, proof/CGPA/income requirements, approved amount, scheme maximum, and remaining seat capacity at disbursement time, and blocks stale or duplicate-reference disbursements before finalizing financial aid.
- Latest Stage 2 shared approval-chain integrity pass: generic approval chain history now has schema-backed workflow metadata (`workflow_type`, `step_order`, `sla_hours`, `parent_approval_id`), assigned-role/admin/recorded-approver access control on the chain view, and regression coverage proving direct unrelated users cannot inspect approval history while valid shared-chain approvals advance exactly once to the next approver.
- Latest Stage 2 student document-request integrity pass: inactive student profiles can still view their document request history, but new official document request creation and direct POST submission are now blocked unless the student status is active. This prevents archived/inactive profiles from generating new bonafide/NOC/ID/letter workflows through student self-service.
- Latest Stage 2 academic-year master-data integrity pass: academic years with linked semesters, batches, or fee structures can no longer be deleted or have start/end years or dates rebounded through admin routes, preventing cascade loss of semester/fee setup and invalid academic boundaries. Safe label/current-year updates remain allowed.
- Latest Stage 2 semester master-data integrity pass: semesters with enrollments, exams, or timetable entries can no longer be deleted or have academic year, number, or dates rebounded through admin routes, preventing cascade loss of roster/exam/timetable history. Safe name/current-semester updates remain allowed.
- Latest Stage 2 legacy course master-data integrity pass: legacy course records with students, fee structures, admissions, exams, or timetable entries can no longer be deleted or structurally changed through admin routes, preventing cascade loss of student/fee/timetable history. Courses with active students cannot be deactivated, while safe label/description updates remain allowed.
- Latest Stage 2 classroom master-data integrity pass: classrooms with timetable or exam history can no longer be deleted, structurally changed, deactivated while active schedules/upcoming exams depend on them, or reduced below active timetable batch capacity. Safe label/capacity increases remain available while preserving room allocation history.
- Latest Stage 2 timetable-slot master-data integrity pass: timetable slots used by timetable entries, teacher availability, PMC locked slots, generated timetable items, or session delivery logs can no longer be deleted, have their start/end time or break classification changed, or be deactivated through admin routes. Safe name/sort-order updates remain allowed so labels can be cleaned up without invalidating schedules.
- Latest Stage 2 fee-structure receipt-history integrity pass: fee structures linked to fee receipt/payment history can no longer be deleted or financially restructured through admin routes, preventing the legacy `fee_payments.fee_structure_id` cascade from destroying receipt history or changing historical receipt meaning. Unused fee setup records remain editable and deletable.
- Latest Stage 2 admission configuration lifecycle pass: required document requirements linked to applicant uploads, selection steps linked to sessions/scores, and admission fee installments linked to admission payments are now protected from destructive deletes and contract/financial restructuring through both admin configuration and admission staff routes. Safe description/instruction-style updates remain available where they do not reinterpret applicant history.
- Latest Stage 2 admission seat-matrix integrity pass: admission seat matrices with selection, waitlist, offer, or completed enrollment history can no longer be deleted, and category/total capacity cannot be reduced below already selected/offered/enrolled applicant commitments. Capacity increases remain allowed so institutes can expand intake without invalidating historical decisions.
- Latest Stage 2 admission waitlist-promotion integrity pass: manual waitlist promotion now requires a configured seat matrix and checks both total capacity and applicant-category capacity before changing a waitlisted merit entry to selected. This prevents direct promotion from bypassing published seat controls when no matrix exists or when a category is already full.
- Latest Stage 2 admission offer auto-promotion integrity pass: applicant-facing and staff-facing offer decline auto-promotion now uses shared seat-capacity checks. Promotion requires a configured seat matrix, excludes the declined applicant's released seat from current commitments, checks total and applicant-category capacity across selected merit entries, active offers, and completed enrollments, and skips final-state or already-offered waitlisted applicants.
- Latest Stage 2 student exam-registration finality pass: student exam registration now hides and blocks result-published exams, and reviewed exam-registration records (`approved` or `rejected`) cannot be reset to `pending` through repeated student self-service POSTs. Pending/idempotent registration remains available for eligible future exams.
- Latest Stage 2 assignment submission finality pass: student assignment self-service can no longer overwrite a graded submission or reset it back to `submitted`, preserving marks, feedback, grader, and graded timestamp. Non-graded resubmissions remain allowed before deadline rules, and resubmissions without a new file preserve the previous uploaded file path.
- Latest Stage 2 quiz attempt finality/visibility pass: quiz submission now rechecks that the quiz is still active and published before scoring, preventing already-started attempts from being submitted after closure or unpublish. Student result viewing now respects `show_result_immediately` while the quiz is active and releases results once the quiz window has closed.
- Latest Stage 2 teacher content archival pass: teacher material and subject announcement delete routes now soft-delete records instead of destroying them. Published teaching content history and material file references remain available via archived records, while normal teacher/student lists continue to hide archived content.
- Latest Stage 2 admin timetable entry history pass: admin timetable entries with attendance, substitution, published status, or version history can no longer be structurally changed through direct update routes. Delete requests for history-bearing entries now archive/deactivate the entry and preserve attendance/history rows; empty draft entries remain physically deletable.
- Latest Stage 2 parent profile archival pass: admin parent delete now soft-deletes the parent profile, preserves the user account and `parent_student` linkage history, removes the active parent role, and writes an archive activity log instead of cascading through parent/student portal history.
- Latest Stage 2 library fine finality pass: library fine payment now requires a returned/lost issue with a positive unpaid fine, blocks direct payment of active issued/overdue fines before final return calculation, prevents duplicate fine payments, and return processing resets positive recalculated fines to unpaid so NOC/clearance cannot be bypassed by stale pre-return fine state.
- Latest Stage 2 CMC company lifecycle pass: admin company deletion now archives recruiter records with placement-drive or internship history by deactivating the company and writing an activity log, preserving placement drives, selected offers, and internship rows that would otherwise be cascade-deleted. Empty setup company records remain deletable.
- Latest Stage 2 profile self-delete hardening pass: generic `/profile` account deletion now blocks institutional student, teacher, parent, and applicant accounts from self-deleting users that own academic/admission/portal history. Plain non-operational users can still delete their account, while institutional accounts must go through the relevant admin/archive workflow.
- Latest Stage 2 user-role integrity pass: admin user-role assignment now grants the actual Spatie authorization role in addition to writing the `user_roles` audit row, reactivates expired assignments safely, revokes actual roles when the final active assignment is removed or expired, expires indefinite roles correctly, and blocks current/final admin role removal from the user-role management routes.
- Latest Stage 2 notification archival pass: user notification delete now soft-deletes records instead of destroying operational notification history, while normal notification lists/counts continue to hide archived records. Ownership policy coverage also confirms direct routes cannot mark-read or archive another user's notifications.
- Latest Stage 2 notice visibility pass: student, parent, and API notice reads now share a database-backed `Notice::visibleTo()` scope that enforces published status, publish/expiry dates, and audience-role visibility. Direct student/API notice detail routes now return 404 for future, expired, or non-visible audience notices instead of exposing internal/future communications.
- Latest Stage 2 notice archival pass: admin notice delete now soft-deletes institute notices instead of destroying official communication history. Archived notices remain hidden from admin/student/parent/API normal reads through default model scoping while preserving the original notice row for audit/history queries.
- Latest Stage 2 applicant document replacement hardening pass: applicant self-service uploads now block replacement of already verified admission documents before validation/storage, preserving the verified file path, version, verifier, and timestamp. Rejected or pending document replacement remains available through the existing applicant correction flow.
- Latest Stage 2 applicant admission-payment proof hardening pass: applicant self-service admission fee proof submission now validates that the installment is active and belongs to the applicant's program/batch, blocks duplicate pending or verified proofs for the same applicant/installment, and still allows rejected proofs to be corrected through a new pending submission.
- Latest Stage 2 applicant admission gateway-payment hardening pass: applicant gateway initiation now requires the payment record to still be pending, and gateway webhooks no longer mutate verified or rejected admission payment records. Final staff decisions remain authoritative even if delayed gateway events arrive later. Parent result visibility regression coverage was also stabilized to assert against the rendered draft mark value instead of random CSRF-token text.
- Latest Stage 2 student document/NOC lifecycle hardening pass: official document requests now require explicit approval before staff can upload and mark a document ready. Pending requests can no longer be fulfilled directly through the admin route, and the admin queue hides the ready-upload control until the request is in approved/processing state, preserving review/audit order for NOC and other official documents.
- Latest Stage 2 transport assignment eligibility hardening pass: admin transport assignment creation now revalidates the selected student status at POST time, blocking inactive/archived student profiles from receiving new active transport assignments through direct routes even though the UI already lists only active students.
- Latest Stage 2 transport assignment end-date hardening pass: active transport assignments can no longer be ended with a future date through direct routes. Immediate ending now requires an end date on or before today, preventing future service from being marked inactive early and freeing student/vehicle eligibility before service actually ends.
- Latest Stage 2 asset assignment return-date hardening pass: active asset assignments can no longer be returned with a future date through direct routes. Returning an issued asset now requires a returned date on or before today, preventing assets from being marked available/maintenance before they are actually returned.
- Latest Stage 2 inventory movement date hardening pass: consumable inventory receive and issue routes now reject future movement dates before changing stock or writing movement history. Current stock can no longer be inflated or reduced today by a future-dated receipt/issue record submitted through direct routes.
- Latest Stage 2 library lost-issue return hardening pass: admin library return routes now allow returns only for active `issued` or `overdue` records. Lost or otherwise finalized issue records can no longer be returned through a direct route that would make a lost copy available again or inflate book availability counts.
- Latest Stage 2 library borrower eligibility hardening pass: admin library issue routes now revalidate borrower status at POST time, blocking inactive/archived student and teacher profiles from receiving new book issues through direct routes even though the issue UI already lists only active borrowers.
- Latest Stage 2 library reservation eligibility hardening pass: student library reservation creation now revalidates active student status at POST time, blocking inactive/archived student profiles from creating new reservation queues through direct routes while preserving read-only access to their existing library page/history.
- Latest Stage 2 library membership limit integrity pass: admin membership create/update now checks the borrower's active issued/overdue books before saving. A membership limit can no longer be lowered below the current active issue count through direct routes, preventing inconsistent circulation limits while books are already out.
- Latest Stage 2 student fee-payment proof eligibility hardening pass: inactive/archived students can still view fee payment submission history, but the create/store routes now block new payment proof submissions and the student payment page hides the submit action with database-backed guidance.
- Latest Stage 2 student fee-payment verification hardening pass: admin/accounts proof verification now blocks pending proofs linked to already closed fee demands and blocks unlinked proofs whose amount exceeds the student's current open fee balance, preventing stale student proofs from creating paid receipts that no active demand can absorb.
- Latest Stage 2 fee-demand ledger finality pass: academic fee demands can no longer be created as `fully_paid` with a non-zero open balance, and demands with linked payment activity can no longer have amount, scholarship deduction, final balance, or due-date ledger fields rewritten through direct update routes. Audited adjustment/refund/waiver workflows remain the expected path for financial corrections.
- Latest Stage 2 hostel fee generation eligibility pass: monthly hostel fee generation now skips stale active allocations whose student profile is inactive/archived, preventing new hostel receivables from being created for non-active students while still counting those records as skipped for operator feedback.
- Latest Stage 2 hostel allocation eligibility pass: hostel allocation UI now lists only active students, direct allocation POST blocks inactive/archived student profiles, and stale active allocations for inactive students cannot be transferred to keep hostel occupancy alive; staff must vacate or reactivate the student through the proper workflow.
- Latest Stage 2 student scholarship eligibility finality pass: student scholarship self-service now blocks inactive/archived students from submitting new applications, and admin approval/disbursement rechecks active student status so stale approved applications cannot create scholarship benefit history after the student leaves active enrollment.
- Latest Stage 2 scholarship scheme finality pass: admission scholarship scheme updates can no longer reduce available seats below existing applicant-awarded/student-approved usage or reduce maximum amount below existing awarded/approved/disbursed amounts, preserving scholarship commitments already made through admission and student scholarship workflows.
- Latest Stage 2 accounts export audit pass: accounts fee-collection, admission-payment, and outstanding-fee CSV exports now write `ActivityLog` export entries with actor, row count, surface, and visible filter summary before streaming the file, preserving traceability for sensitive finance data exports without changing CSV content.
- Latest Stage 2 admission enrollment finality pass: applicants with completed enrollment confirmations can no longer be moved to contradictory CRM statuses such as withdrawn through direct status routes, and bulk applicant status updates skip enrolled applicants. Completed enrollment changes must go through academic student lifecycle or a future audited cancellation workflow.
- Latest Stage 2 student placement result-visibility pass: the legacy `Student::calculateCGPA()` helper now counts only non-absent marks from published exams, keeping placement eligibility and other model-helper consumers aligned with official result publication boundaries. Draft/unpublished marks can no longer make a student eligible for a placement drive before CoE/Exam publication.
- Latest Stage 2 student placement eligibility pass: inactive/archived student profiles can still review placement application history, but the student placement page now disables new applications with database-backed guidance and direct placement application POSTs reject non-active students before creating `placements` rows.
- Latest Stage 2 student career-event eligibility pass: inactive/archived student profiles can still view published career events and existing registration history, but student career-event registration and cancellation routes now reject non-active students before mutating `career_event_registrations`; the event list hides register/cancel controls and shows active-student-only guidance.
- Latest Stage 2 CMC career-event attendance finality pass: once a career-event registration is marked attended, the normal CMC attendance queue now locks that attended state and direct update attempts cannot silently revert it to absent. The registration page shows an attended-lock badge instead of a reversible "Mark absent" action.
- Latest Stage 2 CMC internship eligibility pass: CMC internship creation now revalidates the selected student at POST time and rejects inactive/archived student profiles before creating `internships` rows. The create form already lists active students only; the direct route now matches that operational boundary.
- Latest Stage 2 CMC registered-event contract pass: career events with registration history can no longer have core contract fields (`event_type`, `event_date`, `registration_deadline`) rewritten through the update route. Safe descriptive edits and capacity increases remain available, while capacity still cannot drop below registered count.
- Latest Stage 2 CMC company lifecycle pass: CMC and Admin company update routes now preserve recruiter identity after placement-drive or internship history exists by blocking company-name rewrites, and companies with upcoming/ongoing placement drives cannot be deactivated through update routes. Safe contact/detail edits remain available.
- Latest Stage 2 student grievance eligibility pass: inactive/archived student profiles can still view existing grievance history, but the student grievance create and direct store routes now block new grievance submission before opening fresh operational cases.
- Latest Stage 2 student leave eligibility pass: inactive/archived student profiles can still view leave application history, but the student leave create and direct store routes now block new leave submissions; the leave index hides the apply action and shows active-student-only guidance.
- Latest Stage 2 teacher leave eligibility pass: inactive/archived teacher profiles can still view leave application history, but teacher leave create, direct store, and pending-leave cancel routes now block new mutations unless the teacher profile is active. The teacher leave index hides apply/cancel controls for inactive teachers, shows active-teacher-only guidance, and displays submitted leave reasons in history rows.
- Latest Stage 2 teacher course-content mutation pass: inactive/archived teacher profiles can still view their assigned-course material, assignment, announcement, and submission history, but direct material upload/archive, assignment create, announcement post/archive, and assignment grading routes now require an active teacher profile. Teacher UI hides those mutation controls and shows active-teacher-only/locked guidance while preserving history visibility.
- Latest Stage 2 teacher attendance/result mutation pass: inactive/archived teacher profiles can still open assigned attendance and exam result-entry pages for history/review, but direct attendance-save and exam-result-save routes now require an active teacher profile. Attendance and result-entry pages show locked guidance and disable mutation controls for inactive teachers, while existing published-result locks remain in place.
- Latest Stage 2 mentor identity/active-profile pass: mentoring now consistently stores and resolves mentor ownership through the mentor user's ID, matching the `students.mentor_id` and `mentor_meetings.mentor_id` schema. PMC mentor assignment still accepts teacher profile IDs from the UI but persists the teacher user ID, teacher mentor dashboards use that identity, inactive teacher profiles can view mentee history but cannot send mentor messages or schedule meetings, and mentor detail attendance summaries now derive subjects through timetable entries instead of a non-existent attendance subject column.
- Latest Stage 2 student mentor workflow pass: inactive/archived student profiles can still view mentor details, message history, and meeting history, but student mentor message and meeting-request POST routes now require an active student profile. The student mentor page resolves the mentor as a user record, hides message/meeting forms for inactive students, and shows locked guidance instead of exposing actions that direct routes reject.
- Latest Stage 2 parent dashboard notice visibility pass: parent dashboard recent-notice previews now use the same `Notice::visibleTo()` scope as the full parent notice list, enforcing publish/expiry dates, soft-deleted notice hiding, and audience rules. Parent dashboards now show only `all`/student-family notices and no longer leak teacher/admin/future/expired notices through the preview card.
- Latest Stage 2 student course-feedback finality pass: inactive/archived students can still view enrolled-subject feedback history, but feedback create/store routes now require an active student profile and the student feedback page hides submit controls with active-student-only guidance. Direct duplicate feedback POSTs no longer overwrite existing submitted feedback rows; one submitted response per student/subject/term is preserved.
- Latest Stage 2 student subject-registration eligibility pass: inactive/archived students can still view current subject registrations and available subjects, but subject add/drop routes now require an active student profile and the subject-registration page hides add/drop controls with locked guidance. This prevents inactive profiles from changing academic rosters, timetable memberships, and legacy enrollment rows through self-service direct routes.
- Latest Stage 2 attendance-condonation eligibility pass: inactive/archived students can still view attendance condonation history, but condonation create/store routes now require an active student profile and the condonation page hides the new-request action with locked guidance. This prevents archived profiles from creating new attendance-eligibility exceptions through direct self-service routes.
- Latest Stage 2 student hostel self-service eligibility pass: inactive/archived students can still view hostel outpass and complaint history, but outpass and hostel-complaint create routes now require an active student profile even if a stale active hostel allocation exists. Student hostel pages hide operational forms and show active-student-only guidance while preserving historical hostel records.
- Latest Stage 2 student resume-builder eligibility pass: inactive/archived students can still view saved resume history, but resume save routes now require an active student profile and the resume builder renders as a read-only locked form with active-student-only guidance. This prevents archived profiles from changing placement/career readiness records through direct POSTs.
- Dean Academics, Academic Department Owner, Director, and Admin retain full PMC oversight; PMC Head/Manager/Officer operate within PMC scope. Existing v0.02/v0.03/v0.04 PMC routes and legacy `chair.*` routes remain stable.
- Current verification baseline: focused `StudentResumeWorkflowTest` passed 2 tests / 16 assertions; adjacent student career/placement regression passed 41 tests / 276 assertions; full `php artisan test` passed 926 tests / 5231 assertions. The previous grievance route list and frontend build baselines remain valid unless those areas are touched again. Historical focused gates remain recorded above for prior hardening passes.

## Development Rules
- Keep schema additive and SQLite-test compatible.
- Keep all existing routes stable unless adding compatible links.
- Extend `AcademicsOperatingDemoSeeder` for every new Academics/PMC surface.
- Use compact, professional, data-dense UI.
- Growing tables should have pagination and export/current-view links.
- Dashboard metrics should link to filtered source lists or relevant workbench pages.
